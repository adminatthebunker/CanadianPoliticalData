# TODO

Actionable checkbox view of [`docs/timeline.md`](./docs/timeline.md). When this file disagrees with the timeline or with a plan doc under [`docs/plans/`](./docs/plans/), **the plan docs win** — update this file rather than the other way round.

- **Last synced with `docs/timeline.md`:** 2026-05-23 (corpus-wide naive-last-name-attribution audit + BC pre-1990 wrong-surname-collision cleanup — 11,066 BC rows reset, 10,039 re-attributed correctly to historical politicians via three-tier disambiguation; ~1,100 opennorth ingest-date-data-quality residue across NB/NS/MB/BC flagged for separate cycle. Audit query is reusable. Prior cycle 2026-05-22 NS Premier Tier-2 Pass-3 shipped via agent — `ROLE_ONLY_NAME_PATTERNS` companion map added as a reusable seam for jurisdictions that stamp role into `speaker_name_raw` instead of `speaker_role`; bonus chunks-reconcile latent-bug fix that retroactively self-heals AB/BC/MB/SK after future timeout aborts; +1,904 NS attributions, **NS 96.73% → 99.70%** — now highest-attributed provincial Hansard. ON middle-initial-aware resolver — agent rejected the brief's destructive "merge Dave/David Cooke" premise after verifying via `ola_member_id` that they're two different MPPs serving simultaneously in different ridings; shipped a non-destructive `resolve_on_speakers_middle_initial()` pass instead; +2,016 ON attributions, **ON 92.89% → 93.11%**. QC bio re-fetch with widened-end-marker regex completed + dated resolver re-run bagged +6,458 attributions; **QC 84.52% → 86.67%** (+2.15pp), purely operational re-run of yesterday's parser improvements. Prior cycle 2026-05-21 cross-province staff/group sweep + audit-driven schedule closures from Total **43,444 rows reclassified** across the three provinces: QC 27,737 (24,608 chorus `Des voix`/`Une voix` → group + 3,129 `Le Secrétaire` → staff); AB 6,192 (4,896 `Hon. Members`/`Some Hon. Members` → group + 1,296 `The Clerk`/`The Sergeant-at-Arms`/`The Clerk Assistant` → staff, including a `Sergeant At Arms` ↔ `Sergeant-at-Arms` canonicalization in `_normalize_role`); ON 9,515 (6,482 newly-tagged chorus from `An hon. member`/`Some hon. members`/`Interjection` + 3,033 already-tagged staff `Clerk`/`Clerk-at-the-Table`/`Deputy Clerk` family). **Five provinces now have group/staff classification (MB, NL, QC, AB, ON);** NS is the only major outlier remaining at `staff=0`, but its empty-role bucket is a different shape (`THE PREMIER` ALL-CAPS, 1,904 rows — Tier-2 Premier-roster Pass-3 work, not staff/group classification). Attribution % unchanged across the staff/group sweep; the win is honest-coverage signal. **Plus QC deeper attribution ship same day:** the existing `resolve-qc-speakers-dated` Click command (shipped 2026-04-27 alongside the historical MNA roster) had **never been scheduled** — daily cron only ran the name-only `resolve-qc-speakers`. Manual first run attributed **+10,917 rows** (Couillard / David / Gaudreault / Ouellet families resolved at 99.4% hit rate when single-date-candidate match); QC attribution **79.1% → 82.6%** (+3.5pp). Schedule seeded at 16:32 UTC between the name-only resolver (16:30) and `fetch-qc-bill-introduced-dates` (16:35); also added to `scripts/seed-daily-ingest-schedules.sql` for pgdata-rebuild reproducibility. Remaining 26,868 surname-ambiguous rows scoped this cycle: ~4,500 blocked by a roster-coverage gap (Khadir / Deltell / James / Malavoy / Hivon / Weil / Doyer / Vien — all 2008-2022 retirees, absent from `politicians` because `qc_former_mnas` ingester walks pre-2008 alphabet listings only). Latent SSL bug in `qc_former_mnas.py` fixed (httpx `verify=False` added matching `qc_bills.py` precedent — was failing every alphabet fetch on the current Python image's CA bundle) + weekly Sun 04:20 UTC schedule seeded (latent-gap-3 of the cycle). Recent-retiree URL family + re-run flagged as separate "QC recent-retiree roster" cycle. ~21K genuine surname-ambiguity rows (Tremblay/Couillard families) need document-level continuity or per-mandate constituency_id backfill — deferred deep refactors. **UPDATE same day via 4-agent parallel batch:** QC roster gap fix turned out to be a `deputes/` vs `patrimoine/` namespace collision in `qc_assnat_id` (not a missing URL family) — Khadir/Deltell/Hivon/Weil/James/Doyer/Vien/Malavoy all backfilled, +6,090 QC attributions; ON dated-resolver rewrite (date-window + first-token disambiguator) +4,088 ON attributions; QC doc-continuity resolver shipped + scheduled but hypothesis invalidated (18 rows lift — bucket lacks bootstrap mentions). QC final: **79.1% → 84.52%** (+5.42pp, +16,989 attributions across the day). Audit MEDIUM bucket triaged to 0, 5 new weekly Sunday schedules.)
- **Why this exists:** the timeline is prose-shaped; this is the version you tick off. One source of priority ordering (timeline), one place to mark progress (here).
- **How to update:** check items as they ship, move them to *Recently shipped*, and re-sync the date above. If a horizon shifts, edit `docs/timeline.md` first, then mirror here.

---

## Now (in flight)

Partially built — finish, do not start new things on top.

- [x] **`/api/v1/search` finalization — v1.0 contract frozen 2026-05-12** (commits `93bcf85` + `<phase-2>`). Hybrid HNSW + BM25 wired since `1df626f` (2026-05-03). Final cycle was pure docs: audited every schema in `services/api/src/routes/search.ts` against `docs/api.md`, fixed the `totalCapped`→`capped` field-name bug, documented the missing knobs (`politician_active`, `anchor_chunk_id`, `include_count`, `source_system`, `chunk_ids` on `/facets`, `?limit` on `/facets`), added the four endpoints that were live but undocumented (`/speeches/count`, `/chunks/:id`, plus polish on `/sessions` + `/meta` + `/politician-quotes`), then declared v1.0 with a version banner, a Stability & Versioning section (backward-compat / breaking / 6-month deprecation policy), and an Error responses section enumerating 400/404/429/503 (incl. the `EmbeddingServiceUnavailableError → 503 {code:"embedding_service_unavailable"}` mapping). Schema source of truth is the route file; docs are the bug if they drift. Unblocks the deferred Later **Public developer API** workstream (it derives `/api/public/v1/*` from this contract). → [`docs/api.md`](./docs/api.md) § Stability & Versioning
- [x] **Premium reports — phase 1c follow-ups.** Phase 1b (LLM map-reduce, `/reports/<id>` viewer, refund flow) shipped. Phase 1c #1 (public-share + citation, migration 0036) shipped. **Phase 1c #4 (report-this-search) shipped 2026-05-05 with broader scope** — became a generic analysis-jobs substrate (`report_jobs.kind` discriminator, migration 0045) with two new search-set kinds (`search_synthesis` + `stance_map`), an "analyse top N" picker (25/50/100/200/500/Other…) with custom-N modal, a kind-aware viewer, and dashboard-style aggregation tables baked into the report HTML. Remaining ranked queue: re-run on new evidence (#3), per-section flagging (#6), compare two politicians (#2). → [`docs/plans/premium-reports-followups.md`](./docs/plans/premium-reports-followups.md)
- [x] **Stripe — live-mode deploy** (2026-05-05). Live keys + price IDs swapped in; webhook endpoint registered in Stripe Dashboard live mode (separate list from test mode); test-mode `users.stripe_customer_id` nulled to clear customer-namespace mismatch. **Stripe Tax not yet activated** — that's still dashboard work + flipping `STRIPE_TAX_ENABLED=true`. → [`docs/operations.md`](./docs/operations.md) § Initial Stripe activation walkthrough, § Stripe Tax
- [x] **Websites provenance + Tier-3 Sonnet agent + `/admin/websites` review UI** (2026-05-12). Migration 0047 mirrors `politician_socials`' provenance shape onto the `websites` table (source / confidence / evidence_url / flagged_low_confidence / discovered_at / last_verified_at + partial index + `v_websites_missing` view). New `websites_agent.py` (Sonnet 4.6 + `web_search_20250305`, hard cap 3 searches per politician), `websites.py` canonicalize+upsert helper, `agent-missing-websites` Click command + admin/jobs_catalog whitelist, and `/admin/websites` review page mirroring `/admin/socials`. **Agent infra ready but not enabled** — no `scanner_schedules` entry seeded; 144 previously-flagged rows from prior on-demand runs already in the review queue. Drive-by: tightened `socials_agent` search budget 3 → 2 per (politician, platform) with explicit `max_uses`. Commits `10c9f75` + `930f5aa`. → [`docs/operations.md`](./docs/operations.md) § Agent-driven enrichment

---

## Next 1 — Database: finish the corpus

Priority one until remaining Hansard pipelines are live and votes are modelled. Every provincial Hansard build is gated on the **research-handoff rule** (CLAUDE.md convention #5) — pause and ask the user for their endpoint research before probing.

### Remaining Hansard pipelines (4 left; NT shipped 2026-04-29)

- [x] **NT Hansard** — consensus-government schema. Drupal HTML scrape with direct `nt_mla_slug` FK attribution (cleanest of any sub-national pipeline). 19 current + 117 former MLAs, 13th-20th Assembly Speaker roster. → [`docs/runbooks/handoff-2026-04-29-nt-hansard.md`](./docs/runbooks/handoff-2026-04-29-nt-hansard.md)
- [x] **NU Hansard — shipped 2026-05-16** (commit `54358ba`). Drupal-9 PDF source at assembly.nu.ca/hansard, 59 sittings 2021-02-24 → 2024-05-31, **11,439 speeches**, 5th Assembly 95.8% / 6th Assembly 96.5% non-presiding attribution. English-primary with `(interpretation)` markers for Inuktitut passages; token-based surname index + first-name disambiguator + Mr.-Speaker-comma false-positive filter. Companion `nu_former_mlas.py` ingester pulls 5th + 6th Assembly rosters (47 MLAs incl. 11 returners + 3 by-election replacements) from Wikipedia with multi-word Inuit-language surname fallback. Daily 21:15 UTC schedule with `since_days=14`. The 537 truly-unresolved are Committee-of-the-Whole departmental witnesses — correctly NULL. → [`docs/research/nunavut.md`](./docs/research/nunavut.md)
- [x] **SK Hansard** — shipped 2026-05-05. HTML pipeline (Word HTML at docs.legassembly.sk.ca for 30L) + PDF parser (29L1S–29L3S, AM/EVE supplementary sittings) + presiding-speaker resolver + bills ingester (`progress-of-bills.pdf`, 367 bills across 8 sessions). 50,948 speeches across 360 sittings; `hansard_status='live'`, `bills_status='partial'`. → [`docs/research/saskatchewan.md`](./docs/research/saskatchewan.md)
- [ ] **PE Hansard** — sits behind WAF/CAPTCHA. Research-handoff gated; may require Playwright/Camoufox track (see *Later*). → [`docs/research/prince-edward-island.md`](./docs/research/prince-edward-island.md)
- [ ] **YT Hansard** — same WAF/CAPTCHA bucket as PE. Research-handoff gated. → [`docs/research/yukon.md`](./docs/research/yukon.md)

### Votes & committees

- [x] **Apply migration `0018_votes.sql`.** Applied 2026-04-30. NT consensus-government data validates the schema without revisions. NT votes extractor live (`extract-nt-votes`), 31 consensus votes across 2013-2026 NT corpus. → [`docs/runbooks/handoff-2026-04-30-votes-layer.md`](./docs/runbooks/handoff-2026-04-30-votes-layer.md)
- [x] **Federal votes extraction (openparliament.ca structured JSON).** Live 2026-04-30. `services/scanner/src/legislative/federal_votes.py` extracts ~928 44-1 divisions × ~340 MPs = ~300K vote_positions with 100% politician_id FK match (via openparliament_slug). Daily 11:30 UTC schedule slot. → [`docs/runbooks/handoff-2026-04-30-federal-votes.md`](./docs/runbooks/handoff-2026-04-30-federal-votes.md)
- [x] **Federal votes — historical sessions** (39-1 through 43-2) shipped 2026-04-30. 4,481 total votes / 1.45M positions / 18.5 years (2006-05 → 2024-12) / 99.98% pol-FK / 335 MB storage. → [`docs/runbooks/handoff-2026-04-30-federal-votes.md`](./docs/runbooks/handoff-2026-04-30-federal-votes.md)
- [x] **Federal historical bills ingestion** — shipped 2026-04-30 via `ingest-federal-bills --all-sessions`. 412 → 5,542 bills across P37-P44 (openparliament.ca coverage floor at P37-S1 / 2001). Sponsor FK rate 86.5% (4,794/5,542) via openparliament_slug. Lifted federal vote→bill linkage to natural ceiling.
- [x] **Federal vote→bill re-link** shipped 2026-05-01 via `relink-federal-votes` (pure-SQL UPDATE pass, no API calls). 10.2% → **54.7%** linkage; 1,993 newly linked, 0 unmatched. Remaining 45% are non-bill divisions (supply, procedural).
- [x] **Provincial historical bills backfills (ON / MB / QC)** shipped 2026-05-01 / 2026-05-02 via `--all-sessions` walker pattern: ON 111 → 3,412 (P36-P44, ola.org HTML), MB 81 → 1,971 (31 sessions, gov.mb.ca HTML), QC 497 → 1,192 (P39-P43, assnat.qc.ca HTML — new historical discoverer added because CSV is current+previous-only by design). All three flipped to `bills_status=live`.
- [x] **`bills_status` auto-derivation** shipped 2026-04-30 in `coverage_stats.py` — completes the trio (hansard / votes / bills) of count-driven status flips. 500-row threshold for 'live'. Preserves 'blocked' editorial flag.
- [x] **Provincial votes extraction (BC/AB/MB/QC/ON/NS/NB/NL)** shipped 2026-04-30. 8 self-contained per-province extractor modules; 7,303 provincial votes added (QC 2,961 div+acclamation rich; NS 2,550 consensus; ON 835; AB 624; BC 249; MB 47; NL 2; NB 0 — needs regex relaxation). → [`docs/runbooks/handoff-2026-04-30-provincial-votes.md`](./docs/runbooks/handoff-2026-04-30-provincial-votes.md)
- [x] **Provincial votes — extractor v2 tuning** shipped 2026-05-05 (commit `4c26b1e`). AB / NS extended `_QUESTION_CALL_RE` with `Contrary minded`, `I hear several Noes`, `Opposed?` to catch Speaker-truncated forms; AB also gained corrections-aware last-match logic (`The motion is defeated. Oh, no, it's carried.`). NB completely restructured around heading-style detection (`Recorded Vote—Proposed Amendment to Motion for Second Reading of Bill 3 Defeated`); inline-outcome consensus path disabled (~80% false-positive on NB debate prose). **Outcomes:** AB 628 → 650 votes (+1 defeat); NS 2,550 → 2,559 votes (+2 defeats); **NB 0 → 125 votes** (53 defeated + 72 passed, all division-shape).
- [x] **Provincial votes daily-ingest schedules** shipped 2026-05-05 (commit `e2ffa9d`). 8 entries seeded: BC/AB/QC/MB/NB/NL/NS at `:50 HH * * *` after each Hansard chain (NS at `:50 13` as new sibling of legacy 12:00–13:30 NS slot); ON at `:55 18` to dodge presiding-speaker resolver collision. Total 10 votes-extraction schedules (Federal + NT + 8 new).
- [~] **QC introduced_date completion** — code shipped 2026-05-04, partial backfill to **368/1,192 (30.9%)**. New `fetch-qc-bill-introduced-dates` Click command + `fetch_qc_bill_introduced_dates` function in `qc_bills.py`. Iterates every undated QC bill, GETs the detail page, regex-extracts `Sitting held on <date>` from inside the `<h3>Introduction</h3>` block, writes a `bill_events` first-reading row, then calls existing `derive_qc_introduced_dates` SQL roll-up. Daily 16:35 UTC schedule slot. **Backfill rate-limited by assnat.qc.ca** after ~190 fetches (silent-timeout slow-loris); both 1.2s and 4s delays tripped it. Remaining ~824 undated bills will catch up over subsequent daily runs (the SELECT filters on `introduced_date IS NULL` so each run only touches the still-undated tail; once below the rate-limit threshold per run we'll converge to ~100%).
- [x] **ON pre-P42 `introduced_date`** — shipped 2026-05-04 (see above). Bonus: re-parse added +13,065 historical bill_events rows beyond just intro-dates.
- [x] **Federal `bill_events` from LEGISinfo XML** — shipped 2026-05-04. New `ingest-federal-bill-events` Click command + `federal_bill_events.py` ingester walks parl.ca/LegisInfo XML; one HTTP GET per session unlocks ~7 milestone timestamps per bill (1st/2nd/3rd reading in House + Senate + royal assent) via inline `Passed{Chamber}{Stage}DateTime` fields. **9,970 federal events** landed across 15 sessions (P37-S1 → P44-S1) on first backfill; 5,471/5,542 bills matched (98.7% via `bills.raw->>'legisinfo_id'`); spot-checks against parl.ca/LegisInfo for C-11/C-21/C-27 match. Daily 11:45 UTC schedule slot. Closes federal stage-timeline gap noted in `services/scanner/src/legislative/federal_bills.py:18-22`.
- [x] **MB + NS `introduced_date` SQL backfill** — shipped 2026-05-04 alongside the federal events work. New `relink-bill-introduced-dates` Click command (pure-SQL CTE pass over `bill_events.stage='first_reading'`). MB: 0 → 81 bills; NS: 0 → 2,114 bills. Daily 07:55 UTC cross-jurisdictional schedule slot before chunk+embed. Generic across jurisdictions; future provinces with the same shape need no code change.
- [~] **Committee transcripts.** **Federal shipped 2026-05-18** via `ingest-federal-committees` (extends `federal_hansard.py` with `/committees/meetings/` discovery; reuses `_pick_speech_type` / `_upsert_speech` / chunker / embedder verbatim). P45-S1 first run: 5,471 speeches / 30 meetings / 100% MP-FK; daily 11:20 UTC. **AB shipped 2026-05-19** (first provincial slice) via `ingest-ab-committees` — assembly.ab.ca PDF transcripts for 11 standing committees + committee-restricted speaker lookup (rejects witnesses) + per-meeting PDF cover-page roster parser for substitute-MLA detection. **9,988 speeches across 60 L31-S2 meetings**; 78 substitute MLAs augmented; AB `committees_status: editorial 'live' → count-derived 'partial'`; daily 15:25 UTC. **Pending:** historical backfills (federal P39-P44 + AB P29-P30, both gated on GPU regression mitigation); next provincial slice (BC difficulty=2, research-handoff gated). → `~/.claude/plans/hay-dude-i-know-bubbly-sun.md` (federal), `~/.claude/plans/okay-what-do-we-validated-mitten.md` (AB), [`docs/plans/semantic-layer.md`](./docs/plans/semantic-layer.md) § phase 4

### Historical-roster backfills (AB/MB pattern → ON/BC/QC)

- [x] **ON historical roster.** Propagate the date-windowed-resolver pattern. Required before pre-current-session ON Hansard speaker attribution is meaningful pre-2010s.
- [x] **BC historical roster.** Pre-P35 Wikipedia ingester (P29-P34, +160 MLAs / +359 terms) + `resolve-bc-speakers-dated` + extended Speaker roster (P29-P37). BC corpus 67.6% → 91.9% attributed. → [`docs/runbooks/handoff-2026-04-29-bc-pre-p35-roster.md`](./docs/runbooks/handoff-2026-04-29-bc-pre-p35-roster.md)
- [x] **QC historical roster.** Shipped 2026-04-27 — `ingest-qc-former-mnas` + `resolve-qc-speakers-dated`. → [`docs/runbooks/handoff-2026-04-27-bc-qc-historical.md`](./docs/runbooks/handoff-2026-04-27-bc-qc-historical.md)

### Corrections inbox

- [x] **Corrections admin review queue.** Public POST (`/api/v1/corrections`), authed user list (`/me/corrections`), admin triage UI (`/admin/corrections` — list, stats, status transitions, reward-credit flow on `applied`), and email notifications all live. SMTP/IMAP poller intentionally not built — provider-side IMAP setup outweighs the web-intake path that already covers the use case; `correction_submissions.source` still admits `'email'` if a poller ever lands.

### Social enrichment

- [ ] **Apify social-post deep enrichment, phase 0/1 → 2–5.** Schema + Twitter pilot done (phase 0/1); Instagram, TikTok, Bluesky direct, Mastodon direct, reverse-WHOIS pending. Steady-state cost $100–$250/mo on quarterly refresh. → [`docs/plans/apify-social-deep-enrichment.md`](./docs/plans/apify-social-deep-enrichment.md)

### Bill text for the laggards

- [~] **SK / PE / YT bill ingest.** SK shipped 2026-05-05 (`ingest-sk-bills`, 367 bills across 28L3S–30L2S, `bills_status='partial'`); reaching `live` (>500) needs the yearly-span 1998-2017 PDFs which use a different table shape. PE / YT still PDF-only / WAF-gated, deferred.

### Tier-2 attribution (cross-jurisdictional)

- [x] **Tier-2 Pass 1 — inline-name presiding-officer resolver** shipped 2026-05-05 (commit `bb41d88`). Cross-jurisdictional resolver targets parens-name presiding labels (`The Deputy Speaker (Mr. Bas Balkissoon)` / `Madam Clerk (Patricia Chaychuk)` / `La Vice-Présidente (Mme Gaudreault)`). Two regex patterns: `(Honorific Name)` covers EN+FR honorifics; bare-name fallback `(Firstname Lastname)` gated on outer-text presiding role. Daily 23:30 UTC schedule. **Outcomes:** ON 4,262 candidates → 862 FK hits (parens-attributed → 99.99%); MB ~1,961 hits (→ 91.5%); QC 16 hits (capped at 67% by surname-only ambiguity — needs Pass 2 date-windowed roster).
- [x] **BC pre-1990 ALL-CAPS resolver** shipped 2026-05-05 (commit `b0b2562`). Parses `MR. G.S. WALLACE (Oak Bay)` / `HON. D. BARRETT (Premier)` shape that `resolve-bc-speakers-dated` mishandled (extracted constituency-in-parens as surname). Three-tier disambiguation: lastname+date single-candidate → riding hint → first-initial. **Outcomes:** ALL-CAPS unattributed 12,742 → 1,725 (resolved 11,017, 86.5%); BC overall 92.5% → 94.4%. Spot-checked W.A.C. Bennett, G.S. Wallace, J.R. Chabot all hit historically-correct MLA terms.
- [~] **Tier-2 Pass 2 — Deputy Speaker SPEAKER_ROSTER** for AB/BC/ON/MB/SK/NT. **QC vertical slice shipped 2026-05-06** as a fourth narrowing step in `inline_presiding_resolver` (date-windowed presiding-role membership when surname/first-name still leaves >1 candidate). Outcomes: QC parens-attributed surface **67.4% → 96.7%** (+21,223 speeches at confidence 0.80); 22-entry `DEPUTY_PRESIDING_ROSTER["QC"]` covering 37L→43L Vice-Présidents incl. the Sylvain Lévesque → Sylvie D'Amours 2024-11-07 succession. AB/BC/ON/MB/SK/NT propagation pending (one province at a time per user pref).
- [~] **Tier-2 Pass 3 — role-only presiding labels** (AB ~60K, BC ~13K, ON ~17K, MB ~11K). **AB Deputy Speaker + Deputy Chair shipped 2026-05-06** in two phases (full coverage 1993→present): 5-entry Deputy Speaker (Marz/Cao/Rogers/Jabbour/Pitt) + Tannas pre-2005 entry + 10-entry Deputy Chair (Gordon/Shariff/Mitzel/Zwozdesky/Jablonski/Feehan/Sweet/Milliken/Reid/van Dijken). AB roster sourced from `politicians.extras.ab_member_info.offices`. **AB `The Deputy Speaker` 0% → 100%** (19,363 attributed); **AB `The Deputy Chair` 0% → 100%** (11,631 attributed); two short Deputy-Chair vacancy gaps acknowledged. Total +30,994 AB role-only speeches at confidence 0.85; AB attribution 87.2% → 90.5%. **BC Deputy Speaker shipped 2026-05-06** (4-entry roster: Reid 39L / Chouhan 41L / Chandra Herbert 42L / Elmore 43L, sourced from per-Parliament Wikipedia + targeted searches). BC parser tags as bare `Deputy Speaker` (no "The" prefix), distinguished in `ROLE_ONLY_OFFICE_MAP["BC"]`. **BC `Deputy Speaker` 0% → 83.5%** (+4,267 speeches); 842 misses across 38L (2005-2009), 40L (2013-2017), and Feb 2025 transition where Deputy Speaker holder couldn't be confirmed from public sources. **MB Deputy Speaker shipped 2026-05-06** — single-entry roster (Tyler Blashko, 43L only — pre-43L MB chamber parser used inline-name shapes already caught by Pass 1). **MB `The Deputy Speaker` 0% → 100%** (+1,027 speeches). **SK Deputy Speaker shipped 2026-05-14** — 4-entry roster covering 28L3S–30L (Wilson 2020-12-01 / Hargrave 2021-10-28 / Bradshaw 2023-10-10 / McLeod 2024-11-26), mined directly from in-Hansard election announcements. SK parser emits lowercase snake `deputy_speaker`. **SK `deputy_speaker` 0% → 100%** (+2,174 speeches); **SK overall 81.91% → 86.10%**. Drive-by: made `_find_politician_id` deterministic via `ORDER BY is_active DESC, created_at ASC` to handle SK's duplicate-politician problem (legassembly + opennorth source_id pairs). Whitelist sync: `resolve-role-only-presiding-officers` added to both `jobs_catalog.py` and `admin.ts COMMAND_CATALOG` (latent gap since the original Pass-3 ship in cycle 2026-05-06). Daily 22:35 UTC SK schedule seeded. **Phase 2 — AB Acting Speaker / Chair (~30K rotating rows): deferred** — AB Hansard lacks in-corpus name signals for rotating roles. **Pending:** BC pre-2008 + 38L/40L gaps (need user research), BC Chairman / The Chair rotating-role buckets (~24K rows), MB Chairperson rotating bucket (~10,909 rows — deferred 2026-05-20, no in-corpus chair-handover signal: probe found only 3 of 11,170 rows contain announcement-shape language, same family as deferred AB Acting Speaker / BC Chairman buckets), ON role-only propagation (~4K, but already at 98.5% — marginal lift), ~~MB Tier-1 chamber-parser empty-role miss (~46K rows)~~ shipped 2026-05-14 via `mb_speaker_role_relink` (see cycle 2026-05-14 entry); follow-up parliamentary-staff role-tag pass shipped 2026-05-20 (+2,109 rows tagged `The Clerk` / `The Deputy Clerk` / `The Clerk Assistant` / `The Sergeant-at-Arms` / `The Deputy Sergeant-at-Arms`), AB/BC/MB daily-cron Pass-3 schedule rows (resolvers exist but no scanner_schedules entries since 2026-05-06 ship — same gap as SK was before today).
- [x] **ON Speaker historical roster backfill (Tier-1 Pass 0)** shipped 2026-05-14 (uncommitted). The `SPEAKER_ROSTER["ON"]` deferred-since-ON-Hansard-shipped 11-entry historical backfill (32L → 43L, 1981 → 2025) was the highest-leverage non-deferred Tier-1/2 bucket on the board, hiding in plain sight: the original 2026-04-26 ON Hansard ship seeded only the 44L Skelly entry and docstring-noted "Backfill earlier Speakers as historical-Hansard ingest expands" — that backfill never ran. **Outcomes:** ON `The Speaker` 46,415 unattributed → 0; **ON overall attribution 87.54% → 92.45%** (+4.91pp, +46,415 speeches at confidence 0.9). 12/12 spot-checks correct spanning Turner / Edighoffer / Warner / McLean / Stockwell / Carr / Curling / Brown across 1989-2006.

---

## Next 2 — Chat interface

A conversational front door over the existing semantic-search + contradictions stack. Retrieval, ranking, and grounded-citation primitives already exist — chat is the UX wrapper.

- [ ] **Write the plan doc.** No `docs/plans/chat-*.md` exists yet. Settle the open questions before any code:
  - Scope: general-purpose Q&A vs. politician-scoped vs. bill-scoped?
  - Grounding discipline: every claim cites a chunk, refusal otherwise?
  - Model: OpenRouter free-tier (like contradictions) or paid (like phase-1b reports)?
  - Metering: free / credit-metered / rate-limited?
  - Session persistence: extend `saved_searches` or new `chat_sessions`?
  - Transcript export, credit-ledger interaction, voice-input handoff (see Next 3).
- [ ] **Build, reusing existing primitives.** Semantic-search hybrid retrieval + contradictions consent/model picker are load-bearing. **Don't fork retrieval.**

---

## Next 3 — Accessibility (incl. voice)

Two distinct workstreams under one priority. WCAG 2.2 AA is the audit target.

### Accessibility audit + remediation

- [ ] **Plan doc for the audit.** None exists.
- [ ] **Keyboard navigation pass** across `/search`, `/coverage`, `/postal`, the politician page, the admin shell.
- [ ] **Screen-reader testing** on the same surfaces.
- [ ] **Color-contrast pass on the map.** Leaflet defaults are not great.
- [ ] **ARIA landmarks + form labels** site-wide.

### Voice interface

- [ ] **Plan doc for voice.** None exists. Must come before any code.
- [ ] **Speech-to-text for query input** (STT on-device where possible, server-side fallback). Lands in the chat surface from Next 2, not in `/search`.
- [ ] **Text-to-speech for results read-back.** Prioritised for low-vision and low-literacy users.
- [ ] **Sovereignty constraint:** STT/TTS must be self-hostable or have a defensible Canadian-data path. Hosted-Whisper-via-third-party is not the default. → [`docs/plans/sovereignty-runtime-deps.md`](./docs/plans/sovereignty-runtime-deps.md)

---

## Next 4 — UI improvements

Independent, ship-each-on-its-own work.

- [ ] **`/chunk/<uuid>` detail page.** Internal view: full speech around a chunk + neighbours. Today chunks deep-link out to source Hansard via `source_url + source_anchor`.
- [ ] **Politician biography brief.** Full-coverage report (all speeches/bills/votes per politician) as a phase-2+ premium SKU on top of phase-1b query-scoped reports. → [`docs/plans/premium-reports.md`](./docs/plans/premium-reports.md) § v2+
- [ ] **Topic dashboard / time series.** "Climate mentions across all legislatures over time"; faceted aggregations by party / jurisdiction / speaker.
- [ ] **Compare politician A vs. B on topic X.** Phase-2+. Probably reuses the chat surface from Next 2.
- [ ] **Map polish.** Symbol legend, faster cluster-zoom transitions, constituency-boundary year picker (boundaries are temporal as of migration 0021).

---

## Always-on (every cycle, regardless of horizon)

- [ ] **Governance docs before public launch.** Takedown / correction policy, DSAR workflow (especially before Apify social enrichment goes public), AI-report disclaimer text. None written yet; small but blocking.
- **Backup system completion.** Path B (parallel `pg_dump` directory format) is documented in [`docs/operations.md`](./docs/operations.md) § Backups but operator-run. Path A (`sovpro db backup` → single gzipped file) is fine for small / portable snapshots and stays as-is.
  - [ ] **Wrap Path B in a single CLI subcommand** — e.g. `sovpro db backup-fast`. One call performs the manifest write (git SHA + row counts + applied migrations), `pg_dumpall --globals-only`, sidecar `pg_dump -Fd -j 8`, ownership fix-up, and `pg_restore --list` verify. Today it's a five-step copy-paste block.
  - [ ] **Schedule it via `scanner_schedules`.** Daily cadence at a quiet UTC hour, well clear of the daily-ingest band. Failures should surface in the admin Jobs page like every other scanner job.
  - [ ] **Retention / rotation** on the primary backup directory. Keep N daily, M weekly, K monthly; prune the rest. Today snapshots accumulate forever at ~216 GB each.
  - [ ] **LUKS USB mirror automation** — at minimum a ready-to-paste script that drives `cryptsetup luksOpen` → `rsync` → `umount` → `cryptsetup luksClose` from one entry point. Stays operator-triggered (USB needs a passphrase), not scheduled.
  - [ ] **Off-host mirror.** S3 / B2 / equivalent on cron — same `rsync` shape as the USB mirror, different destination. `operations.md` already names this as the production posture; not yet implemented.
  - [ ] **Logged restore drill.** Run `pg_restore -j 4` end-to-end against a fresh staging DB and time it. The HNSW rebuild floor (30–60 min on the 3.4 M-chunk corpus) is currently an estimate; replace with a measured number and re-drill quarterly.
  - [ ] **Encryption-at-rest check.** Confirm the internal NVMe target (`/media/bunker-admin/Internal/canadian-political-data-backups/`) is on an encrypted partition or migrate it to one. Backups carry user emails, magic-link redemption history, Stripe customer IDs, and full speech text — same threat model as the LUKS USB.
- [ ] **Embedding-model drift monitoring.** Re-run the eval set under [`services/embed/eval/queries/queries.jsonl`](./services/embed/eval/queries/queries.jsonl) on any model change. Qwen3-Embedding-0.6B is current; BGE-M3 wrapper kept on disk for rollback only. → [`docs/plans/embedding-model-comparison.md`](./docs/plans/embedding-model-comparison.md)
- [ ] **AI-contradictions false-positive watch.** Live and free-tier; watch for quoted-opponent and party-transition-boundary failure modes. → [`docs/plans/ai-contradictions-handoff.md`](./docs/plans/ai-contradictions-handoff.md)
- [ ] **Coverage-dashboard accuracy.** Run `refresh-coverage-stats` after every Hansard or bills ingest so `/coverage` doesn't lie.
- [ ] **Enable `agent-missing-websites` weekly schedule when budget allows.** Migration 0047 + agent + admin review UI shipped 2026-05-12; agent runs cost ~$0.35–$0.50 per ~2K-politician sweep on Anthropic. To enable, paste one INSERT into psql (see [`docs/operations.md`](./docs/operations.md) § Agent-driven enrichment). **Pre-req now satisfied:** Roster hygiene shipped 2026-05-12 (commits `0114575` + `965fd3c`) — QC/MB/SK retirement detection live, only budget remains as a gate. Same pattern applies to the long-standing `agent-missing-socials` command (no schedule today either).
- [x] **Roster hygiene — close defunct `politician_terms` + reconcile `is_active`** (shipped 2026-05-12, commits `0114575` + `965fd3c`). Three slices: (a) one-shot SQL closed 1,196 stale `politician_terms.ended_at=NULL` rows on already-inactive provincial politicians (4 AB + 232 MB + 960 QC) — was a latent drift, not breaking any production view but inflating any `pt.ended_at IS NULL` "sitting now" proxy query; (b) seeded weekly `scanner_schedules` rows for `ingest-manitoba-mlas` (Sun 03:30 UTC) + `ingest-quebec-mnas` (Sun 03:45 UTC) + added both to `jobs_catalog.py` + `admin.ts COMMAND_CATALOG` whitelist; both opennorth wrappers already called `detect_retirements()` on full fetches — they just never ran; (c) SK was the only production-breaking case (159 active vs 61-seat chamber, polluting `v_websites_missing` + `v_socials_missing`) — needed net-new code because SK politicians are keyed on `sk_assembly_slug` with no opennorth `source_id` so the canonical helper couldn't see them. New `_detect_sk_retirements()` in `sk_mlas.py` mirrors the helper's body but identifies candidates by `(level='provincial', province_territory='SK', is_active=true)`. SK_RETIREMENT_ROSTER_FLOOR=55 safety gate against sparse Hansard speaker-index fetches. First run retired 98; SK now at exactly 61 active. Schedule seeded at Sun 04:00 UTC. AB/BC retirement-detection wiring untouched (already correct via opennorth + LIMS schedules).
- [ ] **Documentation freshness.** Update [`docs/api.md`](./docs/api.md) when API contracts change; edit `docs/timeline.md` (and re-sync this file) when priorities shift; mirror major surface additions across `README.md` "Where to go" / dedicated section, `CLAUDE.md` orientation, `docs/operations.md` env vars + walkthroughs, and the `mkdocs/docs/developers/` site if user-facing.

---

## Later (deferred, plan docs exist)

Parked behind the priorities above — not abandoned.

- [~] **Public developer API** (`/api/public/v1/*`) with three paid tiers. → [`docs/plans/public-developer-api.md`](./docs/plans/public-developer-api.md)
  - [x] **Phase 1a — free-tier infra + 3 read endpoints** (shipped 2026-05-12, commits `35b3f31` + `<phase-ii>` + `<phase-iii>`). Migration 0048 (`private.api_keys` + `api_key_events` + `api_usage_daily`), token lib (`cpd_<env>_<22b62>_<6cs>` HMAC-hashed at rest with `API_KEY_PEPPER`), `requireApiKey` / `optionalApiKey` middleware, `/me/api-keys` CRUD with rotate (24h grace) + revoke + audit, frontend page at `/account/api-keys` with one-shot full-token banner, per-tier rate-limit middleware (free=60/hr per key, anon=30/hr per IP, dev/pro reserved), `/api/public/v1` plugin with permissive CORS + 3 endpoints (`/coverage`, `/jurisdiction-sources`, `/politicians/:id`). Smoke-tested: 60 OK then 5x 429 with audit rows. See [`docs/api.md`](./docs/api.md) § Public API.
  - [x] **Phase 1b — Stripe subscriptions for dev/pro tiers, test mode only** (shipped 2026-05-12, commits `b3f95b9` + `<phase-ii>` + `<phase-iii>`). Migration 0049 (subscription columns on `private.users` + `private.subscription_events` audit table). Stripe lib helpers: `createSubscriptionCheckoutSession` (mode='subscription' reusing the credit-pack Tax opts block), `createPortalSession` (Stripe Customer Portal), `getSubscription`, `priceIdToPlan`, `setSubscriptionCancelAtPeriodEnd`. New `/api/v1/me/subscriptions` routes (GET state, POST checkout/cancel/reactivate/portal). Webhook handler extended with three new event types (subscription.created/updated/deleted) — auto-promotes all of a user's `api_keys.tier` on subscribe (or upgrade), demotes to free on subscription.deleted; `cancel_at_period_end` does NOT trigger demotion (user keeps tier until period actually ends); past_due also doesn't demote (Stripe handles dunning). All sync writes inside one transaction. Downstream idempotency via UNIQUE on `subscription_events.stripe_event_id`. Frontend `BillingPage.tsx` at `/account/billing`: status card with chip + plan picker (free users only) + Cancel/Reactivate/Manage Subscription buttons. **Live-flip is a separate operator step** — see [`docs/operations.md`](./docs/operations.md) § Subscriptions activation walkthrough for the test-mode → live-mode procedure (mirrors the 2026-05-05 credit-pack live-flip).
  - [x] **Phase 1e — bulk-export endpoints with `read:bulk` scope (lean MVP)** (shipped 2026-05-12, commits `a551390` + `aca99dc` + `<1e-iii>`). Adds `read:bulk` as an opt-in scope on `private.api_keys.scopes` (orthogonal to tier — free-tier keys CAN have bulk download). New `services/api/src/middleware/api-scope-gate.ts` exports `requireScope('read:bulk')` factory; 403 with `code='insufficient_scope'` + `required_scope` + `current_scopes`. `/me/api-keys` POST accepts opt-in scopes; `ApiKeysPage.tsx` create form gains a Scopes checkbox fieldset. Two new endpoints under `/api/public/v1/exports/*`: `GET /exports/dumps` lists current full-dataset dump artifacts (filename, size, mtime, kind: pgcustom/sha256/manifest); `GET /exports/dumps/:filename` streams a specific file with strict regex (`^cpd-public-[A-Za-z0-9_-]+\.(pgcustom|sha256|manifest\.tsv)$`) preventing path traversal. Both `requireApiKey + requireScope('read:bulk')`. API container now mounts `/media/.../public-dumps:/srv/datasets:ro` parallel to nginx; new `PUBLIC_DUMPS_DIR` env var. Same files served anonymously at `/datasets/` autoindex — this surface adds auth + per-key metering. Smoke-tested: anon→401, free-key-no-bulk→403 with detailed body, scoped key→200 list (6 files across 2 dump sets) + 200 stream (Content-Disposition + Content-Length + readable manifest); 4 path-traversal probes all rejected. New mkdocs page `developers/bulk-export.md` with curl + pg_restore guide. **Phase 1f deferred:** per-jurisdiction-month Parquet slicer (originally part of the v1.1 plan-doc spec) — needs a scanner snapshot generator pipeline; one cycle when prioritized.
  - [x] **Phase 1d — paid-tier `/search/*` + free-tier search auxiliaries** (shipped 2026-05-12, commits `<1d-i>` + `4c365c3` + `<1d-iii>`). Six new endpoints under `/api/public/v1/search/*`. **Pro-tier (TEI-dependent):** `/speeches`, `/speeches/count`, `/facets` — `requireApiKey` + `requireTier('pro')` + shared TEI semaphore (max 2 concurrent + 6 queued = 8 slots; 503 with `Retry-After: 5` on overflow). **Free-tier auxiliaries (no TEI):** `/sessions`, `/chunks/:id`, `/meta` — `optionalApiKey`, anonymous-friendly. Implementation pattern: each public handler proxies to the corresponding internal `/api/v1/search/*` route via `app.inject()` — Fastify's in-process pipeline — so route logic stays single-source-of-truth in `services/api/src/routes/search.ts`. New `services/api/src/lib/tei-semaphore.ts` (`p-limit` wrapper) + `services/api/src/middleware/api-tier-gate.ts` (`requireTier('dev'|'pro')` factory returning 403 with `code='insufficient_tier'`). Two new env vars: `PUBLIC_TEI_MAX_CONCURRENT` (default 2), `PUBLIC_TEI_MAX_QUEUE` (default 6). Smoke-tested: anon→401, free-key→403 with actionable body, pro-key→200 (10000/hr limit), 12-concurrent burst→exactly 8 OK + 4 503. OpenAPI now lists 9 endpoints across 4 tags.
  - [x] **Phase 1c — OpenAPI + Swagger UI + `/developers` mkdocs section** (shipped 2026-05-12, commits `83316ac` + `<phase-ii>` + `<phase-iii>`). `@fastify/swagger` + `@fastify/swagger-ui` + `fastify-type-provider-zod` scoped to the public plugin (Fastify encapsulation — internal `/api/v1/*` routes untouched). Swagger UI mounts at `/api/public/v1/docs/`, raw OpenAPI 3.0.3 JSON at `/api/public/v1/docs/json` (and `/yaml`). Bearer auth scheme documented with `bearerFormat = "cpd_<env>_<random>_<checksum>"`. mkdocs `mkdocs/docs/developers/` section with four pages — index (pitch + quickstart + tier table + Swagger UI link), authentication (token format + create/rotate/revoke + plan-tier auto-promote interaction), rate-limiting (per-tier limits + X-RateLimit headers + retry strategy + caching guidance), errors (full 400/401/404/429/503 catalog with response shapes). Frontend nav gets a "Developers" link to docs.canadianpoliticaldata.org/developers/. Response zod schemas deliberately omitted from route declarations (pg returns Date for timestamptz; would need z.union or .transform on every timestamp field) — response shapes documented in mkdocs + Swagger UI's `description` strings instead.
- [ ] **Bulk export endpoints** (Parquet / CSV) — `read:bulk` scope, per-jurisdiction-month presigned exports. Sits behind dev-API v1.0 as v1.1. → same plan doc.
- [ ] **Map tiles self-hosting.** CARTO + OSM currently CDN-loaded. Three options scoped: nginx raster cache, PMTiles + MapLibre GL (~25 GB Z0–Z14 Canada), OpenMapTiles container. → [`docs/plans/sovereignty-runtime-deps.md`](./docs/plans/sovereignty-runtime-deps.md) § item 3
- [ ] **Browser automation (Playwright / Camoufox)** for PE/YT WAF jurisdictions. Only worth it if direct outreach to legislatures for a civic-transparency allowlist fails. → [`docs/plans/national-expansion-scoping.md`](./docs/plans/national-expansion-scoping.md) q5.3
- [ ] **Openparliament.ca live-call → scheduled refresh.** `/api/v1/openparliament` hits `api.openparliament.ca` per request; move to a scanner refresh job + DB cache (outage-mitigation). → [`docs/plans/sovereignty-runtime-deps.md`](./docs/plans/sovereignty-runtime-deps.md) § item 4

### Major municipal councils

Goals-doc-listed *later* item ([`docs/goals.md`](./docs/goals.md):61). Tier-1 = ten cities. First-pass research dossiers shipped 2026-05-05; promotion to a build awaits the four standing priorities clearing.

- [x] **Tier-1 research dossiers (10 cities) + cross-cutting overview** shipped 2026-05-05. → [`docs/research/cities/overview.md`](./docs/research/cities/overview.md)
- [ ] **Schema-design session for municipal layer.** Decide: `municipality_slug` discriminator on `politicians` / `politician_terms` / `speeches`; by-laws as `bills.bill_type='bylaw'` vs new table; `speech_quality='realtime_caption'` field for Montréal-style captions; multi-body councillor representation (Mississauga + Peel pattern).
- [ ] **Tier-1 build batch 1 — Montréal (captions reference).** `donnees.montreal.ca` CKAN + `sous-titrage-conseil-municipal` ZIP archive parse. Bilingual schema seam. → [`docs/research/cities/montreal.md`](./docs/research/cities/montreal.md)
- [ ] **Tier-1 build batch 2 — Toronto (CKAN decisions / attendance).** `open.toronto.ca` CKAN; 15 confirmed council-meeting datasets. → [`docs/research/cities/toronto.md`](./docs/research/cities/toronto.md)
- [~] **Tier-1 build batch 3 — eScribe family (Calgary + Edmonton scaffold landed 2026-05-05; parked).** Migration `0046_municipal_meetings.sql` applied. `services/scanner/src/legislative/{escribe,escribe_ingest,youtube_captions}.py` + 7 Click commands wired and runnable. Scaffold ingests inline upcoming meetings (3 meetings landed for Calgary; Edmonton 0 — FullCalendar-only). **Honest finding:** these cities are seriously under-published on Hansard-equivalents; eScribe past-meetings AJAX is opaque to server-side callers; **Calgary has no YouTube channel** for council (only eScribe video stream); **Edmonton does have YouTube** but the correct handle wasn't found before parking. eScribe difficulty re-rated 3 → 4. Priority order revised — see [`docs/research/cities/overview.md`](./docs/research/cities/overview.md) § Priority ordering. **Next steps when unparking:**
  - Edmonton: find correct YouTube handle, point `escribe.CITIES['edmonton'].youtube_channel` at it, run Stages 5-7 of the captions chain. Partial-Hansard via auto-captions becomes live with no new code.
  - Calgary: probe `data.calgary.ca` (Socrata) for council-decisions / motions republish — best low-friction path. eScribe video-stream captions extraction is browser-automation work; defer.
  - Ottawa / Mississauga / Hamilton: same scaffold applies — eScribe-block is family-wide. Probe their open-data portals (`open.ottawa.ca`, `data.mississauga.ca`, `open.hamilton.ca`) before committing to browser automation.
- [ ] **Tier-1 build batch 4 — Vancouver Opendatasoft.** `opendata.vancouver.ca` v2.1 API; 15k req/IP/day cap. New backend adapter (different from CKAN/Socrata). → [`docs/research/cities/vancouver.md`](./docs/research/cities/vancouver.md)
- [ ] **Tier-1 build batch 5 — Winnipeg + Québec (bespoke).** Lowest priority within tier-1. Winnipeg gated on DMIS reachability re-probe; Québec needs séance-archive URL discovery. → [`docs/research/cities/winnipeg.md`](./docs/research/cities/winnipeg.md), [`docs/research/cities/quebec.md`](./docs/research/cities/quebec.md)
- [ ] **Tier-2 research dossiers** (Brampton, Surrey, Halifax, London ON, Laval, Markham, Vaughan, Gatineau, Saskatoon, Longueuil) — only after at least one tier-1 city ships, so the schema is settled.

---

## Recently shipped (last three cycles, 2026-04-26 → 2026-05-23)

### Cycle 2026-05-23

- [x] **Corpus-wide naive last-name attribution audit + BC pre-1990 wrong-surname-collision cleanup** (uncommitted; follow-up to cycle 2026-05-22 NS Houston cleanup which surfaced the anti-pattern). Ran an audit query against `speeches` joined to `politicians` + `politician_terms`: `politician_id IS NOT NULL AND confidence=1.0 AND speaker_role IS NULL AND speaker_name_raw ~* '^(THE |The |Le |La |An?\\s|Some |Hon\\.|HON )' AND NOT EXISTS (SELECT 1 FROM politician_terms pt WHERE pt.politician_id=p.id AND speech-date overlaps pt-span)`. **Surfaced two distinct anti-patterns:** (1) **wrong-surname-collision**: 12,837 BC rows where pre-1990 ALL-CAPS speeches like `HON. MR. BENNETT` (1972-1986) were attributed to **modern** Bill Bennett (started 2001) instead of the historical William Bennett (1969-1986); same shape for `HON. MR. WILLIAMS`/`MACDONALD`/`FRASER`/`HARTLEY`/`HAGEN`/`JOHNSTON` etc. (2) **opennorth-ingest-date data-quality issue**: ~1,100 rows across NB (518) / NS (265) / MB (16) / BC (301) where the politician IS still actively serving but their `politician_terms.started_at` was set to the date opennorth's ingest first encountered them (e.g., 2026-04-14), rather than their actual elected date — so their speeches from 2024-2026 appear "pre-tenure". Distinguished the two via a **5-year-gap heuristic** (Pattern 1 has gaps of months-to-2-years; Pattern 2 has gaps of 30-50 years). **Cleanup transaction (Pattern 2 only):** reset `politician_id=NULL` + cleared chunk denorms for 11,066 BC speeches + 13,118 chunks where gap > 5 years. Re-ran `resolve-bc-allcaps` to re-attribute via the three-tier lastname+date / riding / first-initial disambiguation; processed in 5,000-row batches because the full corpus run timed out on the asyncpg statement budget. **Outcomes:** 10,039 BC rows correctly re-attributed (to William Bennett / Robert Williams / Alexander Fraser / etc. — the historical politicians whose terms actually cover the speech dates); 1,027 honestly NULL (genuinely ambiguous after disambiguation — multiple historical candidates with same surname AND no first-initial or riding signal). BC attribution **95.06% → 94.88%** (-0.18pp net, but with the 10,039 re-attributions now correct vs wrong — data-quality win, not lift). Audit re-run with 5-year-gap filter confirms 0 remaining Pattern 2 cases. **Pattern 1 (opennorth ingest-date) is the next cycle:** ~1,100 rows blocked on `politician_terms.started_at` being set to opennorth's ingest date rather than the politician's actual term start. The right fix is in the opennorth ingester (`opennorth.py` or similar — set started_at to the politician's election date from the API response, not `datetime.now()`). Documented for separate cycle. **Audit query is reusable** — future cycles can re-run the same SQL to catch new instances of naive last-name attribution. **Lesson:** confidence=1.0 + speaker_role=NULL + role-shape speaker_name_raw is the auditable signature of the Phase-1-style naive last-name attribution. Future ingest pipelines should NEVER assign politician_id based on surname alone without a date-window check. No new code shipped this slice — purely an audit + targeted cleanup.

### Cycle 2026-05-22

- [x] **NS Premier Tier-2 Pass-3 + reusable name-pattern resolver seam + chunks-reconcile latent-bug fix** (uncommitted; follow-up from cycle 2026-05-21's open list). NS had 1,904 unattributed `speaker_name_raw='THE PREMIER'` rows covering 2013-12-03 → 2021-03-31 (McNeil + Rankin Liberal era). Agent spawned for the roster Pass-3 work, mirroring AB/BC/MB/SK/QC patterns from prior cycles. **Surprise reframed the work:** NS Hansard parser stamps the role into `speaker_name_raw` (`'THE PREMIER'`), NOT into `speaker_role` (which is NULL for all 2,486 such rows). The existing `resolve_role_only_presiding` only matched on `speaker_role`, so the resolver needed a NEW match path. Agent shipped `ROLE_ONLY_NAME_PATTERNS` as a companion map alongside `ROLE_ONLY_OFFICE_MAP` — first jurisdiction using the name-pattern path; reusable seam for any future jurisdiction whose parser stamps role into the raw-name slot rather than `speaker_role`. NS roster has 2 entries (Stephen McNeil 2013-10-22 → 2021-02-23; Iain Rankin 2021-02-23 → 2021-08-31 — Dexter NDP pre-2013 omitted because no NS Hansard data pre-2013-12 exists; Houston PC post-2021-08 is Pass-1 parens-form territory). **Bonus latent-bug fix discovered + closed cross-jurisdictionally:** `resolve_role_only_presiding` had no chunks-reconcile sweep, only the inner per-batch UPDATE. A timeout during a long run would leave `speech_chunks.politician_id` desynced from `speeches.politician_id` and would NEVER self-heal on subsequent runs (the speeches no longer match the NULL filter). Agent added a reconcile sweep mirroring `resolve_speakers`. Affects AB/BC/MB/SK retroactively — future timeout aborts now self-heal. Daily 13:45 UTC NS schedule seeded (between legacy NS presiding-speaker at 13:30 and NS votes at 13:50, before BC chain at 14:00); mirrored into `scripts/seed-daily-ingest-schedules.sql`. **Outcomes:** 1,904 NS rows attributed (1,803 McNeil + 101 Rankin); **NS attribution 96.73% → 99.70%** (+2.97pp) — now highest-attributed provincial Hansard corpus on the board. **Pre-existing-bad-data finding flagged for separate cycle:** 547 NS `THE PREMIER` rows are attributed to Tim Houston at confidence 1.0 spanning 2016-04-14 → 2026-04-02 — including ~5 years before Houston was Premier; predates Pass-3, came from an earlier pipeline. Outside this cycle's scope; documented in `docs/research/nova-scotia.md`.

- [x] **ON politician dedupe → middle-initial-aware resolver (brief premise rejected, non-destructive alternative shipped)** (uncommitted; same cycle, follow-up to yesterday's ON dated-resolver rewrite that left 1,982 still-ambiguous). The brief assumed Dave Cooke + David Cooke were the same person stored twice and instructed the agent to merge them. **Agent's investigation decisively contradicted the premise:** `Dave Cooke` (`ola_member_id=325`) is NDP MPP for **Windsor-Riverside** 1977-1997; `David R. Cooke` (`ola_member_id=477`) is Liberal MPP for **Kitchener** 1985-1990 — two different MPPs who served simultaneously through P33-P34 in different ridings/parties. Merging would have corrupted the dataset. Zero ON politicians share an `ola_member_id`; every same-surname-overlapping-span pair in the data is two real people. **Real blocker identified + shipped:** yesterday's first-token disambiguator stops at the first initial (`D.`), which matches both Dave and David. The actual disambiguator already in the Hansard data is the **middle initial** (`David S. Cooke` vs `David R. Cooke`). New `resolve_on_speakers_middle_initial()` function (140 lines in `services/scanner/src/legislative/on_hansard.py`): extracts speech middle initial from second whitespace-delimited token of `full_name`; extracts politician middle initial from `name` minus `first_name` prefix and `last_name` suffix; joins on exact-match middle-initial + surname + politician-term date window; attributes when exactly one contemporaneous MPP survives. Conservative — requires middle initials on BOTH sides; politicians without a middle in `name` are no-ops, falling through to the existing first-token resolver. `resolve-on-speakers-dated` Click command now chains the middle-initial pass as a second step (operator surface unchanged). One targeted politician-table edit: Dave Cooke's `name` field enriched `'Dave Cooke'` → `'Dave S. Cooke'` so middle-initial extraction yields `S`; `ola_member_id=325` unchanged (this is enrichment, not a merge). **Outcomes:** +2,016 ON rows attributed (Cooke split cleanly: Dave 4,139 / David R. 422); **ON attribution 92.89% → 93.11%** (+0.22pp); idempotent re-run sees `scanned=0 updated=0`. Residue ~65K ON unattributed rows are now overwhelmingly role-based / non-person-attributable: `Interjection` (4,526), `Mr. Chairman` (2,816), `Clerk at the Table` (2,640), `An hon. member` (826), timestamps (`4:40 p.m.`), etc. Not a duplicate-politician problem. **Process insight:** the agent's refusal to follow a destructive brief premise is the right behavior — the dataset's `ola_member_id` was the truth source the brief didn't account for. Similar premise-checks should be standard for any merge / delete operation on roster data.

- [x] **NS Houston pre-Premiership mis-attribution cleanup** (uncommitted; same cycle, immediate follow-up to Agent A's NS Premier Pass-3). Agent A's report flagged "547 pre-existing rows attribute `THE PREMIER` to Houston for dates outside his Premiership (back to 2016-04-14) at confidence 1.0" as separate pre-existing bad data outside Pass-3's scope. Tackled this cycle. **Investigation:** the Phase-1 NS ingest pipeline had attributed `speaker_name_raw='THE PREMIER'` to Houston at confidence 1.0 by simple last-name match (only one NS politician has surname Houston) without any date-window guard. Of the 582 total Houston-confidence-1.0 rows in the corpus today, 112 fall outside Houston's actual Premiership (`spoken_at < 2021-08-31` — he became Premier 2021-08-31) and were wrong: 17 in 2016, 18 in 2017, 9 in 2018, 68 in pre-Aug-2021. The other 435 rows (post-2023-10-19, all within his term) were correct. **Cleanup:** single transaction reset `politician_id=NULL` on the 112 wrong rows + cleared 144 chunk denorms; re-ran `resolve-role-only-presiding-officers --province NS` which re-attributed all 112 via the McNeil/Rankin roster from yesterday (Stephen McNeil 44 rows 2016-2018 + Iain Rankin 68 rows pre-Aug-2021). `scanned=112 resolved=112 no_term_match=0 chunks_updated=144` — clean 100% hit rate. **Outcomes:** 0 remaining wrong-Houston attributions pre-2021-08-31; NS attribution % unchanged at 99.70% (politician_id swap only — no new resolution, just correcting wrong ones). Closes the open follow-up from cycle 2026-05-22 Agent A's run. **Lesson:** the Phase-1 ingest pipeline did naive last-name attribution without date-windowing, which would silently mis-attribute any single-instance surname (Premier, party leaders, etc.) if the politician's tenure didn't cover the speech date. Worth auditing other jurisdictions for the same anti-pattern as a separate hygiene cycle. No new code.

- [x] **QC bio re-fetch with widened-end-marker regex — shipped** (uncommitted; same cycle). Yesterday's `qc_former_mnas.py` cycle added `_PAS_REPRESENTE_RE` ("Ne s'est pas représenté en YYYY") to the four-regex set; existing politicians weren't bio-re-fetched with the new regex on the partial run. **Today's full re-walk:** `ingest-qc-former-mnas --bio-for-existing` (no `--limit`) walked all 16 alphabet pages + 2,491 historical MNA bios (skipped 125 current). Stats: `pages=16 unique=2616 bios=2491 bio_failed=0 spans_extracted=2146 span_miss=345 inserted=0 updated=0 terms_inserted=0 terms_skipped=2146` — politicians table unchanged (idempotent) but the existing-term widening logic stamped trailing `ended_at` values that yesterday's limited run hadn't reached. **Re-running the dated resolver bagged the lift:** `resolve-qc-speakers-dated: scanned=6519 updated=6458 still_unresolved=61` — 99.1% hit rate on the newly-reachable candidates. **Outcome: QC attribution 84.52% → 86.67%** (+2.15pp, +6,458 attributions at confidence 0.85). Many more historical MNAs now have correct `ended_at` boundaries so the date-window CTE finds unique candidates for surnames that previously had ambiguous overlapping terms. Cross-cycle finding confirmed: Agent A's "widen-never-narrow" logic on `ended_at` works correctly when re-walked with an enhanced regex set; the gap was that the partial 2026-05-21 run only hit small-N deputes/ rows. **No new code shipped this slice** — purely an operational re-run of the prior cycle's parser improvements + resolver re-run.

### Cycle 2026-05-21

- [x] **Cross-province staff/group `speech_type` classification sweep — QC + AB + ON same-day triple ship** (uncommitted; plan at `~/.claude/plans/what-should-we-work-cheerful-sun.md`). QC shipped first as the manual validation; AB + ON then parallelized via two background `general-purpose` agents working on disjoint parsers. **Total: 43,444 rows reclassified across three provinces in one cycle.** Five provinces now have group/staff classification (MB, NL, QC, AB, ON). NS remains at `staff=0` but the empty-role bucket is a different shape (`THE PREMIER` ALL-CAPS, 1,904 rows — Tier-2 Premier-roster Pass-3 work, not staff/group classification) — separate cycle. Each province's parser quirks below.

- [x] **QC parser staff/group `speech_type` classification** (uncommitted; plan at `~/.claude/plans/what-should-we-work-cheerful-sun.md`). Targets the largest analogous attribution-honesty gap on the board: at the start of the cycle QC had 37,784 `speech_type='floor' AND speaker_role IS NULL AND politician_id IS NULL` rows (bigger than MB's 46K ever was) and the cross-province corpus snapshot showed `staff=0` for QC/ON/NS/AB — confirming TODO line 224's note that "NS, AB, ON unchecked" applied to QC too. **Phase 1 investigation reframed the scope.** The QC chamber parser at `services/scanner/src/legislative/qc_hansard_parse.py:168-190` already detects every relevant role via `_ROLE_PATTERNS` — including French chorus shapes (`Des voix` line 188 / `Une voix` line 189) and the staff shape (`Le Secrétaire` line 186). What was missing is conditional speech-type assignment: line 409 hardcoded `speech_type="floor"` for every emitted turn regardless of detected role. So the bug wasn't "parser-misses-the-shape" (MB's pattern) — it was "parser-knows-the-shape-but-emits-wrong-type". **Slice A — parser conditional:** added `_GROUP_ROLES = frozenset({"Des voix", "Une voix"})` and `_STAFF_ROLES = frozenset({"Le Secrétaire"})` after `_ROLE_PATTERNS`, mirroring the role-set discrimination pattern from `services/scanner/src/legislative/nl_hansard_parse.py:227-233` (preferred over MB's `_STAFF_RAW_RE` raw-text approach because QC already extracts roles cleanly via `_ROLE_PATTERNS` — no need for a parallel raw-text regex). Line 409 replaced with `"group" if turn_attr.role in _GROUP_ROLES else "staff" if turn_attr.role in _STAFF_ROLES else "floor"`. **Slice B — one-shot backfill:** two `UPDATE` statements inside a single `BEGIN/COMMIT` transaction; no relink module needed because there's no role-tagging gap to back-populate (unlike the MB cycle), this is pure `speech_type` reclassification on already-role-tagged rows. Future ingests pick up the new parser logic automatically. **Outcomes:** 24,608 chorus rows reclassified `floor → group` (Des voix 17,501 + Une voix 7,107); 3,129 Secretary-General rows reclassified `floor → staff`; total 27,737 rows. QC `floor` 313,345 → 285,608; QC `group` 0 → 24,608; QC `staff` 0 → 3,129. QC attribution % unchanged at 79.1% (`politician_id` denominator unchanged — chorus + staff are by-design-NULL); honest-coverage signal corrected. Presiding-officer roles (`Le Président` 64,304 / `Le Vice-Président` 71,216 / `Le Président de commission`) and cabinet/leadership roles (`Le Premier ministre` / `Le Ministre` / `Le Leader` / `Le Whip` / `Le Chef`) deliberately stay `floor` — these are real MNA speeches where the role tag describes the speaker's function. **Doc hygiene:** `docs/research/quebec.md` status snapshot extended with the speech-type-classification bullet + the natural-next-cycle pointer; this file's sync header updated. **Out of scope:** the 37,784 surname-ambiguity bucket (M. Couillard 3,737 / M. Fournier 2,633 / M. Bédard 2,442 etc. — verified every common QC surname has 7-15 historical politicians via `politicians.last_name` query) — separate cycle to attack via date-windowed Pass-4 disambiguation; coverage-stat denominator hygiene (should `coverage_stats.py` exclude `speech_type IN ('group','staff')` from unattributed-politicians count? Open Always-on question applies to MB/NL/QC); French staff pattern extensions (Le Sergent d'armes / Le Greffier / Le Huissier) — none observed in current corpus, skipped preemptive adds; cross-province staff/group audit for NS/AB/ON — flag as follow-on cycles after QC validates the pattern. No new commands, no new schedules, no migrations.

- [x] **AB parser staff/group `speech_type` classification** (uncommitted; same cycle as QC ship above; spawned as a parallel `general-purpose` agent). Pure QC-pattern application — AB parser at `services/scanner/src/legislative/ab_hansard.py` already detects all the relevant chorus + staff roles via `_GROUP_SPEAKER_RE` + `_ROLE_SPEAKER_RE`; nothing to add to the regex tables. **What's new:** `_GROUP_ROLES = frozenset({"Hon. Members", "Some Hon. Members", "Some Members"})` and `_STAFF_ROLES = frozenset({"The Clerk", "The Clerk Assistant", "The Sergeant-at-Arms"})` added near lines 289-308; `_normalize_role()` extended at lines 532-552 with a `Sergeant At Arms → Sergeant-at-Arms` canonicalization step so the space-separated variant routes to the same canonical role (single-keyed `_STAFF_ROLES` membership); `_upsert_speech` at lines 863-873 made `speech_type` conditional with group/staff overriding the URL-based `_pick_speech_type` fallback (a chorus turn inside a committee transcript still tags `group`, not `committee` — important because AB committees shipped 2026-05-19). **Outcomes:** 4,896 chorus rows reclassified `floor → group` (Hon. Members 4,241 + Some Hon. Members 655); 1,296 staff rows reclassified `floor → staff` (The Clerk 1,018 + The Sergeant-at-Arms 258 + The Clerk Assistant 19 + The Sergeant At Arms 1 canonicalized); total **6,192 rows** in one transaction. AB attribution % unchanged at 89.8% (`politician_id` denominator unchanged); honest-coverage signal corrected. AB Acting Speaker (12,669) / The Chair (19,420) / The Chairman (1,223) rotating-role buckets explicitly out of scope (deferred — no in-corpus chair-handover signal). `docs/research/alberta.md` line 7 status snapshot bumped to 2026-05-21 with speech-type-classification bullet. **Surprise:** AB doesn't have a parser-stage `speech_type` at all — type is computed in the storage layer (`_upsert_speech`) via `_pick_speech_type(sitting.url)`. That's actually cleaner than QC's parser-stage choice; the conditional landed at the storage layer where URL routing already happened, so it composes correctly with the committee-vs-chamber routing.

- [x] **Four-agent parallel batch — QC roster gap fix + ON resolver rewrite + QC doc-continuity + MEDIUM-bucket audit triage** (uncommitted; same cycle, immediate follow-up to the audit ship). Four `general-purpose` agents on disjoint scopes, all running in parallel. **Agent A (QC recent-retiree roster) reframed the problem completely** — the brief assumed the 8 named 2008-2022 retirees (Khadir/Deltell/James/Malavoy/Hivon/Weil/Doyer/Vien) lived in a separate URL family the ingester didn't walk, but the truth was they were ALREADY in the existing `/fr/membres/notices/index-*.html` alphabet listings under the `deputes/` URL family — silently lost to a namespace collision. The `deputes/` and `patrimoine/anciens-parlementaires/` URL families on assnat.qc.ca maintain SEPARATE integer ID spaces that collide at the low end: `patrimoine/baby-charles-25.html` (pre-Confederation) vs `deputes/khadir-amir-25/` (modern). The old `qc_former_mnas.py` treated both integers as the same `qc_assnat_id` namespace and used `ON CONFLICT (qc_assnat_id) DO UPDATE`, so the patrimoine entries (alphabetically earlier) claimed the low IDs first and subsequent `deputes/` inserts for Khadir/Hivon/Deltell etc. became no-op updates against the wrong politician. Agent A's fix: dedup keyed on `(bio_kind, qc_assnat_id)`; patrimoine inserts now use `qc_assnat_id=NULL` + `source_id='assnat.qc.ca:former-mnas:patrimoine:{slug}-{id}'`; new Pass 1.5 demotes any legacy patrimoine row currently occupying a `qc_assnat_id` slot that a `deputes/` listing entry needs. Drive-by addition: `_PAS_REPRESENTE_RE` matches "Ne s'est pas représenté(e) en YYYY" (covers Hivon/Khadir/Weil/Vien who chose not to run again rather than being defeated — the old four-regex set missed this end-marker). Curly-apostrophe normalisation U+2019 → ASCII added to `_strip_html` so the new regex matches. **Outcomes Agent A:** all 8 named retirees backfilled (Deltell=17 / Khadir=25 / Hivon=27 / Weil=33 / James=49 / Doyer=91 / Vien=191 / Malavoy=255 — note the small integers they should always have had); politicians (QC) 2,559 → 3,263 (+704 deputes-family rows previously occluded); resolver lift across two runs: **+6,090 QC speeches** (5,412 first run + 678 widened-ended_at re-run after the new "ne s'est pas représenté" regex). **Agent B (ON dated-resolver rewrite)** replaced the parliament-keyed CTE (`source='ola.org:parliament-N'` join, 90/32,297 hit rate) with the QC-style date-window pattern + a first-token disambiguator inside the candidates CTE (strips honorifics from `raw->'on_hansard'->>'full_name'`, matches first token against `politicians.first_name` — handles full names, `'F.'` initials, and bare single-letter initials). ON politician_terms date coverage probed clean (100% `started_at`, 95.1% `ended_at`). **Outcomes Agent B:** ON attribution **92.46% → 92.89%** (+0.43pp, **+4,088 speeches**); idempotent re-run sees 1,982 still-ambiguous + 0 updates. Surprise: the 23K "potential" was mostly parser noise — 280 distinct unresolved surnames in ON, only 15 match any ON provincial politician; rest are `Chairman`, `Table`, `Interjection`, `P.m.`, `Président` (a leaked QC artifact, ID `5b593e87…`), etc. Practical ceiling was ~7K, not 23K. Remaining 1,982 dominated by duplicate politician records (Dave Cooke + David Cooke share every overlapping term — future politician-merge pass would unlock). **Agent C (QC document-level continuity disambiguation)** built `resolve_qc_speakers_doc_continuity()` in `qc_hansard.py` (mirrors `resolve_qc_speakers_dated()` shape; ground-truth bootstrap from already-attributed rows in the same `raw->'qc_hansard'->>'document_id'`, count-distinct=1 gate, confidence 0.75). Click cmd `resolve-qc-speakers-doc-continuity` + jobs_catalog + admin.ts. Daily 16:33 UTC schedule (between dated at 16:32 and presiding at 16:45). **Hypothesis invalidated empirically — only 18/26,868 rows lifted** (the brief expected 10K-15K). Probe revealed the remaining unresolved bucket is dominated by docs where the speaker is ALWAYS bare-surname (no `M. Stéphane Bédard` / `M. Bédard (Chicoutimi)` earlier in the same doc to bootstrap from). Example: doc 418599 has "Dubé" unresolved 307× with zero longer-form mention. Resolver still runs daily so it can pick up lift as other resolvers attribute upstream rows it can then propagate from. **Real path forward for the 21K surname-ambiguity bucket** is per-mandate `politician_terms.constituency_id` backfill (currently NULL for historical QC) so the `constituency_hint` parser slot (populated on 1,217/26,868 unresolved) becomes useful, plus a riding→politician ground-truth source. **Agent D (MEDIUM-bucket triage)** investigated all 7 audit MEDIUM commands: `chunk-speeches` + `embed-speech-chunks` → added to `_ONE_SHOT_VERIFIED` (covered by `chunk-and-embed-speeches` chain at `0 8 * * *` — scheduling them separately would re-introduce the queue-ordering race the wrapper removes); `enrich-ab-mlas` `45 4 * * 0`, `enrich-bc-member-parliaments` `48 4 * * 0`, `enrich-socials-all` `42 4 * * 0`, `ingest-mb-mlas` `50 4 * * 0`, `ingest-nt-mlas` `52 4 * * 0` — all weekly Sundays, slotted between today's other audit additions (04:40) and `verify-socials` (05:00). Verified `ingest-mb-mlas` ≠ alias of `ingest-manitoba-mlas` (different sources: LA surname-slug pages vs Open North REST — the former stamps `mb_assembly_slug` for FK lookups, the latter handles retirement detection). **Audit final state: HIGH=0, MEDIUM=0, BUDGET=2, LOW=28.** **Cross-cutting today's outcome:** QC attribution **79.1% → 84.52%** (+5.42pp, +16,989 attributions across the day: dated +10,917 + roster +6,090 + doc-continuity +18 - small race deltas); ON attribution **92.46% → 92.89%** (+4,088 from B). All four agents preserved their own province_research.md updates; this aggregation entry is the cross-cutting record. No new migrations.

- [x] **Schedule-gap audit + 7 latent schedules closed** (uncommitted; same cycle, follow-up to the three "code shipped, schedule forgotten" instances surfaced earlier today — `resolve-qc-speakers-dated`, `ingest-qc-former-mnas`, and (historically) SK Pass-3). New `scripts/audit-schedule-gaps.py` parses `jobs_catalog.COMMANDS` via AST, queries `scanner_schedules`, and categorises the unscheduled commands into HIGH/MEDIUM/BUDGET/LOW buckets. Heuristics: `*-dated` + `*-former-*` + `resolve-*` + coverage/views → HIGH; agent-missing-* → BUDGET-GATED; daemon (`run-scrape-jobs`, `dispatch-scrapes`, etc.) + parked (escribe/caption/councils) → LOW; everything else → MEDIUM operator-eyeball. A `_ONE_SHOT_VERIFIED` allowlist captures commands manually triaged as one-shot-complete or superseded so the audit doesn't keep nagging — populated 2026-05-21 with `resolve-bc-allcaps` (pre-1993 corpus, doesn't grow), `resolve-bc-speakers-dated` ("after pre-P35 backfill" one-shot, 0 backlog observed), `resolve-acting-speakers` (federal-only, superseded by `resolve-inline-presiding-officers` 2026-05-05 cross-jurisdictional), and `ingest-ns-bills` (Socrata historical backfill — sibling `ingest-ns-bills-rss` covers daily current-session). Description-pattern guard recognises `*-dated` resolvers whose description mentions "after backfill" as MEDIUM (needs verification before scheduling) rather than HIGH. **Initial run output:** 42 unscheduled commands; 11 HIGH, 7 MEDIUM, 2 BUDGET, 22 LOW. **7 HIGH gaps closed this cycle** (daily-cron INSERTs into `scanner_schedules` + mirrored into `scripts/seed-daily-ingest-schedules.sql`): `ingest-bc-former-mlas` (Sun 04:25 UTC), `ingest-mb-former-mlas` (Sun 04:30), `ingest-nu-former-mlas` (Sun 04:35), `ingest-on-former-mpps` (Sun 04:40) — continuing the QC-former-mnas slot block earlier today; `resolve-on-speakers-dated` (40 18 daily, between resolve-on-speakers at 35 18 and extract-on-votes at 55 18); `refresh-coverage-stats` (50 23 daily) + `refresh-views` (55 23 daily) — closes the TODO line 141 Always-on items. **Manual one-shot of `resolve-on-speakers-dated` first** attributed only 90/32,297 candidates (0.3% hit rate) — much lower than QC's +10,917 (99.4%) because ON's resolver is parliament-keyed (joining `raw->'on_hansard'->>'parliament'` against `politician_terms.source='ola.org:parliament-N'`) rather than date-windowed; the parliament tag on speeches rarely matches the source tag on politician_terms. The 23K+ ON backlog needs a different attack (likely date-window like QC) — flagged as a separate cycle. **Re-running the audit post-fix:** 0 HIGH, 7 MEDIUM (chunk/embed possibly covered by chunk-and-embed-speeches chain, enrichment commands need operator eyeball), 2 BUDGET, 25 LOW. Audit script auto-detects this pattern going forward — next time a Click command lands without a schedule, `python3 scripts/audit-schedule-gaps.py` will surface it on the next operator run. **Future structural fix flagged:** promoting `jobs_catalog.COMMANDS` to a real `scanner_commands` table with a `schedule_required` BOOLEAN column would let the audit become a single SQL query — out of scope this cycle but worth considering when the catalog grows further.

- [x] **QC roster-ingester SSL fix + weekly schedule + surname-ambiguity bucket scoping** (uncommitted; same cycle, immediate follow-up to the dated-resolver schedule fix below). Continuing the QC attribution work, attacked the 26,872 surname-ambiguous bucket that the dated-resolver couldn't reach. **Findings reframed the scope from "code work" to "data work":** (a) the 1,272 parens-bearing rows split into ridings (`M. Paradis (Lévis)`) and first-name witnesses (`M. Paquin (Mathieu)`); ground-truth bootstrap from already-resolved Hansard rows only catches 16/1,217 because `politician_terms.constituency_id` is mostly NULL for historical QC MNAs; first-name+last-name match against `politicians` catches 0 because those names are typically committee witnesses (Mathieu Paquin is not an MNA — he's appearing at a committee). (b) The 25,596 bare-surname bucket splits into ~4,500 rows that would resolve if specific missing MNAs were in `politicians` (Khadir 1,143 / Deltell 835 / James 490 / Malavoy 477 / Hivon 360 / Weil 341 / Doyer 319 / Vien 311 / Tanguay 233 / Marsan 56 — all served 2008-2022, all retired, all absent from the roster) and ~21K rows of genuine surname ambiguity (Fournier/Bédard/Dupuis families with 5-10 candidates each whose terms overlap). **What was actually broken and now fixed:** the `qc_former_mnas.py` ingester (shipped 2026-04-27) had two latent bugs that surfaced today: (1) its `httpx.AsyncClient` was missing the `verify=False` that other QC ingesters use to work around assnat.qc.ca's intermittent cert-chain validation issue — every alphabet-index fetch was failing with `CERTIFICATE_VERIFY_FAILED` on the current Python image's CA bundle; (2) the ingester had never been added to `scanner_schedules`, mirroring the same latent-gap pattern as today's earlier dated-resolver fix. **Slice A — SSL fix:** `services/scanner/src/legislative/qc_former_mnas.py:310` now passes `verify=False` to the httpx client, with a comment block explaining the precedent (same pattern as `qc_bills.py:427-428` for the QC RSS endpoint). Smoke-tested: alphabet-walk now successfully fetches all 16 letter pages, finds 2,556 unique MNAs. **Slice B — weekly schedule:** new `scanner_schedules` row `('QC former MNAs roster refresh', 'ingest-qc-former-mnas', '{}'::jsonb, '20 4 * * 0', true)` runs every Sunday at 04:20 UTC; also added to `scripts/seed-daily-ingest-schedules.sql` for pgdata-rebuild reproducibility. **Outcome of the re-run with SSL fix:** 0 new MNAs inserted (`inserted=0 updated=0 terms_inserted=0`) — all 2,556 listed MNAs are already in the roster (123 current + 2,433 existing). This confirms a parser-coverage gap: the `/fr/membres/notices/index*.html` alphabet listings cover **historical-only** MNAs (pre-2008-ish era); recent retirees who served 2008-2022 (Khadir et al.) live in a separate URL family not yet walked by the ingester. **Out of scope (separate "QC recent-retiree roster" cycle):** (a) identify the assnat.qc.ca URL pattern for 2008-2022 retirees (likely something like `/fr/deputes/{slug}-{id}/index.html` direct fetches keyed off a different listing page, or the bilan-de-l'Assemblée per-Legislature member lists); (b) extend `qc_former_mnas.py` (or write a sibling ingester) to walk that family; (c) re-run `resolve-qc-speakers-dated` to attribute the ~4,500 newly-recoverable rows. **Out of scope (further cycles):** the ~21K genuine surname-ambiguity rows (multiple Fournier / Bédard / Dupuis whose terms overlap) need document-level continuity (find the first long-form mention of the speaker in the same document) OR per-mandate constituency_id backfill on `politician_terms` — both deep refactors. The 1,272 paren-bearing rows split into riding+witness shapes that need a witness-tagging schema decision (`speech_type='witness'`?) and a riding→politician_id ground-truth source. No new code beyond the SSL one-liner; no migrations.

- [x] **QC date-windowed surname resolver — first end-to-end run + daily-cron seed** (uncommitted; follows the QC staff/group ship above the same day). Routine close-out of the deferred follow-up the QC staff/group ship explicitly flagged out of scope: the 37,784 surname-ambiguity floor empty-role bucket where common QC surnames have 7-15 historical politicians each (M. Couillard 3,737 / M. Fournier 2,633 / M. Bédard 2,442). **Discovery:** the `resolve-qc-speakers-dated` Click command (`services/scanner/src/legislative/qc_hansard.py:1026-1130`) was shipped 2026-04-27 alongside the historical MNA roster (`ingest-qc-former-mnas`) as its date-windowed companion resolver, registered in both `jobs_catalog.py:283` and `admin.ts COMMAND_CATALOG:173`, but **never added to `scanner_schedules`**. The daily QC chain only ran the name-only `resolve-qc-speakers` (which skips ambiguous surnames by design); the dated resolver that picks up exactly those surnames using `politician_terms.started_at`/`ended_at` date-windowing had been sitting on the shelf for 24 days. Same latent-gap shape as SK Pass-3 had before 2026-05-14. **Slice A — manual first run:** `docker compose run --rm scanner resolve-qc-speakers-dated` returned `scanned=10978 updated=10917 still_unresolved=61` (99.4% hit rate when there's a single candidate matching surname + date-overlap). The single-CTE pattern in `resolve_qc_speakers_dated()` updates `speeches` + `speech_chunks` in one statement; chunk propagation runs only against rows whose `politician_id` actually changed (`IS DISTINCT FROM` guard) — keeps the work idempotent. **Slice B — schedule seed:** new `scanner_schedules` row `('QC date-windowed speaker resolver', 'resolve-qc-speakers-dated', '{}'::jsonb, '32 16 * * *', true)` slotted between the name-only resolver (`30 16`) and `fetch-qc-bill-introduced-dates` (`35 16`). Mirrored into `scripts/seed-daily-ingest-schedules.sql:139-141` so a pgdata rebuild reproduces the same state. Future ingests stay caught up automatically — the resolver is idempotent and steady-state will only touch newly-ingested QC speeches each daily run. **Outcomes:** QC attribution `79.1% → 82.6%` (+3.5pp, +10,917 speeches at confidence 0.85). Remaining `politician_id IS NULL` rows characterized cleanly: 27,737 by-design-NULL (group + staff, correctly classified earlier this cycle), 26,868 surname-ambiguous (multiple QC politicians whose terms overlap the speech date — Tremblay 1,200+ / Couillard family / Lefebvre etc.), 4 edge cases (parser left no surname). The 26,868 ambiguous bucket needs riding-hint or first-initial disambiguation — separate deeper-cycle work mirroring how BC's pre-1990 ALL-CAPS resolver (commit `b0b2562`) extended the dated-resolver with three-tier `lastname+date / riding / first-initial` narrowing. **Doc hygiene:** `docs/research/quebec.md` status snapshot extended with the dated-resolver schedule + +10,917 backfill outcome; this file's sync header updated. No new code, no new migrations.

- [x] **ON parser staff/group `speech_type` classification** (uncommitted; same cycle as QC + AB ships; spawned as a parallel `general-purpose` agent). Hybrid scope — ON parser at `services/scanner/src/legislative/on_hansard_parse.py` already detected the staff family (Clerk / Clerk-at-the-Table / Deputy Clerk / Acting Clerk-at-the-Table / Clerk of the Committee / Acting Clerk of the Assembly / Acting Table Clerk / Sergeant-at-Arms — 9 variants) but did NOT detect chorus shapes (`An hon. member` / `Some hon. members` / `Interjection` and case variants — speaker_role was NULL for all of them). **What's new:** `_ROLE_PATTERNS` extended at lines 197-266 with 8 new staff entries (placed BEFORE the existing generic `The Clerk` pattern so more-specific Clerk-at-the-Table family wins — order is load-bearing) + 4 new chorus entries (`An hon. member` / `Some hon. members` / `Interjection` / `A hon. member` fallback variant); `_STAFF_ROLES` + `_GROUP_ROLES` frozensets added. `_build_speech` emission site at lines ~478-482 replaced hardcoded `speech_type="floor"` with the three-way conditional. **Outcomes:** 6,482 newly-tagged chorus rows reclassified empty-role-floor → `group` (Interjection 5,068 + An hon. member 1,154 + Some hon. members 260; the SQL set both speaker_role AND speech_type in one UPDATE pass per role); 3,033 already-tagged staff rows reclassified `floor → staff`; total **9,515 rows** in one transaction. ON attribution % unchanged at 92.5%. ON Acting Speaker (16,275) / Deputy Speaker (13,285 — has its own Pass-3 work) / rotating-chair family (~10K) explicitly out of scope. The parens-name variant `The Clerk-at-the-Table (Mr. Wai Lam (William) Wong)` (26 rows) handled by explicit `_STAFF_ROLES` membership rather than touching `_ROLE_WITH_PARENS_RE` (which is non-greedy on the first `(` so nested-parens names leave the role uncanonicalized) — the William Wong case is the only nested-parens form in the corpus, enumeration is cleaner than nested-parens parser support. `docs/research/ontario.md` line 7 status snapshot bumped to 2026-05-21. **Surprise:** ON's `parse_attribution` has TWO paths through `_ROLE_PATTERNS` (role-with-parens branch at line 314 + bare-text fallback at line 394) — adding entries to the single role table covers both shapes simultaneously, no parallel `_GROUP_RAW_RE` analog needed (unlike MB).

### Cycle 2026-05-20

- [x] **MB parliamentary-staff role-tagging + Chairperson defer-decision + TODO/timeline hygiene** (uncommitted; plan at `~/.claude/plans/what-should-we-work-cheerful-sun.md`). Close-out cycle on the MB chamber-parser empty-role bucket originally flagged at "~46K rows" in TODO.md line 75. Investigation surfaced two stale items: (a) the parser-recovery pass that flag described as pending actually shipped 2026-05-14 via `mb_speaker_role_relink.py` (see cycle 2026-05-14 entry); (b) the residual MB-unattributed bucket is 43,480 rows but mostly explicitly-deferred-elsewhere — 29,320 `Honourable Members` chorus (by-design-NULL), 10,909 `The Chairperson` rotating (deferred), 2,109 parliamentary staff (this cycle's target), 625 metadata headings (correctly classified), 224 floor edge cases (long-tail). **Slice A — parser regex extension:** `_ROLE_PATTERNS` in `services/scanner/src/legislative/mb_hansard_parse.py:263–278` consolidated two narrow patterns (`^the\s+clerk\s*$` / `^the\s+sergeant[-\s]at[-\s]arms\s*$`) into 5 parens-aware patterns covering Clerk Assistant / Deputy Clerk / Clerk / Deputy Sergeant-at-Arms / Sergeant-at-Arms with `(?:the\s+|madam\s+|mr\s+)?(?:acting\s+)?` honorific prefixes and `(?:\s+\S.*)?` trailing-name tails (since `_norm()` replaces parens with whitespace, `Madam Clerk (Patricia Chaychuk)` normalizes to `madam clerk patricia chaychuk`). Order significant (clerk-assistant before deputy-clerk before clerk) because `_match_role` returns on first match. **Slice B — daily relink picks up changes:** existing `relink-mb-speaker-roles` cron at 17:40 UTC imports `_match_role` + `_clean_speaker` directly from the parser, so the back-population is automatic. Manual `--dry-run` then live run on 2026-05-20. **Outcomes:** 2,109 staff rows role-tagged (`The Clerk: 1,586 / The Deputy Clerk: 335 / The Clerk Assistant: 93 / The Deputy Sergeant-at-Arms: 87 / The Sergeant-at-Arms: 8`); zero remaining NULL-role rows in `speech_type='staff'` MB bucket; MB attribution % unchanged (politician_id denominator unchanged by design — these are clerks, not MLAs); but the "unattributed-and-untagged" honest-coverage row count dropped 2,958 → 849 (remaining 849 = 625 metadata + 224 floor edge cases, both intentionally out of scope). **Slice C — Chairperson defer-decision captured in code:** comment block added above `ROLE_ONLY_PRESIDING_ROSTER["MB"]` in `services/scanner/src/legislative/presiding_officer_resolver.py:713` documenting the 2026-05-20 probe (`SELECT COUNT(*) FROM speeches WHERE province_territory='MB' AND speaker_role IN ('The Chairperson','The Deputy Chair') AND text ~* '(invite.*take the chair|elected.*chairperson|...)'` returned 3 of 11,170 — content references, not announcement shapes). Same shape as deferred AB Acting Speaker / BC Chairman families; re-evaluate if an external roster source (Manitoba Assembly Journals?) emerges. **Slice D — doc hygiene:** TODO.md line 75 and `docs/timeline.md` Tier-2 attribution bullet updated to strike the stale "~46K rows" cross-reference and correct the "~1.2K Chairperson" count to actual "~10,909 — deferred". No new schedules, no new commands, no migrations.

### Cycle 2026-05-19

- [x] **AB standing-committee transcripts — first provincial-committee pipeline** (commit `ebdba93`, plan at `~/.claude/plans/okay-what-do-we-validated-mitten.md`). New `services/scanner/src/legislative/ab_committees.py` mirrors `ab_hansard.py`'s PDF-fetch + pdftotext + speaker-line parser, swapping in `assembly.ab.ca/.../transcripts-by-type?comm={ACRONYM}` listings and `docs.assembly.ab.ca/LADDAR_files\docs\committees\{acronym}\...` PDF URLs. 11 standing-committee acronyms hardcoded (HS/EF/CI/CEB/FC/LO/MS/PB/PE/PA/RS). `ab_hansard.py` got a surgical change: `_pick_speech_type` is URL-aware (routes `/docs/committees/` → 'committee'); `_upsert_speech` accepts optional `committee_acronym` / `committee_name` kwargs that emit an `ab_committee` raw payload shape. Whitelist sync in both `jobs_catalog.py` + `admin.ts COMMAND_CATALOG`; daily 15:25 UTC schedule between AB Hansard (15:15) and the AB presiding resolver (15:30); seeded in `scripts/seed-daily-ingest-schedules.sql`. Speaker resolution uses a **committee-restricted lookup** that joins `politician_committees` on `committee_name + ended_at` gate (correctly rejects witnesses with surname collisions — AIMCo CIO "Mr. Lord" no longer over-matches to MLA Jonathan Lord). **Outcomes:** **9,988 speeches across 60 L31-S2 meetings** (5 active committees: HS/LO/PA/MS/RS — others didn't meet); 3,344 resolved (33.5% — committee transcripts are ~40% witnesses by nature). AB `committees_status: editorial 'live' → count-derived 'partial'`. 10,432 chunks built + embedded same-day. End-to-end semantic search returns 44 hits on `q=AIMCo+conflict+of+interest`.

- [x] **`chunk-speeches --source-system` / `--speech-type` filters** (commit `ebdba93`). The chunker's `WHERE c.id IS NULL ORDER BY spoken_at DESC` global queue sits behind a 481K-row backlog of perma-skipped tiny turns; a freshly-ingested provincial slice can't reach the front of the queue for days. New optional kwargs on `chunk_pending()` AND-in to the SELECT via asyncpg `$N` binds; default behaviour unchanged for the daily cron. Operator-facing log line surfaces filter values when set. `embed-speech-chunks` unchanged — when the unembedded pool is empty, a plain run only processes the chunks the targeted chunk just added. Reusable primitive for the next provincial-committee slice (BC/ON/QC). Verified: AB committee chunk-then-embed completed in ~3 min (first cycle, 2,017 chunks) / ~14 min (full re-run after roster augmentation, 8,415 chunks), GPU healthy throughout, 0 errors / 0 retries.

- [x] **AB committees substitute-MLA detection from PDF cover-page roster** (commit `67c4d05`). Follow-up that the AB ingester's first cycle left unaddressed. Committee-restricted lookup correctly rejected witnesses but also missed substitute MLAs — MLAs not on a committee's regular `politician_committees` roster who attend in someone's place, listed on the PDF cover page with one-or-more asterisks (e.g. `Cyr, Scott J., Bonnyville-Cold Lake-St. Paul (UC)*`). New `parse_meeting_roster` + `AttendeeRef` + `_ROSTER_LINE_RE` + `_ROLE_RE` + `resolve_attendees_to_politicians` + `augment_lookup_with_attendees` in `ab_committees.py`. Regex handles compound surnames (`Calahoo Stonehouse`), lowercase-prefix surnames (`van Dijken`, `de Jonge`), honorific-in-first (`Hon. Ric`), middle initials with period, Acting-Chair role variant, and substitute markers both after the paren (`(UC)*`) and after the comma (`(NDP),*Acting Chair`). FK-matched attendees merge into the existing per-meeting SpeakerLookup, skipping already-present IDs. New `IngestCommitteeStats` counters surface per-run health. **Outcomes:** 589 roster lines parsed across 60 meetings, 553 (94%) FK-resolved, 78 net-new substitutes augmented. HS 2026-04-13 attribution went from 35 → 53 resolved (+18 turns: Cyr 13 / Petrovic 4 / Schulz 1); witness control preserved (Lord / Thompson stay NULL).

- [x] **BC Committee-of-the-Whole misclassification — resolved** (uncommitted; investigation prompted by *Now* item (d) on the timeline). The BC chamber parser's `SECTION_TO_TYPE` map in `services/scanner/src/legislative/bc_hansard_parse.py:289` routed three Committee-of-the-Whole-family section headings (`Committee of the Whole` / `Committee of the Whole House` / `Committee of Supply`) to `speech_type='committee'`, leaking **34,386 BC speeches across 235 chamber-Hansard documents (2008-2026)** into the standing-committee bucket. All 34,386 confirmed as those three sections via `raw->'bc_hansard'->>'section'` aggregation — none are real standing-committee transcripts. C-of-W is chamber business (same MLAs, same room, just resolved into committee for clause-by-clause), so the right type is `floor`. **Fix:** parser remapped (with comment explaining the rationale + the `raw.bc_hansard.section` preservation seam that makes the change reversible); one-shot `UPDATE` reclassified all 34,386 rows (0 orphans); BC `jurisdiction_sources.committees_status` flipped from the false-positive `live` to `none` (BC has neither transcripts nor a `politician_committees` roster, so `none` is honest). **Side effects:** `/api/v1/committees/meetings` listing dropped 324 → 89 (BC 235 gone, AB 60 + federal 29 remain — exactly the genuine standing committees); `speech_chunks` doesn't denormalize `speech_type` (search uses EXISTS subquery on `speeches`), so no chunk-table update needed; the `committee-meetings.ts` comment block warning is replaced with a "resolved 2026-05-19" note. Closes the unfinished investigation that the API route already documented at design time but never executed.

### Cycle 2026-05-18

- [x] **Federal committee transcripts — `speech_type='committee'` via openparliament fabric** (uncommitted; plan at `~/.claude/plans/hay-dude-i-know-bubbly-sun.md`). Phase 4 of `docs/plans/semantic-layer.md` finally landing now that votes shipped 2026-04-30 cleared the gate. Small PR by code volume because the infrastructure was ~90% pre-built — `federal_hansard.py:_pick_speech_type()` was already routing `document_url.startswith("/committees/")` to `speech_type='committee'`; chunker, embedder, API search routes, frontend `SPEECH_TYPE_VALUES`, and `SpeechFilters.tsx` were already speech_type-agnostic. **What's new:** `fetch_committee_meetings()` paginates openparliament's `/committees/meetings/?format=json&date__gte=…&date__lte=…` endpoint, filtering `has_evidence=true` (transcripts publish on irregular delays; 14-day overlap re-visits until evidence lands) and skipping in-camera by default. `ingest_committees()` mirrors `ingest()` but swaps the discovery loop; everything else (session FK, slug→politician cache, `fetch_speeches_for_document`, `_upsert_speech`) reused verbatim. New `ingest-federal-committees` Click command mirrors `ingest-federal-hansard` surface (parliament/session/since/since-days/until/limit-meetings/limit-speeches/include-in-camera), uses `clamp_since_with_days()` precedence + `federal_session_bounds()` 896k-mis-tagging guardrail. Whitelist sync: `jobs_catalog.py` + `admin.ts COMMAND_CATALOG` (CLAUDE.md two-place rule). Schedule seeded daily 11:20 UTC `{since_days: 14}` between federal-hansard (11:15) and federal-votes (11:30); mirrored into `scripts/seed-daily-ingest-schedules.sql`. `coverage_stats.py` extended with `_committees_status` derivation (5,000 threshold for 'live', 1-4999 'partial', None preserves editorial — AB's roster-based 'live' stays untouched until/unless AB committee transcripts ship; YT 'blocked' preserved). **Outcomes:** P45-S1 first run **5,471 speeches across 30 meetings, 100% MP politician-FK** via openparliament_slug (witnesses correctly land as `politician_id=NULL` — same shape as unresolved floor speakers). 4,896 of 5,430 chunks embedded before GPU went unhealthy mid-run; remaining 534 queue for next daily run. **Coverage flip: federal `committees_status: 'none' → 'live'`.** `docs/research/federal.md` Committee Activity section updated. **Out of scope (deferred follow-ups):** historical backfill (`--all-sessions` walker for P39-P44 — gated on GPU regression mitigation, since sustained embed load reliably re-triggers Xid 62 on `nvidia-open` 580.142); provincial committee pipelines (research-handoff gated per province; AB difficulty=2 natural next); witness schema (`speaker_type` column / `witnesses` table) — deferred until analytics use case emerges; `bill_events.committee_name` ingestion from LEGISinfo XML (column has existed since migration 0009 with UNIQUE constraint participation; `federal_bill_events.py` parses milestones but skips committee referrals — natural pair with transcripts).

- [x] **GPU regression re-surfaced mid-smoke-test — same Xid 62 fault class as 2026-04-15** (operational, no commit; documented in `project_embed_regression.md` memory). At 11:08 UTC, `pid=text-embeddings`: Xid 62 (`Internal micro-controller error`) → 8× Xid 45 (`Preemptive cleanup`) on CUDA channels 0x0c–0x13 → Xid 154 explicitly transitioning the GPU into `GPU Reset Required` state. Subsequent `GspRmAlloc` calls returned `NV_ERR_RESET_REQUIRED (0x00000062)` — GPU itself refused to allocate new channels. Driver: `NVIDIA UNIX Open Kernel Module 580.142` — confirming we're still on the `nvidia-open` variant suspected as root cause in the original memory; planned mitigation (proprietary `nvidia-driver-580` swap) hasn't been done. Trigger this time: 30-meeting committee smoke test pushed ~170 sequential 32-chunk batches through TEI in a tight loop — sustained CUDA work is the trigger, not routine daily ingest. **Resilience layer worked exactly as designed:** TEI's device-aware healthcheck flipped to unhealthy on CPU fallback, scanner-side preflight refused subsequent batches, 5-consecutive-batch-failure abort guard fired cleanly. **Mitigation:** `apt remove --purge nvidia-open* && apt install nvidia-driver-580 && reboot` — pure package swap, ~15-minute change, no physical device work. Flagged in *Now*; gates comfortable historical-backfill work.

### Cycle 2026-05-16

- [x] **Daily-ingest survey — forward-incremental harness + 3 phases of stabilization** (commit `54358ba`, plan at `.claude/plans/we-want-to-work-mutable-liskov.md`). **Phase 1 — daily-job stabilization (5 fixes):** `jobs_catalog.py` consolidated 7 duplicate dict keys (`ingest-{quebec,manitoba}-mlas`, `ingest-{nb,nl,ns}-former-mlas`, `relink-mb-speaker-roles`, `resolve-role-only-presiding-officers`) — silent dict-merge had been masking arg-spec mismatches causing never-succeeded weekly roster failures. `speech_chunker.py` bumped `sync_chunk_denorm` timeout 600s → 1800s after Tier-2 attribution burst pushed daily run past cap (603s on 2026-05-15). `mb_votes.py` + `nl_votes.py` broadened SQL pre-filter to match the regex classifier's `(Motion|Amendment|Bill|Question)` shape instead of `motion` only — lifts MB 47 → 413 votes (+292 bill_links), NL 2 → 14 votes. **Phase 2 — forward-incremental pass (12 pipelines):** new `services/scanner/src/legislative/_forward.py` with `resolve_since` (DB high-water minus 14d overlap) and `clamp_since_with_days` (only narrows when explicit `--since-days` passed; preserves load-bearing "full session" default added after the 896k mis-labelled federal speeches incident). `extract-federal-votes` skips per-vote ballot fetches below cutoff — 1565s → 35s (~45×), 22 api_calls vs ~1500. `ingest-federal-hansard` + 10 provincial Hansard ingesters (`ingest-{bc,qc,ns,on,mb,ab,nb,nl,sk,nt}-hansard`) gained `--since-days` clamp narrowing the session-bounded default — federal-hansard 1097s → 11s (~100×), bc-hansard 319s → 51s (~6×). `ingest-federal-bills` skips per-bill detail fetches for bills introduced before cutoff. Live `scanner_schedules` rows updated to pass `{"since_days": 14}`; seed script updated to match so a pgdata rebuild reproduces same state. **Phase 3a — admin `COMMAND_CATALOG` sync (18 missing commands):** ON Hansard chain (4 cmds), SK chain (3 cmds), `ingest-federal-bills`, 3 resolvers (ab-speakers / bc-allcaps / inline-presiding-officers), 7 escribe entries. 110/110 keys in lockstep with scanner `jobs_catalog.py`. **Phase 3b — SK votes pipeline** (first per-MLA roll-call on a consensus-shape province): new `services/scanner/src/legislative/sk_votes.py` — Journals PDF parser, 127 PDFs from `/legislative-business/journals/`, structured `YEAS / POUR — N / NAYS / CONTRE — M` grids parsed via pdftotext reading-order, surnames FK-matched against `sk_assembly_slug` with `(Constituency)` parens disambiguation. New `extract-sk-votes` Click cmd + catalog + admin entry + daily 22:50 UTC schedule. Smoke test on 29L3S (2023 Journal): 11/11 divisions parsed correctly, 9 votes / 410 vote_positions inserted, 372/410 (90.7%) MLA resolution. **Phase 3c — NU Hansard pipeline + 5th & 6th Assembly rosters** (see NU checkbox above for full outcome). **Estimated O(1,000–3,500) historical SK votes recoverable** via `--all-journals` backfill (deferred).

- [x] **In-flight scanner fixes — 5 operator-side maintenance changes** (commit `8ae9e24`). **(a) `on_hansard.py`:** scoped the post-ingest `speech_chunks` UPDATE to the date floor of the just-processed sittings (`idx_chunks_filters` lookup) instead of unscoped seq-scanning ~5M ON chunk rows. The unscoped form had been exceeding asyncpg `statement_timeout` on every daily run since 2026-05-03; scoped form keeps the join under the index. **Fixes the 14-consecutive-daily-failure `ingest-on-hansard` timeout streak (5/3–5/14, succeeding 5/14+).** **(b) `qc_bills.py`:** wraps RSS fetch in try/except with `verify=False` fallback + early-return on `httpx` errors. assnat.qc.ca SyndicationRSS-210 endpoint now serves a FortiADC WAF challenge (`waf_deny_rule_id=1190000001`) with missing intermediate cert ahead of it; CSV path (`ingest-qc-bills`) remains the bill-discovery source of truth. Daily `ingest-qc-bills-rss` logs warning and exits 0 instead of failing. **(c) `federal_hansard.py`:** closes P44-S1 at 2024-12-17 (dissolution) and adds **P45-S1 starting 2025-05-26** to `FEDERAL_SESSION_DATES`. **(d) `politicians_op_backfill.py`:** broadens missing-slug collector to also source from `vote_positions.politician_name_raw` (federal-votes shape) alongside `speeches.raw->'op_speech'->>'politician_url'` (Hansard shape) — lets newly-sitting MPs land via division ballots before they appear in any speech. **(e) `opennorth.py`:** switches `constituency_boundaries` upsert to write `boundaries_version + effective_from` per migration 0021 temporal schema — `ON CONFLICT (constituency_id)` raised `InvalidColumnReferenceError` because the table's unique key is the `(constituency_id, boundaries_version)` pair. `docs/research/yukon.md` + `prince-edward-island.md` appended with 2026-05-14 re-probe notes — YT yukonassembly.ca + yukon.ca both Cloudflare cf-mitigated; PE bills page returns 200 OK with a 1.4KB perfdrive challenge stub. **Net:** no server-side path to PE/YT; correctly classified to *Later*.

### Cycle 2026-05-14 (fourth arc — clearing the deferred follow-ups)

- [x] **MB 1970-01-01 fallback-date bug + remaining edge-case attribution misses** (uncommitted). Closes the two deferred follow-ups flagged in the third arc's TODO. Six slices. **Slice 1 (1970-fallback root cause + backfill):** root cause at `mb_hansard_parse.py:455` — `extract_sitting_date()`'s tag-strip path didn't decode HTML entities, so `&nbsp;` between the day-of-week and month in legacy MB sittings (`Tuesday,&nbsp;June 12, 2007`) blocked the `_HEADER_DATE_RE` match and sent the parser into the `or date(1970, 1, 1)` fallback. One-line fix: added `stripped = _decode_entities(stripped)` between the tag-strip and whitespace-collapse. Also relaxed `_HEADER_DATE_RE` to accept an optional comma after the month name (one 39_2/vol_38a sitting had a typo `Thursday,  May, 8, 2008`). Backfill: ad-hoc Python script using cached `raw_html` on each affected sitting's sequence=1 row, called the corrected `extract_sitting_date`, UPDATEd `spoken_at` preserving the time-of-day component. **1,803 rows fixed across 10 sittings spanning 2007-04 → 2008-09 (38L-5S / 39L-1S / 39L-2S).** **Slice 2 (surname-only FK miss):** Mrs. Driedger (68 rows) / Mrs. Stefanson (18) / Mr. Robinson (4) were unresolved because surname-only ambiguity (3 Driedgers / 2 Stefansons / 2 Robinsons in MB politicians). After the date backfill in Slice 1, `resolve-mb-speakers-dated` could finally fire — date-windowed narrowing collapsed each to a unique candidate. **94 rows resolved.** **Slice 3 (paren-only constituency fragments — Radisson / Brandon West / etc.):** root cause is a multi-paragraph speaker-name parser bug — MB Hansard sometimes splits `MLA Jelynn Dela Cruz (Radisson):` across THREE paragraphs (`<b>MLA Jelynn </b>` → `<p class=MsoHeading7><a>Dela Cruz</a></p>` → `<b> (Radisson):</b> I, first…`), the parser classifies the first two as headings (overwriting current_section) and binds speaker_name_raw to just `(Radisson)`. Did NOT fix the parser (regression risk; multi-paragraph speaker reconstruction is a deep change). Instead, a one-shot Python backfill: for each paren-only row, used the `raw->'mb_hansard'->>'section'` field (which captures the last heading) as a surname/full-name hint, looked up MB politicians by last-name (compound-surname-aware), narrowed by date-windowed `politician_terms` when ambiguous. **61 of 111 paren-only rows resolved.** Sample resolutions: `(Radisson)` + section "Dela Cruz" → Jelynn Dela Cruz; `(Premier)` + section "Selinger" → Gregory Selinger; `(Minister of Mineral Resources)` + section "Chomiak" → David Walter Chomiak; etc. All 10 spot-checks correct. **Slice 4 (Pass-1 Acting Chairperson miss):** `The Acting Chairperson (Mr. Reid)` (25 rows, 2008-2010) failed Pass-1 surname disambiguation because 3 MB politicians have last_name="Reid" (Daryl Reid + two 19th-century). On the 2008-2010 dates only Daryl Reid was active. One-shot SQL UPDATE bound all 25 to Daryl Reid at confidence 0.85. **Slice 5 (soft-hyphen `An Hon­our­able Member` group-tag slip):** 8 MB rows tagged `speech_type='floor'` that should be `group`, blocked by U+00AD SOFT HYPHEN characters embedded in `Hon­our­able`. Two-part fix: (a) one-shot UPDATE on existing 8 rows; (b) parser-side `_clean_speaker` now strips soft hyphens so future ingests stay clean. Also widened `_GROUP_RAW_RE` to accept optional period after `Hon` (`Hon\.?(?:ourable)?`) for forward-compat against the dotted variant — MB doesn't use it but other provincial parsers might. **Slice 6 (parser regex resilience for future ingests):** `_HEADER_DATE_RE` extended with optional comma after month name; `_decode_entities` call inserted into `extract_sitting_date` tag-strip path. Both changes harden the parser against the same shape of bug re-introducing itself. **Outcomes:** **0 rows remaining at spoken_at='1970-01-01'** (down from 1,803); **MB attribution 89.33% → 89.37%** (small headline lift because the 1,803 date-fixed rows mostly already had politician_id set — the bug was the date, not the attribution; the actual attribution lift came from the 94 surname-only + 94 paren-only resolutions); **all 94 surname-only FK-miss rows attributed**; **61 of 111 paren-only constituency-fragment rows attributed**. **Out of scope:** 50 paren-only rows whose section heading is generic ("Introduction of Guests" / "Motion presented." / similar non-name fragments) — fundamentally can't be resolved without a parser fix that reconstructs the multi-paragraph name; deep parser refactor deferred.

### Cycle 2026-05-14 (third arc — operational hygiene)

- [x] **AB/BC/MB Pass-3 daily-cron schedule rows seeded** (uncommitted). Closes the schedule gap flagged in both earlier arcs' TODO entries — `resolve-role-only-presiding-officers` was whitelisted in both `jobs_catalog.py` + `admin.ts COMMAND_CATALOG` since today, with only SK seeded at `35 22 * * *`. Three new `scanner_schedules` rows: BC `50 14`, AB `50 15`, MB `50 17` — each 5 minutes after the existing presiding-speaker resolver in its province's daily chain, keeping role-only resolution downstream of Tier-1 Speaker attribution. Verified via `SELECT name, cron, args FROM scanner_schedules WHERE command='resolve-role-only-presiding-officers' ORDER BY cron` — 4 rows present (AB / BC / MB / SK).
- [x] **MB heading-misclassification cleanup — parser-side filter + SQL backfill** (uncommitted). MB chamber parser was inserting committee-transcript section headings (`Meetings`, `Committee Membership`, `Matters under Consideration`, `Officials Speaking on Record at the …`, etc.) as `speech_type='floor'` rows because the existing `_is_heading()` structural detection (`MsoHeadingN` class + pure-bold-no-colon rule) missed shapes with trailing colons or embedded styling. **Slice 1 (parser):** new `_HEADING_TEXT_RE` in `services/scanner/src/legislative/mb_hansard_parse.py` covering 20+ named-section-heading shapes from committee transcripts; wired into the speaker-line detection branch at line ~547 so heading-shaped text now updates `current_section` instead of opening a turn. Regex unit-tested on 30 cases (20 headings + 10 real speakers); 0 false positives. **Slice 2 (backfill):** one-shot SQL UPDATE applied the same regex to existing rows: 628 MB rows reclassified `speech_type='floor' → 'metadata'`. The `speech_type` column is plain text (no enum), so `'metadata'` is schema-safe; downstream coverage queries that filter on `speech_type='floor'` will correctly exclude these going forward. Explore-agent estimate was ~1,183 rows but the inflated count included real-speaker FK-miss rows (Mrs. Driedger / Mrs. Stefanson / Mr. Robinson / constituency fragments) which are separate work — actual heading-only count is 628 across 111 distinct heading values. **Out of scope:** `Mrs. Driedger` / `Mrs. Stefanson` / `Mr. Robinson` (~94 rows total — real-speaker FK misses, parser extracted surname but couldn't bind to politicians); `(Radisson)` / `(Brandon West)` / `(Fort Whyte)` constituency fragments (~38 rows — parser bug eating the speaker-name part); `The Acting Chairperson (Mr. Reid)` (25 rows — should match Pass 1 paren-name resolver but doesn't populate politician_id); `An Honourable Member` (6 rows — should be tagged `speech_type='group'` by the existing _GROUP_RAW_RE but slipped through); MB 1970-01-01 fallback-date bug (~1,803 rows — explicitly deferred per "troublesome → disregard" guidance).

### Cycle 2026-05-14 (second arc)

- [x] **MB chamber-parser empty-role bucket: regex extension + relink + Deputy Speaker pre-43L roster expansion** (uncommitted; plan at `~/.claude/plans/what-needs-doing-in-cozy-neumann.md`). Closes the explicit follow-up flagged in the earlier arc's TODO entry: "MB Tier-1 chamber-parser empty-role miss (~46K rows) — different attack entirely from Tier-2 Pass 3." Actual current count was 21,490 (the 46K figure pre-dated the 2026-05-06 named-speaker resolver Phase C). Three slices + one drive-by. **Slice A — parser regex extension** in `services/scanner/src/legislative/mb_hansard_parse.py`: added 4 entries to `_ROLE_PATTERNS` catching `Mr./Mrs./Ms./Madam` honorific-prefixed variants of `Speaker`, `Deputy Speaker`, `Chairperson`, `Deputy Chairperson` — same semantic roles as the existing `^the\s+...` patterns, routed to identical canonical strings. The MB parser's `_norm()` lowercases + strips punctuation, so `Mr. Deputy Speaker` becomes `mr deputy speaker` for matching. No deletions, no behaviour change to existing patterns. **Slice B — one-shot relink:** new `services/scanner/src/legislative/mb_speaker_role_relink.py` exporting `relink_mb_speaker_roles(db, *, limit, dry_run)` — imports `_match_role` + `_clean_speaker` from the parser so the relink and future ingests share the regex set byte-for-byte (single-source-of-truth: adding a new role pattern to the parser auto-widens the relink). New `relink-mb-speaker-roles` Click command in `__main__.py` mirroring `relink-bill-introduced-dates` shape; whitelisted in both `jobs_catalog.py` + `admin.ts COMMAND_CATALOG`. Dry-run + real-run both reported `scanned=23,597 role_assigned=20,452` with by_role breakdown `{The Deputy Speaker: 10,624, The Chairperson: 9,461, The Speaker: 278, The Deputy Chair: 89}` — within 1% of the planned estimates. **Slice C — MB Deputy Speaker roster expansion:** `ROLE_ONLY_PRESIDING_ROSTER["MB"]["Deputy Speaker"]` extended from 1 entry (Tyler Blashko 43L only) to 6 entries spanning 37L–43L (1999-10-25 → present): Conrad Santos (37L+38L), Marilyn Brick (39L), Thomas Nevakshonoff (40L), Doyle Piwniuk (41L), Andrew Micklefield (42L), Tyler Blashko (43L). Names + per-Legislature windows mined from Pass-1-attributed inline-paren rows (`Mr./Madam Deputy Speaker (NAME)` shapes already resolved by `inline_presiding_resolver`) — a free in-corpus source of truth identical-in-spirit to AB's "Hansard records election announcements verbatim" mining from cycle 2026-05-06 phase 1a. All 6 candidates pre-existed in `politicians` with clean first_name/last_name; no new stubs created. **Slice D — run resolvers + verify lift:** `relink-mb-speaker-roles` (20,452 rows role-tagged) → `resolve-presiding-speakers --province MB` (Tier-1 picked up 2 of 287 Speaker rows; 276 were 1970-01-01 fallback-date corpus artifacts outside the roster window — separate data-quality issue, out of scope) → `resolve-role-only-presiding-officers --province MB` (Pass-3 attributed all 11,688 Deputy Speaker rows across the 6 historical entries: 5000 Micklefield + 2207 Piwniuk + 1326 Brick + 1076 Blashko + 752 Santos + 342 Nevakshonoff; 12 leftovers were 1970-fallback dates). First Pass-3 run timed out partway; re-ran with `ASYNCPG_COMMAND_TIMEOUT=300` for clean completion. Spot-checked 12 random Deputy Speaker rows; 12/12 correct (including one self-referential Micklefield 2022-10-27 turn: *"Maybe not the most formal Deputy Speaker ever, but I do want to acknow..."*). **Outcomes:** MB overall attribution **86.67% → 89.33%** (+2.66pp, +10,985 speeches at confidence 0.85). Chairperson/Deputy Chair ~10,971 rows newly role-tagged but unattributed — sets up future Pass-3 rotating-role work cleanly; attribution itself deferred consistent with AB Acting Speaker / BC Chairman family (no in-corpus chair-handover signal). **Schedule seeded:** daily 17:40 UTC `MB speaker-role relink (parser regex catch-up)` in `scanner_schedules`, between MB Hansard ingest (17:00) and the MB presiding-speakers resolver chain (17:45) — belt-and-suspenders against future ingests producing new empty-role rows for any speaker_name_raw shape not yet in `_ROLE_PATTERNS`. coverage_stats refreshed. **Out of scope:** 276 `The Speaker` rows + 12 `The Deputy Speaker` rows with `spoken_at=1970-01-01` corpus-fallback dates (separate data-quality issue — MB Hansard date extractor falls back to 1970 when sittings can't be parsed); MB Chairperson/Deputy Chair attribution (rotating, deferred); 860 heading misclassifications (`Meetings` / `Committee Membership` shapes — Slice E flagged but skipped); 187 misc edge cases (`Mrs. Driedger` 68 rows + `(Radisson)` constituency-only + Pass-1-should-have-caught fragments — separate small follow-ups).

### Cycle 2026-05-14 (first arc)

- [x] **ON Speaker historical roster backfill (Tier-1 Pass 0 expansion) + SK Deputy Speaker (Tier-2 Pass 3 propagation)** (uncommitted; plan at `~/.claude/plans/what-needs-doing-in-cozy-neumann.md`). Two slices, both roster-only data changes in `services/scanner/src/legislative/presiding_officer_resolver.py`. **Slice A — ON Speaker historical:** the `SPEAKER_ROSTER["ON"]` entry that had carried just Donna Skelly (44L only) since the ON Hansard ingest shipped was extended to 12 entries spanning 32L–44L (1981–present): John Melville Turner (32L), Hugh Alden Edighoffer (33L+34L), David William Warner (35L), Allan Kenneth McLean (36L 1st), Chris Stockwell (36L 2nd), Gary Carr (37L), Alvin Curling (38L 1st), Michael A. Brown (38L 2nd), Steve Peters (39L), Dave Levac (40L+41L), Ted Arnott (42L+43L), Donna Skelly (44L). Boundary dates from Wikipedia "Speaker of the Legislative Assembly of Ontario" with consecutive entries chaining via next-Speaker-start = previous-Speaker-end so the two intra-Legislature successions (McLean → Stockwell 1996-10-03 after the McLean resignation scandal; Curling → Brown 2005-10-11 after Curling left to be Ambassador to the Dominican Republic) attribute cleanly. **Outcomes:** ON `The Speaker` 46,415 unattributed → 0; **ON overall attribution 87.54% → 92.45%** (+4.91pp). All 11 historical Speakers already existed in `politicians` with clean first_name/last_name from the ON Hansard parens-name parser; no new stubs created. Spot-checked 12 random rows across 1989-2006 spanning 5 Speakers; 12/12 correct. **Slice B — SK Deputy Speaker (Pass 3 propagation):** added SK to `ROLE_ONLY_PRESIDING_ROSTER` and `ROLE_ONLY_OFFICE_MAP["SK"]["deputy_speaker"]` (lowercase snake — the SK parser emits `deputy_speaker`, not `The Deputy Speaker`). 4-entry roster covering 28L3S–30L mined directly from in-Hansard election announcements (`it is my duty to inform you that … has been elected … as your Deputy Speaker and Chair of Committees of the Whole Assembly`): Nadine Wilson 2020-12-01 (29L start), Joe Hargrave 2021-10-28 (after Wilson left the Sask Party caucus over vaccine policy), Fred Bradshaw 2023-10-10, Blaine McLeod 2024-11-26 (30L start). **Outcomes:** SK `deputy_speaker` 2,174 unattributed → 0; **SK overall attribution 81.91% → 86.10%** (+4.19pp). Spot-checked 12 random rows; 12/12 correct. **Drive-by fix:** `_find_politician_id` `LIMIT 1` lookup was nondeterministic — added `ORDER BY is_active DESC, created_at ASC` to both the exact-match and surname-fallback queries. SK has a known duplicate-politician problem (the same MLA appears with both `legassembly.sk.ca:30L-speaker-index:slug` and `opennorth:saskatchewan-legislature:slug` source_ids) — Blaine McLeod would otherwise have bound to the orphan stub. Defensive change benefits every roster, not just SK. **Whitelist sync:** added `resolve-role-only-presiding-officers` to both `services/scanner/src/jobs_catalog.py` and `services/api/src/routes/admin.ts COMMAND_CATALOG` per CLAUDE.md § Admin panel two-place rule (the command existed in `__main__.py` but neither catalog since the Pass-3 phase-1a ship in cycle 2026-05-06 — schedule gap was latent). **Schedule seeded:** daily 22:35 UTC `SK role-only presiding (Deputy Speaker)` row in `scanner_schedules`, sitting between the existing SK presiding-speaker resolver (22:30 UTC) and the cross-jurisdictional inline-presiding-officer resolver (23:30 UTC). **Net deliverable:** +48,589 speeches attributed (46,415 ON + 2,174 SK) at confidence 0.85–0.9; ~37K speech_chunks updated. coverage_stats refreshed so `/coverage` reflects the lift. **Out of scope this round (deferred):** ON `The Acting Speaker` (16,275 rotating, same family as deferred AB Acting Speaker — no in-corpus name signal), SK `chorus` (4,841 generic), AB/BC/MB role-only Pass-3 daily-cron schedules (their resolvers exist + roster shipped 2026-05-06, but no `scanner_schedules` entries — same gap that motivated this cycle's SK schedule seed; should be backfilled in a future cycle).

### Cycle 2026-05-06

- [x] **Tier-2 Pass 4 — cross-jurisdictional named-speaker resolver + MB ingester fix** (commits `59f0c7a` + `<pending>`). Two-phase ship targeting MB's 46K-row Tier-1 chamber-parser miss bucket and similar surfaces in AB/NB/ON. **Phase B (commit `59f0c7a`):** Fixed `mb_former_mlas` parser to handle 4 banner format variants on gov.mb.ca (relaxed surname char class for Mc/Mac/Van prefixes; new no-comma regex for `<strong>SELBY Erin</strong>` shape; adjacent-strong fallback for split `McFADYEN, / Hugh`; `<p>`-tag fallback for `<p>McGIFFORD, Diane</p>`). Bonus fix: term-source precedence is now page-aware (narrative wins on living, strong wins on deceased) so cabinet-minister portfolio spans don't masquerade as MLA tenure spans. **Outcomes:** MB politicians 798 → 880 (+82); names_seen 824 → 887; previously-missing Selby/McFadyen/McGifford + ~60 others now in roster. Spot-checked all 3 named MLAs against Wikipedia — terms align. **Phase C (`<pending>`):** New `named_speaker_resolver.py` + `resolve-named-speakers` Click command. Targets `politician_id IS NULL` + NULL/empty `speaker_role` + name-bearing `speaker_name_raw`. Three confidence buckets: 0.85 (full-name single match), 0.80 (surname-only single match), 0.75 (date-windowed narrowing closed ambiguity). Pre-filters vocatives (`Mr. Speaker`), parliamentary staff (Clerks/Sergeants), and generic placeholders (`An Honourable Member`). Cross-jurisdictional regex handles AB-style `Mr. R. Miller` initial-first, `Mr. Schow moved` trailing-verb, `Member Brar` honorific variant. **Outcomes:** MB Pass 4 +21K (81.1% → 86.2%); AB +2.6K (90.5% → 91.1%); ON +4.6K (86.6% → 87.5%); NB +1.3K (61.6% → 62.2%, limited by surname ambiguity without first names — `fk_hits_dated=0` indicates sparse NB `politician_terms` date windows); BC ~30 (BC's 28K NULL rows are mostly ALL-CAPS pre-1990 forms handled by `bc_allcaps_resolver`, not Pass-4 territory). **Cross-jurisdictional total: ~30K speeches resolved.** Spot-checks across all provinces correct against Wikipedia / historical record. **Bonus fix shipped same cycle:** `mb_former_mlas` event regexes were missing two filler patterns ("Not a candidate **in** g.e. <date>" and "Elected **by-election** <date>") — added to `_EVENT_END_RE` / `_EVENT_START_RE`, plus the term upsert now UPDATEs `ended_at` on existing rows when it differs (was SKIP-only). Closed Oscar Ferdinand Bjornson's 1966-NULL stale term to 1966-1969 + restored Hugh McFadyen's 2005-2007 first term that was missing. **NB / NS / NL historical-roster backfills shipped 2026-05-06** as a coordinated trio. NB (58L-60L, 85 MLAs, 62.2%→93.9%, +7,400 speeches), NS (62L-64L, 99 MLAs, 66.5%→91.7%, +16,150 speeches), NL (50L, 44 MHAs, 59.2%→63.2%, +1,900 speeches — modest because NL's miss bucket is dominated by `SOME HON. MEMBERS:` heckles and `CHAIR:` rotating roles outside Pass-4 domain). All three rosters hand-curated from Wikipedia per-Legislature articles (legnb.ca / nslegislature.ca / assembly.nl.ca don't publish machine-readable former-members directories). Pass 4 regex made case-insensitive + Hon-as-sole-honorific to handle NS's ALL-CAPS `HON. ZACH CHURCHILL` shape; new `_NAMED_INITIAL_RE` added for NL's `J. BROWN:` initial-prefix shape. Compound-surname fallback (`H. CONWAY OTTENHEIMER:` → matches DB's `last_name='Ottenheimer'` after retrying with last word only). All three ingesters scheduled weekly Sun 03:30/03:35/03:40 UTC. **NL + MB chamber-parser staff/group classification shipped 2026-05-06.** Both jurisdictions had their headline attribution % depressed by mis-classified non-politician rows. NL's miss rate was inflated by 11,409 `SOME HON. MEMBERS:` heckles (already tagged `speech_type='group'`) plus 583 staff turns tagged `speech_type='floor'`; MB's miss rate had 29,233 heckles AND 2,126 staff turns ALL tagged `speech_type='floor'`. Both parsers updated to derive `speech_type` from the raw label (priority: group → staff → floor), plus one-shot SQL backfills on existing data. **Outcomes:** NL 36.8% miss → real-speech attribution **84.7%**; MB 86.2% → real-speech **93.3%**. Same staff-classification pattern would apply to other jurisdictions if their parsers don't already separate parliamentary-staff turns (NS, AB, ON unchecked). **Pending:** the 5,230 NL `CHAIR:` rotating-role rows + the 25K MB unresolvable real-speech remainder (mostly Pass-4-ambiguous surnames + older Hansard format edges). NL CHAIR same shape as deferred AB Acting Speaker work — no inline name signal in NL Hansard. Single-entry roster: Tyler Blashko (Lagimodière, NDP), MB Deputy Speaker from 2023-11-21 (43L opening, same date as Lindsey Speaker acclamation) → present. New `ROLE_ONLY_PRESIDING_ROSTER["MB"]` + `ROLE_ONLY_OFFICE_MAP["MB"]["The Deputy Speaker"] = "Deputy Speaker"`. Pre-43L MB chamber parser used inline-name shapes (`Madam Deputy Speaker (Marilyn Brick)` etc.) which Pass 1's `inline_presiding_resolver` already handled — so MB only emits the role-only `The Deputy Speaker` shape for the current 43rd Legislature, making this a clean single-entry vertical slice. Confirmed by querying Pass-1 attributed rows for MB Deputy Speaker (all 23 hits resolve to Tyler Blashko). **Outcomes:** MB `The Deputy Speaker` 0% → **100%** (+1,027 speeches resolved at confidence 0.85). Spot-checked 6 random rows across 2024-2026; all 6 attribute to Tyler Blashko. **Out of scope this round:** MB `The Chairperson` (~1,262 rotating-role rows, same shape as deferred AB/BC rotating buckets); MB Tier-1 chamber-parser empty-role bucket (~46K rows where MB parser left `speaker_role` NULL on regular MLA speeches — different attack, separate from Tier-2 Pass 3).

- [x] **Tier-2 Pass 3 phase 1c — BC Deputy Speaker** (commit `e3c10da`, follows `a571979`). Adds BC to `ROLE_ONLY_PRESIDING_ROSTER` and `ROLE_ONLY_OFFICE_MAP`. 4-entry roster covering 39L→43L: Linda Reid (2009-08-25 → 2013-06-26, became Speaker for 40L) / Raj Chouhan (2017-06-22 → 2020-12-07, became Speaker for 42L) / Spencer Chandra Herbert (2020-12-07 → 2024-10-19) / Mable Elmore (2025-03-01 → present). BC's chamber parser emits bare `Deputy Speaker` (no "The" prefix), distinguished from AB in `ROLE_ONLY_OFFICE_MAP["BC"]["Deputy Speaker"] = "Deputy Speaker"` vs `ROLE_ONLY_OFFICE_MAP["AB"]["The Deputy Speaker"] = "Deputy Speaker"`. Roster sourced from per-Parliament Wikipedia pages (41L+42L+43L) + targeted web searches for Reid 2009 appointment + Elmore March 2025 appointment. **Outcomes:** BC `Deputy Speaker` 0% → 83.5% (+4,267 speeches resolved at confidence 0.85). 842 unattributed remain across three documented gaps: 38L (2008+early 2009, ~376 rows — no public source identified the 38L Deputy Speaker), 40L (2013-2017, ~408 rows — Linda Reid was 40L Speaker, Raj Chouhan was Assistant Deputy Speaker, but the actual 40L Deputy Speaker couldn't be confirmed from public sources), Feb 2025 (~58 rows — between 42L Spencer Chandra Herbert end and Mable Elmore March 2025 appointment). Spot-checked 12 random rows across 39L/41L/42L/43L windows; all 12 attribute correctly. **Out of scope this round:** BC `Chairman` / `The Chair` rotating-role buckets (~24K rows total, similar shape to the deferred AB Acting Speaker work).

- [x] **Tier-2 Pass 3 phase 1b — AB Deputy Speaker pre-2005 + Deputy Chair of Committees full chain** (commit `a571979`, follows `daef1e4` Pass 3 phase 1a). Two roster extensions to `ROLE_ONLY_PRESIDING_ROSTER["AB"]`: pre-Marz Deputy Speaker filled with Donald A. Tannas (1993-08-30 → 2005-03-01, covers 23L+24L+25L) closing the prior 24L+25L gap; Deputy Chair of Committees added as a new office with 10 entries spanning 1997-04-14 → present (Gordon 24L / Shariff 25L+26L / Mitzel + Zwozdesky 27L mid-Leg cabinet swap / Jablonski 28L / Feehan + Sweet 29L mid-Leg cabinet promotion / Milliken + Reid 30L mid-Leg cabinet promotion / van Dijken 31L). `ROLE_ONLY_OFFICE_MAP["AB"]` extended with `"The Deputy Chair" → "Deputy Chair of Committees"`. **Roster source:** `politicians.extras.ab_member_info.offices` (assembly.ab.ca's structured member records, ingested by `enrich-ab-mlas`) — found by exploring the politicians table's extras column rather than scraping Wikipedia or mining Hansard text. Already-ingested data, just needed querying. **Outcomes:** `The Deputy Speaker` 85.4% → 100% (19,363/19,363); `The Deputy Chair` 0% → 100% (11,631/11,631); +14,459 speeches resolved this round on top of phase 1a's 16,535. Spot-checked 16 random rows across both offices and all parliaments 24L–31L; all 16 attribute to historically-correct holder. Two Deputy-Chair vacancy gaps acknowledged (Feehan→Sweet 2016-02-08 to 2016-03-09 ~1 month, Milliken→Reid 2022-06-27 to 2022-11-30 ~5 months — office vacant during cabinet promotion of incumbent). First-name fallback in `_find_politician_id` absorbed `Don Tannas → Donald A. Tannas`, `Richard Marz → Richard A. Marz`, `Judith Gordon → Judith D. Gordon`, `Leonard Mitzel → Leonard W. Mitzel` automatically. **Total AB attribution: 87.2% → 90.5%.** Files touched: `services/scanner/src/legislative/presiding_officer_resolver.py`. Phase 2 (Acting Speaker / Chair, ~30K rotating-role rows) **deferred** — AB Hansard lacks any in-corpus name signal for these roles and rows are mostly procedural; building external rotation scraping is high cost / low ROI.

- [x] **Tier-2 Pass 3 phase 1a — AB Deputy Speaker resolver scaffolding** (commit `daef1e4`). New `ROLE_ONLY_PRESIDING_ROSTER` (dict of province → office → list[SpeakerTerm]) parallel to `SPEAKER_ROSTER` and `DEPUTY_PRESIDING_ROSTER`, plus `ROLE_ONLY_OFFICE_MAP` (province → speaker_role → office) so the resolver can join `speeches.speaker_role` to `politician_terms.office` for each per-province configuration. Helpers `ensure_role_only_presiding_politicians` / `ensure_role_only_presiding_terms` mirror the existing Speaker shape, and `resolve_role_only_presiding` iterates over the role map for a given province, joining each role's speeches against the right office's date-windowed terms. New Click command `resolve-role-only-presiding-officers` with `--province` scope; seed is idempotent at startup. Confidence 0.85. **Roster mining:** AB's Hansard *itself* records every Deputy Speaker election verbatim, so a regex query on `speeches.text` produced ground-truth dates without external scrape. Initial AB roster: 5 entries spanning 2005-03-01 → present (Marz / Cao / Rogers / Jabbour / Pitt across 26L–31L). **Outcomes:** AB `The Deputy Speaker` attribution **0% → 85.4%** (+16,535 of 19,263 candidates resolved); 2,828 `no_term_match` were the pre-2005 (24L+25L) gap closed in phase 1b above. New `ROLE_ONLY_PRESIDING_ROSTER` (dict-of-dicts: province → office → list[SpeakerTerm]) + `ROLE_ONLY_OFFICE_MAP` (province → speaker_role → office) + `ROLE_ONLY_SOURCE_TAG = "role_only_presiding_seed"` + `ensure_role_only_presiding_politicians` / `ensure_role_only_presiding_terms` / `resolve_role_only_presiding` in `services/scanner/src/legislative/presiding_officer_resolver.py`. New `resolve-role-only-presiding-officers` Click command in `__main__.py` with `--province` scope; seed is idempotent on startup. **Roster mining trick:** AB Hansard prints every Deputy Speaker election verbatim (`<Member> is hereby declared Deputy Speaker and Chair of Committees for the Legislative Assembly of Alberta for the Nth Legislature.`), so a single regex query on `speeches.text` produced ground-truth dates without any external scrape. AB roster: 5 entries — Marz (26L, 2005-03-01) / Cao (27L, 2008-04-14) / Rogers (28L, 2012-05-23) / Jabbour (29L, 2015-06-11) / Pitt (30L+31L, 2019-05-21 → present). First-name fallback in `_find_politician_id` absorbed `Richard Marz` → DB's `Richard A. Marz` automatically. Confidence 0.85. **Outcomes:** AB `The Deputy Speaker` attribution **0% → 85.4%** (+16,535 speeches resolved across 19,263 candidates); 2,828 `no_term_match` are exactly the pre-2005 (24L+25L) acknowledged gap. Spot-checked 12 random rows across 26L-31L; all 12 attribute to historically-correct Deputy Speaker for the date window. Total AB attribution now 87.2%. **Out of scope this round:** AB Deputy Chair of Committees (mid-Leg replacements complicate roster shape); AB Acting Speaker / Chair (rotating, ~30K rows, need same-document name propagation); pre-2005 Deputy Speaker (~2,828 rows, no in-Hansard text-mining source); BC/ON/MB role-only propagation. **Files touched:** `services/scanner/src/legislative/presiding_officer_resolver.py`, `services/scanner/src/__main__.py`. **Plan + design history:** `/home/bunker-admin/.claude/plans/checking-out-the-todo-md-wise-robin.md`.

- [x] **Tier-2 Pass 2 — date-windowed disambiguation (QC vertical slice)** (no commit yet). Layered a fourth narrowing step onto `inline_presiding_resolver.py` at the existing tie-break point — when surname/first-name narrowing still leaves >1 candidate, intersect with the set of politicians who held a Speaker / Deputy-Speaker term on the speech date. New `DEPUTY_PRESIDING_ROSTER` + `DEPUTY_SOURCE_TAG = "deputy_presiding_seed"` + `ensure_deputy_presiding_politicians` / `ensure_deputy_presiding_terms` helpers in `presiding_officer_resolver.py`, mirroring the existing Speaker-roster shape. Pass-2 hits land at confidence **0.80** (vs Pass-1 0.85) via a separate UPDATE bucket. `resolve-inline-presiding-officers` Click command now seeds the deputy roster on startup (idempotent). **QC roster:** 22 entries covering 37L (2003-06-04, Sirros/Leblanc/Gendron/Cusano) → 43L (2022-11-29, Picard/Soucy/Benjamin + Sylvain Lévesque 2022-11-29 → 2024-11-06 → Sylvie D'Amours 2024-11-07 → present). Initial draft skipped Sylvain Lévesque on a Wikipedia inconsistency; resolver-side miss-bucket diagnosis (3,087 unattributed `Le Vice-Président (M. Lévesque)` rows clustered in 2023-2024) immediately surfaced the gap, news-search nailed the exact resignation date, +1 commit-message worth of roster fix. **Outcomes:** QC parens-attributed surface **67.4% → 96.7%** (+29.3 pp); +21,223 speeches resolved at confidence 0.80; total QC corpus attribution 78.0% → 79.0%. Acceptance bar was ≥90%. Spot-checked 12 random Pass-2 hits against speech dates — all 12 match the historically-correct Vice-Président (Lévesque/Gaudreault/Soucy across 41L/42L/43L windows). No regression on Pass-1 hits across ON/MB/QC. Drive-by fix: latent crash where bare-name miss path referenced `m.group(...)` after `m` was None. **Files touched:** `services/scanner/src/legislative/presiding_officer_resolver.py`, `services/scanner/src/legislative/inline_presiding_resolver.py`, `services/scanner/src/__main__.py`. **Plan + design history:** `/home/bunker-admin/.claude/plans/checking-out-the-todo-md-wise-robin.md`. **Next:** propagate the same shape to AB/BC/ON/MB/SK/NT, one province per cycle (per user pref).

### Cycle 2026-05-05

A 10-commit day spanning four distinct slices plus an end-of-day operational catchup, **followed by a separate evening shipping-arc covering Stripe live-mode deploy + the premium-reports analysis-jobs substrate**. Daytime: provincial votes follow-ups, the **complete SK Hansard + bills + presiding-resolver arc** (from `none` to live across all three coverage columns), two Tier-2 attribution lifts (cross-jurisdictional inline-name resolver + BC pre-1990 ALL-CAPS resolver), and a **politician-socials + website hygiene catchup** that re-verified all 6,114 social rows, ran the first full infrastructure-scan in 21 days, surfaced a QC/MB term-closure data bug, and seeded the missing weekly `verify-socials` + `scan` schedules. Net +73K speeches attributed across the corpus, SK flipped to live, BC pushed from 92.5% → 94.4%; sovereignty tier-1 +5 / CA-hosted +21 since the previous 2026-04-14 scan baseline. Evening: Stripe production cutover + a generic analysis-jobs substrate that replaces the per-politician-only premium report path with a kind-discriminated table, three new search-set CTAs, a user-controllable picker, and chart-equipped report output.

- [x] **Provincial votes extractor v2** (commit `4c26b1e`) — AB/NS Speaker-truncated question-call coverage (`Contrary minded`, `I hear several Noes`, `Opposed?`) + AB corrections-aware last-match + NB heading-pattern detection (`Recorded Vote—…Defeated`). Outcomes: AB +1 defeat (was 0); NS +2 defeats (4→6); **NB 0→125 votes**. Inline-outcome consensus path disabled for NB after showing ~80% false-positive on debate prose.
- [x] **Provincial votes daily-ingest schedules** (commit `e2ffa9d`) — 8 new entries in the rollout block: BC/AB/QC/MB/NB/NL at `:50 HH`, NS at `:50 13` (new sibling of legacy 12:00–13:30 slots), ON at `:55 18` (collision-dodging). Total 10 votes-extraction schedules live.
- [x] **SK MLA roster ingester** (commit `7245dda`) — sourced from the per-legislature Hansard speaker index (`docs.legassembly.sk.ca/legdocs/Assembly/Debates/Indexes/{N}/{N}L-SP-full.html`); SK has no per-MLA stable identifier so `politicians.sk_assembly_slug` is synthesised `firstname-lastname` (migration `0044`). 30L: 61 MLAs with party + constituency + cabinet portfolio in one HTTP call. → [`docs/research/saskatchewan.md`](./docs/research/saskatchewan.md)
- [x] **SK Hansard HTML pipeline** (commit `e0a11fe`) — Word-HTML parser at `services/scanner/src/legislative/sk_hansard_parse.py`; archive-paginated discovery at `www.legassembly.sk.ca/legislative-business/archive/`; per-sitting `(parl, sess, ymd)` URL pattern. First-run validation on 30L2S: 147 speeches per sitting, 92.5% attribution.
- [x] **SK Hansard schedule + 29L roster honorific fix** (commit `60de19a`) — daily 22:00 → 22:15 → 22:30 chain (mlas → hansard → presiding-resolver). 29L speaker-index entries use `Mr./Ms./Mrs./Miss` honorifics (vs 30L's bare names); regex extended to drop those from synthesised slugs.
- [x] **SK presiding-speaker resolver** (commit `c2820b3`) — extends `presiding_officer_resolver.py` `SPEAKER_ROSTER` with SK entries (Hagel/Osika/Kowalsky/Toth/D'Autremont/Tochor/Docherty/Weekes/Goudy, 23L–30L); SK Hansard parser refactored to distinguish role='speaker' / 'deputy_speaker' / 'chair' / 'deputy_chair' (was collapsing all to 'speaker', risking mis-attribution). 29L4S 0% → 73.8% attribution.
- [x] **SK Hansard PDF parser** (commit `2a8c99a`) — `sk_hansard_pdf_parse.py` mirrors the AB Hansard pattern via `pdftotext`; 29L1S–29L3S backfill (PDF-only era). Discovery dedupes HTML-over-PDF on `(parl, sess, ymd, time_of_day)`; AM/EVE supplementary PDFs treated as separate sittings. **Outcomes:** SK 154 → 360 sittings, **24,461 → 50,948 speeches**, `hansard_status` flipped `partial` → `live`.
- [x] **SK bills ingester from `progress-of-bills.pdf`** (commit `e6dcae1`) — single-PDF tabular ingester following the MB billstatus pattern but with `pdftotext -layout`. Discovery scrapes the bills page (opaque CMS slugs); per-page header detection because column widths drift across pages of the same PDF. Three-tier disambiguation incl. `--selftest` golden cases + `--dry-run` + `--all-sessions`. **Outcomes:** 367 bills across 28L3S–30L2S, 1,214 stage events, 100% sponsor FK on current session / 70% across historical, `bills_status='partial'`.
- [x] **Tier-2 inline-name presiding-officer resolver (Pass 1)** (commit `bb41d88`) — `inline_presiding_resolver.py` cross-jurisdictional. Targets `The Deputy Speaker (Mr. Bas Balkissoon)` / `Madam Clerk (Patricia Chaychuk)` / `La Vice-Présidente (Mme Gaudreault)` shapes the chamber parser missed. Daily 23:30 UTC schedule. **Outcomes:** ON +862 (parens-attributed → 99.99%), MB +1,961 (→ 91.5%), QC +16 (capped at 67% by surname-only ambiguity).
- [x] **BC pre-1990 ALL-CAPS resolver** (commit `b0b2562`) — `bc_allcaps_resolver.py` parses `MR. G.S. WALLACE (Oak Bay)` / `HON. D. BARRETT (Premier)` / `HON. MR. MICHAEL` (double-honorific). Three-tier disambiguation: lastname+date → riding hint → first-initial. ROLE-vs-RIDING parens classifier keeps `(Premier)` / `(Attorney-General)` from polluting the riding-FK path. **Outcomes:** ALL-CAPS unattributed **12,742 → 1,725** (-11,017, -86.5%); BC overall 92.5% → 94.4%.
- [x] **Stripe live-mode deploy** (operational, no commit). Switched `STRIPE_SECRET_KEY` from `sk_test_…` to `sk_live_…`, swapped in three live `price_…` IDs (the prefix gotcha bit once — Stripe's UI surfaces `prod_…` more prominently than `price_…`), registered the webhook endpoint fresh in Dashboard live mode (separate from test-mode list), corrected an initial `.ca` vs `.org` URL mismatch in the webhook config (live deploy is on `.org`; `.ca` doesn't currently resolve). Cleared a `cus_USkvnnh5JleF0W` test-mode customer ID off `users.stripe_customer_id` for both seeded users — Stripe's customer namespace is per-mode and the live API rejects test-mode customer IDs with `"No such customer"`. Verified `/account/credits` end-to-end: real $5 charge → webhook → `credit_ledger` row with `kind='stripe_purchase'`, `state='committed'`, +50 credits. Stripe Tax left **not activated** (still pending dashboard work).
- [x] **Premium-reports analysis-jobs substrate + S-tier search-set kinds** (no commit, all migration + worker + frontend). **Migration 0045** generalises `private.report_jobs` with a `kind` text column (CHECK enumerating `full_report | search_synthesis | stance_map | topic_pulse | narrative_timeline | voting_audit | compare_politicians`), an `inputs` JSONB column for kind-specific request bodies, and `politician_id`/`query` relaxed to nullable. The `report_jobs` table name is preserved; "report" is now a generic term for any paid-analysis artifact in this codebase. **API:** `POST /reports/{,/estimate}` accepts `{kind, ...inputs}` via zod discriminated union (defaults to `full_report` when `kind` omitted, for back-compat with the existing per-politician callers). New `KIND_COST_FORMULA` registry in `services/api/src/lib/reports.ts:knobsFor` — full_report stays at config-driven knobs, new kinds hard-coded with bumped cap of **500** (`search_synthesis` 5+⌈K/10⌉, `stance_map` 10+⌈K/10⌉; `topic_pulse`/`narrative_timeline`/`voting_audit`/`compare_politicians` reserved as slot-only). **Worker:** `KIND_HANDLERS` dispatcher in `services/scanner/src/reports_worker.py`; lifted existing logic into `handle_full_report` unchanged; new `handle_search_synthesis` and `handle_stance_map` share `_handle_chunk_driven_kind` skeleton via a generic `run_map_reduce_pipeline`. Concurrency bumped 2 → 4 to keep K=500 round-trip under ~3min. Aggregations (party / top speakers / year span / language) computed server-side from the chunks list and passed to the reduce prompt as JSON; updated reduce prompts instruct the model to render a `<table class="report-stats">` of the dashboard-shape stats at the top of the HTML output. Bleach allowlist extended to permit `table/thead/tbody/tr/th/td`. **Frontend:** new `AnalysisButton` + `AnalysisConfirmModal` + `useAnalysisSubmit` generic substrate; `AIFullReportButton`, `FullReportConfirmModal`, `useFullReportSubmit` slimmed to thin shims. New `<SearchSetAnalysisRow>` on `HansardSearchPage` renders Synthesize + Stance map CTAs on both the Analysis tab (default variant) and Timeline view (compact variant). New "Analyse top N" picker (preset 25/50/100/200/500 + "Other…" with a slider+number-input dialog showing live cost preview, persisted to localStorage). `/facets` extended to return `chunk_ids` of its top-N CTE plus accept a `?limit=` parameter (clamped [10, 500]); `useSpeechFacets` hoisted to the page so the dashboard tiles + the analysis CTA share one fetch. Kind chips on `/me/reports` list; kind-aware fallback titles in viewer when politician is null. End-to-end validated at K=500: 217s wall time, 169K input + 39K output tokens, ~$1.10 model cost vs 55-credit charge. **Plan + design history:** `/home/bunker-admin/.claude/plans/slick-help-me-now-merry-stonebraker.md`.
- [x] **Politician-socials + website hygiene catchup** (operational, no commit). All 6,114 socials re-verified after 8-day drift (4,957 live / 9 dead / 1,148 stuck-unknown on FB+LinkedIn anti-bot residual; 2 fresh live→dead flips on YT and Bluesky). All 2,353 sites re-scanned after 21-day drift; sovereignty distribution refreshed (tier 1 85→90, CA-hosted 1,481→1,502, US-hosted 512→496). `enrich-legislatures` + `enrich-municipal` + wikidata retry confirmed per-jurisdiction website discovery is at the structural ceiling — 0 new personal sites across all 12 provinces/territories; wikidata 452 upserts mostly metadata-refresh on known handles. Manual research filled 5 high-confidence socials (Jasraj Hallan FB+IG+LinkedIn, Kim Tyers FB, Harry Bains Twitter); territorial gap confirmed as real-world absence (NU/YT MLAs largely off-grid). **Cleanup:** 22 NU `<riding>mla.ca` parked-domain `personal_url`s nulled + 25 corresponding websites deactivated; 2 QC councillors defeated Nov 2025 (Jean Fontaine, Marianne Lambert) marked inactive; Bertrand Collins's bertrandcollins.ca recorded. **Schedules seeded:** `verify-socials` Sun 05:00 UTC + `scan` Sun 06:00 UTC weekly — closes the manual-cadence drift that motivated this entire catchup.

### Cycle 2026-05-04

- [x] **Federal `bill_events` from LEGISinfo XML** + **MB/NS `introduced_date` SQL backfill** (single bundle). New `services/scanner/src/legislative/federal_bill_events.py` parses parl.ca/LegisInfo XML — list-level XML carries inline `PassedHouseFirstReadingDateTime` / `PassedSenateFirstReadingDateTime` / `PassedHouseThirdReadingDateTime` / etc. milestone timestamps per bill, so one HTTP GET per session unlocks ~7 stage events per bill. New `ingest-federal-bill-events` and `relink-bill-introduced-dates` Click commands. **Outcomes:** federal `bill_events` 0 → **9,970** across 15 sessions (P37-S1 → P44-S1); 5,471/5,542 federal bills matched via `bills.raw->>'legisinfo_id'` exact join (98.7%); MB `introduced_date` 0 → 81; NS `introduced_date` 0 → 2,114. Daily 11:45 UTC federal events schedule slot + 07:55 UTC cross-jurisdictional intro-date relink slot. Spot-checked C-11 / C-21 / C-27 stage dates against LEGISinfo web view; matched.
- [x] **NL + AB historical `bill_events` parser fixes.** Two single-line parser bugs uncovered while investigating coverage gaps. **NL** (`nl_bills.py:_parse_date`): pre-GA 49 sessions use `"Mar. 5/2012"` (period-abbreviated month + slash separator) but parser only knew `"March 5, 2026"`; added `%b %d/%Y` + `%B %d/%Y` fallbacks. NL bills with first-reading events: **198 → 573** (+189%); total NL events 914 → 2,737. **AB** (`ab_bills.py:_AM_PM_RE`): historical pages emit `eve` (evening sittings) suffix in addition to `am`/`pm`; added `eve` to the strip regex. AB total events: **4,973 → 6,599** (+33%); 17 additional bills got first-reading dates unlocked. The bigger AB gap (10K bills with no events) is upstream sparseness, not parser-side. Both shipped 2026-05-04.
- [x] **ON pre-P42 `introduced_date` + full event-history backfill.** Single 11-line `if/elif/else` branch in `on_bills.py:extract_status_events` to handle pre-P42 sessions' 4-column `/status` table (`Date / Bill stage / Activity / Committee`) in addition to the 5-column P42+ layout (`Date / Bill stage / Event / Outcome / Committee`). Re-ran `parse-on-bill-pages` against the already-cached HTML — no upstream calls. **bills.introduced_date: 786 → 3,414 (100%)**; total ON `bill_events`: 4,755 → **17,820** (+13,065). Every ON session P36-P44 now at 100% first-reading coverage. Shipped 2026-05-04.


For context. Move items here from above as they land; trim aggressively after a couple of cycles.

### Cycle 2026-05-01 → 2026-05-03

- [x] **Search perf lift + speech-detail rebuild + graph-view exploration** — single bulk commit `1df626f` (2026-05-03). API: pg pool 10 → 30 (25/25 burst test green; was 13/25), query-embedding LRU (60s/500), result-cache LRU on the frontend, `anchor_chunk_id` re-centring, `include_count` split, `source_system` denorm. Frontend: `SpeechDetailPage` rebuild with `ExchangeSpeechRow` + `RelatedSpeechesPanel` (list + graph modes) + `QuoteShareMenu` + `MapleLeafLoader`; `SearchMapView` (radial graph of top-K, click-to-recentre via anchor-chunk) + `AnchorChunkBanner` on `/search`. Edge: nginx variable-based `proxy_pass` with Docker embedded DNS resolver (kills 30s 502s on container rebuild); `/docs/` root-anchored in `.gitignore`. Runbook trail: [`docs/runbooks/handoff-2026-04-27-semantic-explore-graph-redesign.md`](./docs/runbooks/handoff-2026-04-27-semantic-explore-graph-redesign.md), [`docs/runbooks/handoff-2026-05-02-search-efficiency-and-laptop-capacity.md`](./docs/runbooks/handoff-2026-05-02-search-efficiency-and-laptop-capacity.md).
- [x] **Provincial historical bills — workstream complete.** `--all-sessions` walker pattern propagated to ON (2026-05-01: 111 → 3,412 across P36-P44, commit `8d39fb7`), then MB + QC (2026-05-02: 81 → 1,971 across 31 MB sessions; 497 → 1,192 across 8 QC sessions via new `discover_qc_bills_html` against assnat.qc.ca historical session pages, commit `11be12d`). All three flipped `bills_status='live'`.
- [x] **Federal vote→bill linkage 10.2% → 54.7%** (2026-05-01) via new `relink-federal-votes` Click subcommand — pure-SQL UPDATE pass against `votes.raw->'openparliament_vote'->>'bill_url'`. 1,993 newly linked, 0 unmatched. Commit `3a90893`.
- [x] **ON / QC bills `introduced_date`** (2026-05-01) — ON parser-side patch derives from /status sub-page first_reading events, hits 100% on P42-P44 (786/786). QC partial via RSS-window roll-up. Older parliaments need separate work — documented gap. Commit `3a90893`.
- [x] **`bills_status` auto-derivation** (2026-04-30) — `coverage_stats.py` now flips all three legislative status columns (hansard / votes / bills) from row counts. 500-row threshold for 'live'. Commit `bdc791f`.
- [x] **Federal historical bills backfill** (2026-04-30) — `ingest-federal-bills --all-sessions` walks every federal session in `legislative_sessions`. 412 → **5,542** bills across P37-S1 → P44-S1 (openparliament.ca coverage floor at 2001). Sponsor FK rate 86.5%. Same commit as auto-derive (`bdc791f`).

### Cycle 2026-04-26 → 2026-04-30

- [x] **Federal votes extractor live** — openparliament.ca structured-JSON pipeline, ~928 44-1 divisions × ~340 MPs ≈ 300K `vote_positions` with 100% politician-FK resolution via openparliament_slug exact match (cleanest votes pipeline in the project). Federal chain extended: 11:00 bills → 11:15 Hansard → 11:30 votes. → [`docs/runbooks/handoff-2026-04-30-federal-votes.md`](./docs/runbooks/handoff-2026-04-30-federal-votes.md) (2026-04-30).
- [x] **Migration 0018 votes applied + NT votes extractor live** — schema validated against NT consensus-government data without revisions; 31 consensus votes across 2013-2026 corpus; daily 21:50 UTC schedule slot. Federal openparliament.ca + provincial regex extractions now unblocked. → [`docs/runbooks/handoff-2026-04-30-votes-layer.md`](./docs/runbooks/handoff-2026-04-30-votes-layer.md) (2026-04-30).
- [x] **NT Hansard pipeline live** — Drupal HTML scrape of ntlegislativeassembly.ca; per-turn `<a href="/meet-members/mla/{slug}">` direct slug-FK attribution (cleanest pattern of any sub-national pipeline); 19 current + 117 former MLAs ingested; 13th-20th Assembly Speaker roster seeded. Migration 0041 + `nt_mlas.py` + `nt_hansard.py` + `nt_hansard_parse.py`. Daily 21:30 UTC schedule slot. → [`docs/runbooks/handoff-2026-04-29-nt-hansard.md`](./docs/runbooks/handoff-2026-04-29-nt-hansard.md) (2026-04-29).
- [x] **BC pre-P35 historical roster + dated resolver + extended Speaker roster** — `bc_former_mlas.py` Wikipedia per-parliament wikitable parser (+160 MLAs / +359 `politician_terms` rows for P29-P34); `resolve-bc-speakers-dated` (single CTE with inline surname extraction); `SPEAKER_ROSTER["BC"]` extended P38 → P29 (+13 historical Speakers). BC corpus attribution 67.6% → 91.9% (+136K speeches; +20K Speaker-tagged rows; +186K chunks). → [`docs/runbooks/handoff-2026-04-29-bc-pre-p35-roster.md`](./docs/runbooks/handoff-2026-04-29-bc-pre-p35-roster.md) (2026-04-29).
- [x] **TEI + embed resilience layer** — device-aware TEI healthcheck (single-token /embed with `--max-time 1`, fails on CPU fallback), `restart: on-failure:5`, scanner-side preflight CPU-fallback check, exponential-backoff per batch (5 attempts 1s→16s), abort-on-5-consecutive-batch-failures guard. Closes the GPU-regression gap. (2026-04-28)
- [x] **`chunk-and-embed-speeches` daily schedule** — single combined Click command + `scanner_schedules` row at 08:00 UTC (= 02:00 Mountain), atomic chunk → embed ordering in one process. First scheduled run 2026-04-29 cleared 1,157 chunks in 13s. (2026-04-29)
- [x] **BC + QC historical-roster backfills** — QC `ingest-qc-former-mnas` (alphabet-walk of assnat.qc.ca, 2,090/2,383 bios with usable career spans, migration 0038); BC `enrich-bc-member-parliaments` (LIMS GraphQL `allMemberParliaments`, 750 (member, parliament) edges, BC terms 103→853). QC P39-P42 resolution +12-33%. (2026-04-27)
- [x] **BC pre-P38 Hansard parser** — era-branching `bc_hansard_parse.py` extension covering P29-P37 (1970-2005). +378K speeches; BC corpus ~198K → ~577K. (2026-04-27)
- [x] **ON historical MPP roster + pre-2007 Hansard parser** — propagation of date-windowed-resolver pattern to ON. (2026-04-26)

### Cycle 2026-04-23 → 2026-04-26

- [x] **Phase 1b — premium reports** — `report_jobs` table (migration 0035), `reports-worker` compose service, OpenRouter map-reduce, sanitised-HTML viewer at `/reports/<id>`, refund flow (released-hold vs compensating admin-credit), admin triage queue. First credit-spending feature.
- [x] **Phase 1c #1 — public-share + citation** — `is_public` flag (migration 0036), `/public/reports/<id>` route, citation block in viewer footer (2026-04-24).
- [x] **ON Hansard pipeline** — name-based resolution + parens-name extraction; 6 ON commands packed into the 18:00 UTC daily-ingest slot (2026-04-24).
- [x] **`/api/v1/search` filter expansion** — `min_similarity`, `parliament_number + session_number`, and `speech_type` filters on `baseFilterSchema`; new `/search/sessions` endpoint backing the cascading parliament/session dropdown; advanced-filters disclosure on `/search` (2026-04-26).
- [x] **Operational hygiene cluster** — `OPENROUTER_MODEL` → `OPENROUTER_CONTRADICTIONS_MODEL` rename with legacy fallback + boot-time deprecation warning; `scripts/backup-database.sh` hardened (`.env`-sourced knobs, file-pinned `DB_PASSWORD`, zstd default 19 → 3); BetaBadge in the site header; `docs/api.md` Search section finally written (2026-04-26).
- [x] **Stripe Tax wiring** — `STRIPE_TAX_ENABLED` env flag, `automatic_tax` + address collection + `tax_id_collection` on the Checkout Session when on, `tax_enabled` field on `/me/credits/packs`, frontend disclosure on `/account/credits`, plan-doc + `docs/operations.md` activation checklist. Default off — operator dashboard activation listed under Now (2026-04-26).
