# Timeline / direction

Where the project is going, in priority order. Last reviewed 2026-05-23 (corpus-wide naive-last-name-attribution audit + BC pre-1990 wrong-surname-collision cleanup — 11,066 BC rows reset and 10,039 re-attributed to the correct historical politicians via three-tier disambiguation. Audit query is reusable; surfaced opennorth ingest-date data-quality residue (~1,100 rows across NB/NS/MB/BC) for a separate cycle. Prior cycle 2026-05-22 NS Premier Tier-2 Pass-3 shipped via agent — new `ROLE_ONLY_NAME_PATTERNS` companion map enables resolution for jurisdictions where the parser stamps role into `speaker_name_raw` instead of `speaker_role`; bonus chunks-reconcile latent-bug fix that retroactively self-heals AB/BC/MB/SK after future timeout aborts. **NS 96.73% → 99.70%** (highest-attributed provincial Hansard). ON middle-initial-aware resolver — agent rejected the brief's destructive "merge Dave/David Cooke" premise after verifying via `ola_member_id` that they're two different MPPs serving simultaneously in different ridings; non-destructive `resolve_on_speakers_middle_initial()` pass shipped instead. **ON 92.89% → 93.11%**. QC bio re-fetch completed + dated resolver re-run bagged **+6,458 QC attributions** (**QC 84.52% → 86.67%**, +2.15pp) — operational re-run of yesterday's `_PAS_REPRESENTE_RE` parser improvements once they could reach the full 2,491 historical-MNA corpus. Prior cycle 2026-05-21 cross-province staff/group sweep + audit-driven schedule closures from Total **43,444 rows reclassified across three provinces in one cycle**: QC 27,737, AB 6,192, ON 9,515. Five provinces now have group/staff classification (MB, NL, QC, AB, ON). **Plus QC deeper-attribution ship same day:** the `resolve-qc-speakers-dated` resolver (shipped 2026-04-27, never scheduled) ran end-to-end for the first time and attributed **+10,917 rows** — QC attribution **79.1% → 82.6%** (+3.5pp); daily 16:32 UTC schedule seeded. Remaining 26,868 surname-ambiguous rows scoped this cycle: ~4,500 blocked by a roster-coverage gap for 2008-2022 retirees (Khadir / Deltell / James / Malavoy / Hivon / Weil / Doyer / Vien). Latent SSL bug in `qc_former_mnas.py` fixed + weekly schedule seeded. **Same-day 4-agent parallel batch** then closed out the QC roster gap (turned out to be a `deputes/`/`patrimoine/` `qc_assnat_id` namespace collision, not a missing URL family — all 8 named retirees backfilled, +6,090 QC attributions), rewrote ON dated-resolver to date-window + first-token disambiguator (+4,088 ON attributions, 92.46%→92.89%), shipped a QC doc-continuity resolver (hypothesis empirically invalidated, 18 rows — but daily-scheduled to catch future upstream signal), and triaged the audit MEDIUM bucket (5 new weekly Sunday schedules + 2 added to `_ONE_SHOT_VERIFIED`). **QC final: 79.1% → 84.52% across the day (+5.42pp, +16,989 attributions).** Remaining ~21K QC surname-ambiguity rows (Fournier/Bédard/Dupuis families) need per-mandate `politician_terms.constituency_id` backfill — deferred. Prior cycle 2026-05-20 MB staff-tag close-out already in Recently shipped).

This is the **what we're building next** doc. For the **why**, read [`goals.md`](./goals.md). For the **how**, read the per-feature plan under [`plans/`](./plans/) linked from each item below. When this file disagrees with the plan docs, the plan docs win — keep this one short and reorder it as priorities shift.

Three horizons:
- **Now** — in flight, expected to land in the next cycle.
- **Next** — the four stated priorities, in order. Each has a one-paragraph framing and a link to the plan doc (or a flag that no plan exists yet).
- **Later** — documented in plan docs but deliberately deferred. Not orphaned; just not the current focus.

A separate **Always-on** section covers governance, monitoring, and ingest hygiene that doesn't fit a horizon.

---

## Now (in flight)

**Empty.** AB standing-committee transcripts shipped 2026-05-19 as the first provincial-committee pipeline (`speech_type='committee'` from assembly.ab.ca PDFs, with substitute-MLA detection from cover-page roster — see *Recently shipped*). Federal committees shipped 2026-05-18.

Un-promoted follow-ups still queued: **(a) `nvidia-open` → `nvidia-driver-580` proprietary swap** — recurring Xid 62 fault class documented in `project_embed_regression.md` memory since 2026-04-15; concrete operator step (apt swap + reboot), ~15 min; blocks comfortable historical-backfill work. **(b) Historical committee backfill** — federal `--all-sessions` walker (P39-P44 openparliament floor) + AB `--all-sessions` walker (P29-P30 assembly.ab.ca PDFs); both gated on (a) because sustained embed load reliably re-triggers the fault. **(c) Next provincial-committee slice** — BC at difficulty=2 is the natural next per `research/overview.md`; needs a research handoff per CLAUDE.md convention #5. **(d) BC Committee-of-the-Whole misclassification — ✅ resolved 2026-05-19** (see *Recently shipped*).

Premium reports has three phase-1c follow-ups still ranked but they're paid-LLM features that need budget context (re-run on new evidence #3, per-section flagging #6, compare two politicians #2 — substrate ready, needs the kind handler + comparative-reduce prompt). Promote one back into *Now* when the next cycle starts on it. Plan: [`plans/premium-reports-followups.md`](./plans/premium-reports-followups.md).

---

## Next (priorities, in order)

### 1. Database — finish the corpus

The product is only as definitive as the data behind it. Database expansion stays priority one until the remaining Hansard pipelines are live and votes are modelled.

- **Remaining Hansard pipelines: PE/YT only, both deferred to *Later*.** **NU shipped 2026-05-16** (Drupal-9 PDF source at assembly.nu.ca/hansard, 59 sittings 2021-02-24 → 2024-05-31, 11,439 speeches, 5th Assembly 95.8% / 6th Assembly 96.5% non-presiding attribution, English-primary with `(interpretation)` markers for Inuktitut passages, multi-word Inuit-language surname fallback). **SK shipped 2026-05-05** (HTML + PDF + bills + presiding-resolver, 50,948 speeches across 360 sittings). **NT shipped 2026-04-29.** PE/YT both re-probed 2026-05-14 + 2026-05-16 — Cloudflare/perfdrive challenges block every server-side path. Now correctly slotted in *Later* behind browser automation / civic-transparency outreach. Status table: [`research/overview.md`](./research/overview.md).
- **Votes table — ✅ done across both shapes.** Migration 0018 applied 2026-04-30; NT consensus-government data validated the schema without revisions (31 NT consensus votes); federal openparliament.ca structured-JSON extraction live the same day with 100% politician-FK resolution via openparliament_slug. Federal historical-session backfill (39-1 → 43-2) shipped 2026-04-30 (4,481 votes / 1.45M positions), provincial regex extensions live across BC/AB/MB/QC/ON/NS/NB/NL (7,303 votes). Federal vote→bill linkage lifted from 10.2% → 54.7% on 2026-05-01 via pure-SQL re-link pass after federal-bills historical backfill.
- **Provincial historical bills — ✅ complete across all active provinces.** `--all-sessions` walker pattern propagated from federal to ON (111 → 3,412), MB (81 → 1,971), and QC (497 → 1,192) over 2026-05-01 / 2026-05-02. QC required a new HTML discoverer (`discover_qc_bills_html`) targeting assnat.qc.ca historical session pages because the donneesquebec CSV is deliberately current+previous only. Every actively-ingestible province now has `bills_status=live`; remaining gaps are NT/NU (consensus-government, naturally smaller) and PE/SK/YT (research- or WAF-gated).
- **Federal stage timelines — ✅ shipped 2026-05-04.** parl.ca/LegisInfo XML ingester closed the long-standing 0/5,542 `bill_events` gap; **9,970 events** across 15 federal sessions on first backfill, 98.7% bill-FK match via `legisinfo_id`. Bundled `relink-bill-introduced-dates` SQL pass closed MB (0 → 81) and NS (0 → 2,114) `introduced_date` denormalisation gap.
- **Committee transcripts — ✅ federal + AB shipped 2026-05-18/19.** **Federal (2026-05-18):** `ingest-federal-committees` extends `federal_hansard.py` with `fetch_committee_meetings` + `ingest_committees` (reuses `_pick_speech_type` / `_upsert_speech` / chunker / embedder verbatim — net-new code ~120 lines). P45-S1 first run: **5,471 speeches / 30 meetings / 100% MP-FK**; daily 11:20 UTC. **AB (2026-05-19, first provincial slice):** new `ingest-ab-committees` mirrors the AB chamber-Hansard PDF pipeline against `assembly.ab.ca/.../transcripts-by-type?comm={ACRONYM}` listings; 11 standing committees hardcoded; committee-restricted speaker lookup via `politician_committees` (correctly rejects witnesses) + per-meeting PDF cover-page roster parser that picks up substitute MLAs the membership table doesn't know about (78 substitutes augmented across 60 L31-S2 meetings). First-cycle outcome: **9,988 speeches / 60 meetings / 31% MLA-FK** (committee transcripts are ~40% witnesses by nature). `coverage_stats.py` gained `_committees_status` auto-derivation (5K-row threshold for 'live'); AB flipped editorial 'live' → count-derived 'partial' (back to 'live' after historical backfill). Daily 15:25 UTC. Side-shipped: `chunk-speeches --source-system / --speech-type` filters so the chunker can bypass the global spoken_at-DESC queue and make a freshly-ingested provincial slice searchable same-day. **Pending:** historical backfills (federal P39-P44 + AB P29-P30, both gated on GPU regression mitigation); next provincial slice (BC difficulty=2, research-handoff gated). Plans: `~/.claude/plans/hay-dude-i-know-bubbly-sun.md` (federal), `~/.claude/plans/okay-what-do-we-validated-mitten.md` (AB), [`plans/semantic-layer.md`](./plans/semantic-layer.md) § phase 4.
- **Historical-roster backfills — ✅ done across AB / MB / ON / QC / BC.** AB (+901, 2026-04-22), MB (+764, 2026-04-23), ON (2026-04-26), QC (~2K via assnat alphabet-walk, 2026-04-27), BC pre-P35 (+160 via Wikipedia + extended Speaker roster P29-P37, 2026-04-29). BC corpus 67.6% → 91.9% attributed in that pass. **2026-05-05 BC ALL-CAPS resolver** (commit `b0b2562`) closed the remaining `MR. G.S. WALLACE (Oak Bay)` / `HON. D. BARRETT (Premier)` shape that the dated resolver mishandled — three-tier lastname+date / riding / first-initial disambiguation; +11,017 speeches; **BC now at 94.4%**.
- **Tier-2 attribution — three passes now live.** Pass 1 (2026-05-05, commit `bb41d88`): cross-jurisdictional `inline_presiding_resolver` extracts names from parens-bearing presiding labels (`The Deputy Speaker (Mr. Bas Balkissoon)` / `Madam Clerk (Patricia Chaychuk)` / `La Vice-Présidente (Mme Gaudreault)`) and FK-matches against `politicians` — ON +862 (→ 99.99%), MB +1,961 (→ 91.5%), QC +16 (capped at 67% by surname-only ambiguity). Pass 2 (2026-05-06, no commit yet) closes the QC ceiling: fourth narrowing step at the existing tie-break — date-windowed presiding-role membership intersected with surviving candidates. New `DEPUTY_PRESIDING_ROSTER` (22 QC entries, 37L→43L) + helpers; confidence 0.80 via separate UPDATE bucket. **QC outcome: 67.4% → 96.7%** parens-attributed surface (+21,223 speeches). Pass 3 (2026-05-06 → 2026-05-14, four phases) handles role-only labels with no inline name. New `ROLE_ONLY_PRESIDING_ROSTER` + `ROLE_ONLY_OFFICE_MAP` + `resolve_role_only_presiding` + `resolve-role-only-presiding-officers` Click command. **AB phase 1a (commit `daef1e4`):** 5-entry Deputy Speaker (Marz/Cao/Rogers/Jabbour/Pitt 26L→31L) mined from in-Hansard election announcements. **AB phase 1b:** pre-2005 Deputy Speaker (Donald A. Tannas, 23L+24L+25L) + 10-entry Deputy Chair of Committees roster (Gordon/Shariff/Mitzel/Zwozdesky/Jablonski/Feehan/Sweet/Milliken/Reid/van Dijken, 24L→present) sourced from `politicians.extras.ab_member_info.offices`. **AB Deputy Speaker: 0% → 100%** (19,363 attributed); **AB Deputy Chair: 0% → 100%** (11,631 attributed). Total AB attribution 83% → 90.5%. AB Acting Speaker / Chair (~30K rotating-role rows) deferred — AB Hansard lacks in-corpus chair-handover signals. **BC Deputy Speaker (4-entry roster, 39L→43L Reid/Chouhan/Chandra Herbert/Elmore):** 0% → 83.5% (+4,267 speeches); 842 misses across 38L + 40L + Feb-2025 transition gaps. BC chamber parser tags these as bare `Deputy Speaker` (no "The"), so `ROLE_ONLY_OFFICE_MAP["BC"]` is structured per-province. **MB Deputy Speaker (single-entry, Tyler Blashko 43L):** 0% → 100% (+1,027 speeches). **SK Deputy Speaker shipped 2026-05-14** as a 4-entry roster covering 28L3S–30L (Wilson 2020-12-01 / Hargrave 2021-10-28 / Bradshaw 2023-10-10 / McLeod 2024-11-26), mined from in-Hansard `it is my duty to inform you that … has been elected … as your Deputy Speaker` announcements. SK parser emits lowercase snake `deputy_speaker`. **SK `deputy_speaker`: 0% → 100%** (+2,174 speeches); **SK overall 81.91% → 86.10%**. Drive-by: `_find_politician_id` now uses `ORDER BY is_active DESC, created_at ASC` to deterministically pick the canonical record when a politician exists as both a `legassembly.sk.ca:30L-speaker-index:*` active record and an `opennorth:saskatchewan-legislature:*` orphan stub (SK's known duplicate-politician problem). Daily 22:35 UTC SK schedule seeded; `resolve-role-only-presiding-officers` added to both `jobs_catalog.py` + `admin.ts COMMAND_CATALOG` (latent gap since 2026-05-06 original ship). ON role-only propagation (~4K rows, ON already 98.5% saturated) is marginal-lift territory; MB Tier-1 chamber-parser empty-role bucket shipped 2026-05-14 via `mb_speaker_role_relink` (closed the ~21K parser-recovery rows; 2026-05-20 follow-up extended `_ROLE_PATTERNS` to cover 2,109 parliamentary-staff shapes — Clerk / Deputy Clerk / Clerk Assistant / Sergeant-at-Arms / Deputy Sergeant-at-Arms with `Madam` / `Mr` / `Acting` honorific variants and parens-name suffixes — staff rows correctly stay `politician_id=NULL`). MB Chairperson rotating bucket (~10,909 rows) deferred per 2026-05-20 probe: MB Hansard does not carry chair-handover announcement language (only 3 of 11,170 rows contain anything resembling it), same shape as deferred AB Acting Speaker / BC Chairman families. AB/BC/MB daily-cron Pass-3 schedule rows still pending (same gap as SK was before today).
- **ON Speaker historical roster (Tier-1 Pass 0) shipped 2026-05-14** as a roster-only data change in `services/scanner/src/legislative/presiding_officer_resolver.py`. The deferred-since-ON-Hansard-shipped 11-entry historical backfill (32L → 43L, 1981 → 2025) closed the largest non-deferred Tier-1/2 attribution gap on the board: the original 2026-04-26 ON Hansard ship seeded only the 44L Skelly entry, with docstring-noted "Backfill earlier Speakers as historical-Hansard ingest expands" — never executed. Boundary dates from Wikipedia; the two intra-Legislature successions (McLean → Stockwell 1996-10-03 after the McLean resignation scandal; Curling → Brown 2005-10-11 after Curling left to be Ambassador to the Dominican Republic) chain cleanly via next-Speaker-start = previous-Speaker-end. **Outcomes:** ON `The Speaker` 46,415 unattributed → 0; **ON overall attribution 87.54% → 92.45%** (+4.91pp); 12/12 spot-checks correct spanning Turner / Edighoffer / Warner / McLean / Stockwell / Carr / Curling / Brown across 1989-2006. All 11 historical Speakers already existed in `politicians` with clean first_name/last_name from the ON Hansard parens-name parser (post-2008 inline-parens turns were already correctly attributed); no new stubs created.
- **Corrections inbox — ✅ admin review queue shipped.** Web flag-button + public POST + authed user list + `/admin/corrections` triage UI (status transitions, reviewer notes, reward-credit flow on `applied`) + submitter/admin email notifications all live; `correction_submissions.source` is `'web' | 'email' | 'api'` (migration 0020). SMTP/IMAP poller deliberately skipped — provider-side IMAP setup outweighs the web-intake path that already covers the use case.
- **Apify social-post deep enrichment — Phase 1 SHIPPED 2026-05-12** as paid user-monitoring (Twitter + Bluesky + Mastodon + Instagram, subscriber-only). See Recently shipped below. Remaining work: Phase 2 (TikTok / Threads / FB-pages actors) + governance-docs gate before v2 public-on-profile promotion. Plan: [`plans/apify-social-deep-enrichment.md`](./plans/apify-social-deep-enrichment.md) (original internal-admin proposal) + `~/.claude/plans/okay-lets-do-purring-hearth.md` (user-billed overlay that shipped).
- **Bill text for SK/PE/YT.** 11/13 sub-national + federal now have bills (**SK shipped 2026-05-05** via `progress-of-bills.pdf` parser, 367 bills across 28L3S–30L2S, `bills_status='partial'`; reaching `live` needs the yearly-span 1998-2017 PDFs which use a different table shape — separate slice). PE / YT still PDF-only / WAF-gated. Plan: bundle each remaining build with the jurisdiction's Hansard pipeline when it lands.

### 2. Chat interface

A conversational front door over the semantic-search + contradictions stack. The retrieval, ranking, and grounded-citation primitives already exist — chat is the UX wrapper that strings them into a turn-based interaction with memory of what the user has already asked.

- **No plan doc yet.** Before writing one, decide: scope (general-purpose Q&A vs. politician-scoped vs. bill-scoped), grounding discipline (every claim cites a chunk, refusal otherwise), model (OpenRouter free-tier like contradictions, or paid like phase-1b reports), and metering (free, credit-metered, or rate-limited).
- **Reuse, don't fork.** The semantic-search hybrid retrieval and the contradictions consent/model picker are the load-bearing pieces. Building a separate retrieval path for chat is the wrong default.
- **Open questions to settle in the plan doc:** session persistence (saved-searches table extension vs. new `chat_sessions`?); transcript export; how chat interacts with the credit ledger; voice-input handoff (see priority #3).

### 3. Accessibility, including voice

Civic-transparency tooling that's only usable by sighted desktop users with steady hands isn't doing its job. Two distinct workstreams:

- **Accessibility audit + remediation.** Keyboard navigation across `/search`, `/coverage`, `/postal`, the politician page, the admin shell. Screen-reader testing on the same surfaces. Color contrast pass on the map (Leaflet defaults are not great). ARIA landmarks and form labels. WCAG 2.2 AA target. No plan doc; needs one before the audit starts.
- **Voice interface.** Two layers: (a) speech-to-text for query input — STT on-device where possible, server-side fallback; (b) text-to-speech for results read-back, prioritized for low-vision and low-literacy users. Tight coupling to the chat interface in priority #2 — voice queries should land in the chat surface, not in `/search`. Unscoped; the plan doc has to come before any code.
- **Sovereignty constraint.** Whatever STT/TTS we pick has to be self-hostable or have a defensible Canadian-data path. Hosted Whisper-via-third-party is not the default. See [`plans/sovereignty-runtime-deps.md`](./plans/sovereignty-runtime-deps.md) for the precedent.

### 4. UI improvements

Smaller, mostly contained UI work. Each can ship independently.

- **`/chunk/<uuid>` detail page.** Internal view showing the full speech around a chunk + neighbouring chunks. Currently chunks deep-link out to source Hansard via `source_url + source_anchor`; the internal page would let a user expand context without leaving the site.
- **Politician biography brief.** Full-coverage report (all speeches, bills, votes per politician) as a phase-2+ premium SKU on top of phase-1b query-scoped reports. Plan: [`plans/premium-reports.md`](./plans/premium-reports.md) § v2+.
- **Topic dashboard / time series.** "Climate mentions across all legislatures over time" style. Faceted aggregations by party, jurisdiction, speaker. Goals doc lists this as the phase-2+ artifact past the v1 search box.
- **Compare politician A vs. B on topic X.** Same phase-2+ bucket. Probably reuses the chat surface from priority #2 once that exists.
- **Map polish.** Symbol legend, faster cluster-zoom transitions, and constituency-boundary year picker now that boundaries are temporal (migration 0021).

---

## Later (deferred, but documented)

Plan docs exist for these. They're not abandoned — they're parked behind the priorities above.

- **Public developer API (`/api/public/v1/*`) with three paid tiers.** Greenfield: free / dev / pro tiers, Stripe subscriptions (distinct from credit-pack one-time), per-tier rate limits, OpenAPI + Swagger UI, key provisioning at `/account/api-keys`. Plan: [`plans/public-developer-api.md`](./plans/public-developer-api.md).
- **Bulk export endpoints (Parquet / CSV) — `read:bulk` scope.** Per-jurisdiction-month presigned exports. Sits behind the dev-API v1.0 launch as v1.1. Plan: same doc as above. Structural prerequisite is **already shipped**: migration `0042` split user/payment data into a `private` schema, and `cli/sovpro db public-dump` produces a redistributable `pg_dump --schema=public` artifact. Bulk exports build on top of that dump rather than introducing a parallel scrubbing pipeline.
- **Map tiles self-hosting.** CARTO + OSM tiles currently CDN-loaded. Three options scoped: nginx raster cache, PMTiles + MapLibre GL (~25 GB Z0–Z14 Canada), OpenMapTiles container. Plan: [`plans/sovereignty-runtime-deps.md`](./plans/sovereignty-runtime-deps.md) § item 3.
- **Browser-automation (Playwright/Camoufox) for PE/YT WAF jurisdictions.** Investment only worth making if the alternative — direct outreach to the legislatures for a civic-transparency allowlist — fails. Plan: [`plans/national-expansion-scoping.md`](./plans/national-expansion-scoping.md) q5.3.
- **Openparliament.ca live-call → scheduled refresh.** `/api/v1/openparliament` currently hits `api.openparliament.ca` per request; move to a scanner refresh job and cache in DB. Outage-mitigation. Plan: [`plans/sovereignty-runtime-deps.md`](./plans/sovereignty-runtime-deps.md) § item 4.
- **Major municipal councils — speech / decision ingest.** Goals doc has this as a *later* item ([`goals.md`](./goals.md):61). First-pass research dossiers landed 2026-05-05 for the tier-1 ten (Toronto, Montréal, Vancouver, Calgary, Edmonton, Ottawa, Mississauga, Winnipeg, Québec, Hamilton); see [`research/cities/overview.md`](./research/cities/overview.md). Three system-fingerprint families: (a) **open-data API** (CKAN at Toronto / Montréal, Opendatasoft v2.1 at Vancouver — lowest-friction path; Montréal's `sous-titrage` captions ZIP archive is the strongest municipal-Hansard-equivalent in Canada); (b) **eScribe** SaaS (Calgary / Edmonton / Ottawa / Mississauga / Hamilton); (c) bespoke (Winnipeg DMIS, Québec city IIS/ASP.NET — last). **Calgary + Edmonton scaffold landed + parked 2026-05-05** — migration `0046_municipal_meetings.sql`, `services/scanner/src/legislative/{escribe,escribe_ingest,youtube_captions}.py`, 7 Click commands. Honest finding: these cities are under-published on Hansard-equivalents, eScribe AJAX is opaque to server-side callers, Calgary lacks any YouTube channel. **Revised build order:** Montréal → Toronto → Vancouver → Edmonton (YouTube only) → eScribe family (parked, gated on Socrata republish probes or Playwright) → Winnipeg / Québec. eScribe difficulty re-rated 3 → 4.
- **Politician website content ingestion → content-change monitoring.** Two-stage stretch goal. **Stage 1:** pull politicians' personal / campaign / party-lander page CONTENT (not just hostnames — those are tracked already in `websites`) into the database so we have a canonical baseline. Likely shape: `website_snapshots` table with `(website_id, fetched_at, http_status, body_text, body_html_hash)`, cadence-driven by the same fabric as `infrastructure_scans`. Useful for catching things that don't appear in Hansard — campaign-positioning shifts, policy-page edits, donation-page changes, MP newsletter swaps. **Stage 2:** enable the `change-detection` (changedetection.io) sidecar — currently commented out in `docker-compose.yml` with corrected image / volume / port wiring left in place so it's uncomment-and-go — and seed a small watch list against the top-25 personal sites + party policy pages + government press indexes. Sender → receiver wiring is half-built: the API webhook receiver (`services/api/src/routes/webhooks.ts` → `scan_changes.change_type='content_changed'`) is production-ready; the gap is the sender, and the gap behind that is having a baseline to diff against. Don't enable Stage 2 until Stage 1 lands, or you'll get diffs against an empty void.

---

## Always-on

Not horizon-bound. These need attention every cycle regardless of what else is in flight.

- **Governance docs before public launch.** Takedown / correction policy. DSAR workflow (especially before Apify social enrichment goes public). Disclaimer text on AI-generated reports. None written yet; small, but blocking.
- **Backup system completion.** Path B (parallel `pg_dump` directory format → internal NVMe + LUKS USB mirror) is documented in [`docs/operations.md`](./operations.md) § Backups but is operator-run, not automated. Pending: a single `sovpro db backup-fast` wrapper, scheduled run via `scanner_schedules`, retention / rotation of old snapshots, off-host mirror (S3 / B2), and a logged end-to-end restore drill so the wall-time floor is a measured number, not an estimate.
- **Embedding-model drift monitoring.** Re-run the eval set under [`services/embed/eval/queries/queries.jsonl`](../services/embed/eval/queries/queries.jsonl) on any model change. Qwen3-Embedding-0.6B is current; BGE-M3 wrapper kept on disk for rollback only. Plan context: [`plans/embedding-model-comparison.md`](./plans/embedding-model-comparison.md).
- **AI contradictions false-positive watch.** Feature is live and free-tier; watch for quoted-opponent and party-transition-boundary failure modes. Plan: [`plans/ai-contradictions-handoff.md`](./plans/ai-contradictions-handoff.md).
- **Coverage dashboard accuracy.** `/coverage` is the honesty surface. After every Hansard or bills ingest, run `refresh-coverage-stats` so the dashboard doesn't lie.
- **Roster hygiene.** Surfaced 2026-05-05 during the socials catchup. QC and MB enrichers leave `politician_terms.ended_at` NULL on roster refresh after elections — open-ended terms accumulate so any `pt.ended_at IS NULL` query overstates the chamber size (QC reads as 1,082 sitting MNAs vs the 125-seat reality; MB reads as 256 vs 57). Separate but adjacent: SK `politicians.is_active=true` is stuck on former MLAs (~2.5x the 61-seat chamber). Both distort coverage metrics, the `v_socials_missing` view, and would waste paid-agent credits chasing handles for ex-politicians. Fix shape: extend each per-jurisdiction enricher to soft-close terms + flip `is_active` for politicians dropped from the upstream current roster. Touches BC/AB/MB/SK/QC enrichers most heavily.
- **Documentation freshness.** When `/api/v1/search` ships, update [`docs/api.md`](./api.md). When the public dev-API ships, add a `/developers` section to [`README.md`](../README.md). When this timeline gets stale, edit it.

---

## Recently shipped (last three cycles, 2026-04-26 → 2026-05-22)

### Cycle 2026-05-23

- **Corpus-wide naive last-name attribution audit + BC pre-1990 wrong-surname-collision cleanup** (uncommitted; follow-up to cycle 2026-05-22's NS Houston cleanup which surfaced the anti-pattern). Audit query found 16,553 suspicious rows with `politician_id IS NOT NULL AND confidence=1.0 AND speaker_role IS NULL AND role-shape speaker_name_raw` whose attributed politician's `politician_terms` don't cover the speech date. **Two distinct patterns:** (1) **wrong-surname-collision** — 12,837 BC rows where 1970s-1980s ALL-CAPS speeches (`HON. MR. BENNETT` 1972-1986) were naive-attributed to the **modern** same-surname politician (Bill Bennett who started 2001) instead of the historical correct one (William Bennett 1969-1986). Cases: Bennett, Williams, MacDonald/Macdonald (different cap!), Fraser, Hartley, Hagen, Johnston, etc. (2) **opennorth ingest-date data-quality issue** — ~1,100 rows across NB/NS/MB/BC where the politician IS still actively serving but their `politician_terms.started_at` is the date opennorth's ingest first encountered them (often 2026-04-14), not their actual election date — so 2024-2026 speeches appear "pre-tenure". **5-year-gap heuristic** distinguishes: Pattern 1 has gaps of months, Pattern 2 has gaps of 30-50 years. **Cleanup (Pattern 2 only):** reset `politician_id=NULL` + cleared 13,118 chunk denorms on 11,066 BC speeches with >5-year gap. Re-ran `resolve-bc-allcaps` in 5K-row batches (full run timed out). **Outcomes:** 10,039 BC rows correctly re-attributed via three-tier lastname+date / riding / first-initial disambiguation; 1,027 honestly NULL (genuinely ambiguous). **BC 95.06% → 94.88%** — small drop, but data-quality win (10,039 attributions now correct vs wrong before). Pattern 2 zero remaining. **Pattern 1 (opennorth) flagged for separate cycle** — fix is in the opennorth ingester (set started_at to politician's actual election date from API response, not ingest date). **Audit query is reusable** — future ingests can re-run same SQL to catch new instances. **Structural lesson:** future ingest pipelines should never assign `politician_id` based on surname alone without a date-window guard. Audit signature: `confidence=1.0 + speaker_role=NULL + role-shape speaker_name_raw`. No new code shipped this slice — purely audit + cleanup.

### Cycle 2026-05-22

- **NS Premier Tier-2 Pass-3 + reusable name-pattern resolver seam + chunks-reconcile latent-bug fix** (uncommitted; agent-driven follow-up from yesterday's open list). NS had 1,904 unattributed `speaker_name_raw='THE PREMIER'` rows covering 2013-12-03 → 2021-03-31 (McNeil + Rankin Liberal era). **Surprise:** NS Hansard parser stamps the role into `speaker_name_raw`, not `speaker_role` (which is NULL for these rows). The existing `resolve_role_only_presiding` only matched on `speaker_role`, so a new match path was needed. Agent shipped `ROLE_ONLY_NAME_PATTERNS` as a companion map alongside `ROLE_ONLY_OFFICE_MAP` — **first jurisdiction using the name-pattern path; reusable seam** for any future jurisdiction whose parser stamps role into the raw-name slot. **Bonus latent-bug fix:** `resolve_role_only_presiding` had no chunks-reconcile sweep, only inner per-batch UPDATE. A timeout would leave `speech_chunks.politician_id` desynced from `speeches.politician_id` and never self-heal. Added a reconcile sweep mirroring `resolve_speakers` — fixes AB/BC/MB/SK retroactively too. Daily 13:45 UTC NS schedule seeded. **Outcomes:** 1,904 NS rows attributed (1,803 McNeil + 101 Rankin); **NS attribution 96.73% → 99.70%** (+2.97pp, now highest-attributed provincial Hansard corpus on the board). Pre-existing-bad-data: 547 NS `THE PREMIER` rows wrongly attributed to Tim Houston at confidence 1.0 (predates Pass-3) flagged for separate cleanup cycle.

- **ON politician dedupe → middle-initial-aware resolver (brief premise rejected, non-destructive alternative shipped)** (uncommitted; same cycle). The brief assumed `Dave Cooke` + `David Cooke` were the same person stored twice and instructed a merge. **Agent's investigation rejected the premise:** Dave Cooke (NDP, Windsor-Riverside 1977-1997, `ola_member_id=325`) and David R. Cooke (Liberal, Kitchener 1985-1990, `ola_member_id=477`) are two different MPPs who served simultaneously in different ridings/parties. Merging would have corrupted the dataset. Real disambiguator was already in the Hansard data: the **middle initial** (`David S. Cooke` vs `David R. Cooke`). Agent shipped `resolve_on_speakers_middle_initial()` — extracts middle initial from speech `full_name` + politician `name`, joins on exact-match + surname + date-window, attributes when exactly one contemporaneous MPP survives. Conservative: requires middle initials on BOTH sides; falls through to existing first-token resolver otherwise. One targeted enrichment: Dave Cooke's `name` field enriched `'Dave Cooke'` → `'Dave S. Cooke'` so the new pass can discriminate. **Outcomes:** +2,016 ON rows; **ON 92.89% → 93.11%** (+0.22pp); Cooke split cleanly (Dave 4,139 / David R. 422). Residue ~65K ON unattributed rows now overwhelmingly role-based parser noise (`Interjection` / `Mr. Chairman` / `Clerk at the Table` / timestamps). **Process insight:** the agent's refusal to follow a destructive brief is the right behavior; `ola_member_id` was the truth source the brief didn't account for. Similar premise-checks should be standard for any roster merge/delete.

- **NS Houston pre-Premiership mis-attribution cleanup** (uncommitted; same cycle, follow-up to Agent A's NS Premier Pass-3). Phase-1 NS ingest had attributed `speaker_name_raw='THE PREMIER'` to Houston at confidence 1.0 by naive last-name match without date-window guard. 112 of 582 Houston-at-confidence-1.0 rows fell outside his Premiership (Houston became Premier 2021-08-31; the 112 spanned 2016-2018 + pre-Aug-2021): 17/18/9 from 2016/2017/2018 + 68 from pre-Aug-2021. Single transaction reset `politician_id=NULL` + 144 chunk denorms; re-running `resolve-role-only-presiding-officers --province NS` re-attributed all 112 via yesterday's McNeil/Rankin roster (44 → McNeil, 68 → Rankin). NS attribution % unchanged at 99.70% (politician_id swap only). **Lesson:** Phase-1 NS pipeline did naive last-name attribution without date-windowing. Same anti-pattern could exist in other jurisdictions for any single-instance-surname role (Premier, party leaders) — worth a corpus-wide audit cycle.

- **QC bio re-fetch with widened-end-marker regex — shipped** (uncommitted; same cycle). Operational re-run of yesterday's parser improvements (`_PAS_REPRESENTE_RE` "Ne s'est pas représenté en YYYY" + curly-apostrophe normalisation + widen-never-narrow `ended_at` logic). Full `--bio-for-existing` walk: 2,491 historical-MNA bios re-fetched, 2,146 spans extracted; politicians table unchanged (idempotent) but many existing politician_terms had their trailing `ended_at` stamped that yesterday's partial run hadn't reached. **Re-running `resolve-qc-speakers-dated` bagged the lift: scanned=6519 updated=6458 still_unresolved=61** (99.1% hit rate on newly-reachable candidates). **QC attribution 84.52% → 86.67%** (+2.15pp, +6,458 attributions). Cross-cycle finding: Agent A's widen-never-narrow logic works correctly when re-walked with enhanced regexes; the gap was that the 2026-05-21 partial `--limit 300` run only hit small-N deputes/ rows. **No new code** — purely operational re-run of prior parser improvements.

### Cycle 2026-05-21

- **Cross-province staff/group `speech_type` classification sweep — QC + AB + ON same-day triple ship** (uncommitted; plan at `~/.claude/plans/what-should-we-work-cheerful-sun.md`). After the MB close-out on 2026-05-20, the cross-province corpus snapshot revealed that QC, AB, ON, and NS all had `staff=0` — none of those parsers were classifying parliamentary-staff or chorus turns. QC shipped first as the manual validation (27,737 rows); AB + ON then parallelized via two background `general-purpose` agents working on disjoint parser files. **Total: 43,444 rows reclassified across three provinces in one cycle.** Five provinces now have group/staff classification (MB, NL, QC, AB, ON); NS remains at `staff=0` but the empty-role bucket is dominated by `THE PREMIER` ALL-CAPS (1,904 rows) — Tier-2 Premier-roster Pass-3 work, not staff/group classification, separate cycle. Each province's parser quirks captured below.

- **QC parser staff/group `speech_type` classification** (uncommitted; plan at `~/.claude/plans/what-should-we-work-cheerful-sun.md`). Targets the largest analogous attribution-honesty gap on the board after MB shipped 2026-05-20 — at cycle start QC had 37,784 floor empty-role rows (bigger than MB's 46K ever was) and the cross-province corpus snapshot showed `staff=0` for QC/ON/NS/AB, confirming TODO line 224's call-out that "NS, AB, ON unchecked" applied to QC too. **Phase 1 investigation reframed the scope.** The QC chamber parser already detects every relevant role via `_ROLE_PATTERNS` (lines 168-190 of `services/scanner/src/legislative/qc_hansard_parse.py`) — including French chorus shapes (`Des voix` line 188 / `Une voix` line 189) and the staff shape (`Le Secrétaire` line 186). What was missing is conditional `speech_type` assignment: line 409 hardcoded `speech_type="floor"` for every emitted turn regardless of detected role. So the bug wasn't "parser-misses-the-shape" (MB's pattern) — it was "parser-knows-the-shape-but-emits-wrong-type". **What's new:** added `_GROUP_ROLES = frozenset({"Des voix", "Une voix"})` and `_STAFF_ROLES = frozenset({"Le Secrétaire"})` after `_ROLE_PATTERNS`, mirroring the role-set discrimination pattern from `services/scanner/src/legislative/nl_hansard_parse.py:227-233` (preferred over MB's `_STAFF_RAW_RE` raw-text approach because QC already extracts roles cleanly — no need for a parallel raw-text regex). Line 409 replaced with `"group" if turn_attr.role in _GROUP_ROLES else "staff" if turn_attr.role in _STAFF_ROLES else "floor"`. **Backfill:** two `UPDATE` statements inside a single transaction (no relink module needed because there's no role-tagging gap to back-populate, this is pure `speech_type` reclassification on already-role-tagged rows; future ingests pick up the new parser logic automatically). **Outcomes:** 24,608 chorus rows reclassified `floor → group`; 3,129 Secretary-General rows reclassified `floor → staff`; total 27,737 rows. QC `floor` 313,345 → 285,608; QC `group` 0 → 24,608; QC `staff` 0 → 3,129. QC attribution % unchanged at 79.1% (`politician_id` denominator unchanged — chorus + staff are by-design-NULL); honest-coverage signal corrected so search filters / coverage queries can exclude non-politician turns. Presiding-officer roles (`Le Président` 64,304 / `Le Vice-Président` 71,216 / `Le Président de commission`) and cabinet/leadership roles (`Le Premier ministre` / `Le Ministre` / `Le Leader` / `Le Whip` / `Le Chef`) deliberately stay `floor` — these are real MNA speeches where the role tag describes the speaker's function. **Out of scope (next QC cycle):** the 37,784 surname-ambiguity floor empty-role bucket — M. Couillard 3,737 / M. Fournier 2,633 / M. Bédard 2,442 etc., verified every common QC surname has 7-15 historical politicians via `politicians.last_name` query; needs date-windowed Pass-4 follow-on with relaxed thresholds. Cross-province staff/group audit (NS/AB/ON each still `staff=0`) flagged as follow-on cycles after QC validates the pattern; each parser has its own shape quirks. **Doc hygiene:** `docs/research/quebec.md` status snapshot bullet added + the natural-next-cycle pointer; TODO + this file's last-reviewed line synced. No new commands, no new schedules, no migrations.

- **AB parser staff/group `speech_type` classification** (uncommitted; same cycle as QC ship above; spawned as a parallel `general-purpose` agent). Pure QC-pattern application — AB parser at `services/scanner/src/legislative/ab_hansard.py` already detected all the relevant chorus + staff roles via `_GROUP_SPEAKER_RE` + `_ROLE_SPEAKER_RE`; nothing to add to the regex tables. **What's new:** `_GROUP_ROLES = frozenset({"Hon. Members", "Some Hon. Members", "Some Members"})` and `_STAFF_ROLES = frozenset({"The Clerk", "The Clerk Assistant", "The Sergeant-at-Arms"})` added near lines 289-308; `_normalize_role()` extended at lines 532-552 with a `Sergeant At Arms → Sergeant-at-Arms` canonicalization step so the space-separated variant routes to the same canonical role (single-keyed `_STAFF_ROLES` membership); `_upsert_speech` at lines 863-873 made `speech_type` conditional with group/staff overriding the URL-based `_pick_speech_type` fallback — a chorus turn inside a committee transcript still tags `group`, not `committee` (important because AB committees shipped 2026-05-19). **Outcomes:** 4,896 chorus rows reclassified `floor → group` (Hon. Members 4,241 + Some Hon. Members 655); 1,296 staff rows reclassified `floor → staff` (The Clerk 1,018 + The Sergeant-at-Arms 258 + The Clerk Assistant 19 + The Sergeant At Arms 1 canonicalized); **total 6,192 rows** in one transaction. AB attribution % unchanged at 89.8%. AB Acting Speaker (12,669) / The Chair (19,420) / The Chairman (1,223) rotating-role buckets explicitly out of scope. `docs/research/alberta.md` line 7 status snapshot bumped to 2026-05-21. **Surprise:** AB doesn't have a parser-stage `speech_type` at all — type is computed in the storage layer (`_upsert_speech`) via `_pick_speech_type(sitting.url)`. The conditional landed at the storage layer where URL routing already happened, so it composes correctly with the committee-vs-chamber routing.

- **Four-agent parallel batch — QC roster gap fix + ON resolver rewrite + QC doc-continuity + MEDIUM-bucket audit triage** (uncommitted; same cycle, parallel follow-up to the audit ship). Four `general-purpose` agents on disjoint scopes. **Agent A (QC recent-retiree roster) reframed the problem completely:** the 8 named 2008-2022 retirees (Khadir/Deltell/James/Malavoy/Hivon/Weil/Doyer/Vien) weren't missing from upstream — they were silently lost to a `deputes/` vs `patrimoine/anciens-parlementaires/` URL-family **namespace collision in `qc_assnat_id`**. Patrimoine pre-Confederation entries claimed low IDs (17, 25, 27, 33, 49, 91, 191, 255), the old `ON CONFLICT (qc_assnat_id) DO UPDATE` turned subsequent `deputes/` inserts for the modern retirees into no-op updates against the wrong politician. Fix: dedup keyed on `(bio_kind, qc_assnat_id)`; patrimoine rows now insert with `qc_assnat_id=NULL` + `source_id='assnat.qc.ca:former-mnas:patrimoine:{slug}-{id}'`; new Pass 1.5 demotes legacy patrimoine rows occupying slots the deputes-listing needs. Drive-by: `_PAS_REPRESENTE_RE` regex catches "Ne s'est pas représenté en YYYY" (Hivon/Khadir/Weil/Vien chose not to run again, the old four-regex set didn't have this end-marker); curly-apostrophe U+2019 → ASCII normalisation so the regex matches. **+6,090 QC attributions** from this fix (well above the 4,500 brief estimate); politicians (QC) 2,559 → 3,263. **Agent B (ON dated-resolver rewrite)** replaced the parliament-keyed CTE (90/32,297 hit rate = 0.3%) with the QC-style date-window + a first-token disambiguator inside the candidates CTE (strips honorifics from `raw->'on_hansard'->>'full_name'`, matches first token against `politicians.first_name` — handles full names, `'F.'` initials, bare single-letter initials). ON politician_terms had 100% `started_at` / 95.1% `ended_at` — clean. **ON 92.46% → 92.89%** (+0.43pp, **+4,088 speeches**). Surprise: the 23K "potential" was mostly parser noise (280 distinct unresolved surnames in ON, only 15 match any provincial politician; rest are `Chairman`/`Table`/`Interjection`/`P.m.`/etc.). **Agent C (QC document-level continuity)** built `resolve-qc-speakers-doc-continuity` (CTE: ground-truth bootstrap from already-attributed rows in the same `document_id`, count-distinct=1 gate, confidence 0.75; daily 16:33 UTC schedule). **Hypothesis invalidated empirically — only 18/26,868 rows lifted** (brief expected 10-15K). The remaining unresolved bucket is dominated by docs where the speaker is ALWAYS bare-surname with no longer-form mention to bootstrap from. Resolver still runs daily so it can pick up lift as other resolvers attribute upstream. Real lever for the 21K bucket is per-mandate `politician_terms.constituency_id` backfill (currently NULL for historical QC). **Agent D (MEDIUM-bucket triage):** `chunk-speeches` + `embed-speech-chunks` → `_ONE_SHOT_VERIFIED` (covered by `chunk-and-embed-speeches` chain); `enrich-ab-mlas`/`enrich-bc-member-parliaments`/`enrich-socials-all`/`ingest-mb-mlas`/`ingest-nt-mlas` → 5 weekly Sunday schedules at 04:42-04:52 UTC; verified `ingest-mb-mlas` ≠ alias of `ingest-manitoba-mlas` (different source families). **Audit final state: HIGH=0, MEDIUM=0, BUDGET=2, LOW=28.** **Cross-cutting outcome today:** QC attribution **79.1% → 84.52%** (+5.42pp, +16,989 attributions across the four-agent ship + earlier dated-resolver run); ON attribution **92.46% → 92.89%**. Each agent updated its own `docs/research/<prov>.md`; TODO + this file aggregate the cross-cutting view.

- **Schedule-gap audit + 7 latent schedules closed** (uncommitted; same cycle, follow-up to the three "code shipped, schedule forgotten" instances surfaced earlier today). New `scripts/audit-schedule-gaps.py` AST-parses `jobs_catalog.COMMANDS`, queries `scanner_schedules`, and buckets the 42 unscheduled commands into HIGH/MEDIUM/BUDGET/LOW via name-pattern + description heuristics; `_ONE_SHOT_VERIFIED` allowlist captures the manually-triaged "doesn't actually need scheduling" cases (`resolve-bc-allcaps` pre-1993, `resolve-bc-speakers-dated` "after pre-P35 backfill" one-shot, `resolve-acting-speakers` superseded by cross-jurisdictional `resolve-inline-presiding-officers` 2026-05-05, `ingest-ns-bills` historical Socrata backfill — daily covered by sibling RSS). **7 HIGH gaps closed:** 4 historical-roster ingesters (BC/MB/NU/ON former-* weekly Sundays 04:25-04:40 UTC, extending the QC slot from earlier today), `resolve-on-speakers-dated` daily (40 18 UTC), and `refresh-coverage-stats` + `refresh-views` daily (50/55 23 UTC, closing the TODO-line-141 Always-on items). Manual one-shot of `resolve-on-speakers-dated` only attributed 90/32,297 (the parliament-keyed mechanism is weaker than QC's date-window — schedule is for steady-state catch-up). Audit re-run post-fix: HIGH bucket empty. Pattern is now automatically catchable — next ungated Click command lands without a schedule, `python3 scripts/audit-schedule-gaps.py` surfaces it. Structural follow-up flagged: promote `jobs_catalog.COMMANDS` to a `scanner_commands` table with `schedule_required` BOOLEAN so the audit becomes a pure SQL query — out of scope.

- **QC roster-ingester SSL fix + weekly schedule + surname-ambiguity bucket scoping** (uncommitted; same cycle, immediate follow-up to the dated-resolver schedule fix below). Attempted to attack the 26,872 surname-ambiguous bucket the dated-resolver couldn't reach. **Investigation reframed the scope.** The 1,272 parens-bearing rows split into ridings (`M. Paradis (Lévis)`) and first-name witnesses (`M. Paquin (Mathieu)`); ground-truth bootstrap from already-resolved Hansard rows only catches 16/1,217 (constituency_id mostly NULL for historical QC), and the first-name witnesses aren't MNAs at all. The 25,596 bare-surname bucket splits into ~4,500 rows blocked by missing roster entries (Khadir 1,143 / Deltell 835 / James 490 / Malavoy 477 / Hivon 360 / Weil 341 / Doyer 319 / Vien 311 / Tanguay 233 / Marsan 56 — all 2008-2022 retirees, all absent from `politicians`) and ~21K genuine surname-ambiguity rows. **What was broken and is now fixed:** the `qc_former_mnas.py` ingester (shipped 2026-04-27) had two latent bugs: (1) its `httpx.AsyncClient` was missing the `verify=False` that other QC ingesters use to work around assnat.qc.ca's intermittent cert chain — every alphabet-index fetch was failing with `CERTIFICATE_VERIFY_FAILED`; (2) the ingester had never been added to `scanner_schedules`, mirroring the same latent-gap pattern as today's earlier dated-resolver fix. **What's new:** `services/scanner/src/legislative/qc_former_mnas.py:310` now passes `verify=False` to the httpx client with a comment pointing at `qc_bills.py:427-428` as the established precedent. New `scanner_schedules` row `('QC former MNAs roster refresh', 'ingest-qc-former-mnas', '{}'::jsonb, '20 4 * * 0', true)` (Sundays 04:20 UTC); mirrored into `scripts/seed-daily-ingest-schedules.sql`. **Re-run outcome:** alphabet-walk now succeeds (16 letter pages, 2,556 unique MNAs found), but 0 new MNAs inserted — all 2,556 are already in the roster. This confirms a **parser-coverage gap in the ingester**: the `/fr/membres/notices/index*.html` alphabet listings cover historical-only MNAs (pre-2008-ish); recent retirees (2008-2022) live in a separate URL family the ingester doesn't walk. **Out of scope (separate "QC recent-retiree roster" cycle):** identify the assnat.qc.ca URL pattern for 2008-2022 retirees + extend `qc_former_mnas.py` + re-run `resolve-qc-speakers-dated` to attribute the ~4,500 newly-recoverable rows. The ~21K genuine surname-ambiguity rows (Fournier/Bédard/Dupuis families) need document-level continuity OR per-mandate `politician_terms.constituency_id` backfill — both deep refactors for future cycles. No new code beyond the SSL one-liner; no migrations.

- **QC date-windowed surname resolver — first end-to-end run + daily-cron seed** (uncommitted; follows the QC staff/group ship above the same day). Routine close-out of the deferred follow-up the QC staff/group ship flagged out of scope: the 37,784 surname-ambiguity floor empty-role bucket (M. Couillard 3,737 / M. Fournier 2,633 / M. Bédard 2,442 — every common QC surname has 7-15 historical politicians). **Discovery:** the `resolve-qc-speakers-dated` Click command (`services/scanner/src/legislative/qc_hansard.py:1026-1130`) was shipped 2026-04-27 alongside the historical MNA roster as its date-windowed companion resolver, registered in both `jobs_catalog.py:283` and `admin.ts COMMAND_CATALOG:173`, but **never added to `scanner_schedules`**. The daily QC chain only ran the name-only `resolve-qc-speakers` (which skips ambiguous surnames by design); the dated resolver that picks up exactly those surnames using `politician_terms` date-windowing had been sitting on the shelf for 24 days. Same latent-gap shape as SK Pass-3 had before 2026-05-14. **Manual first run:** `docker compose run --rm scanner resolve-qc-speakers-dated` returned `scanned=10978 updated=10917 still_unresolved=61` (99.4% hit rate when there's a single candidate matching surname + date-overlap). The single-CTE pattern updates `speeches` + `speech_chunks` in one statement; chunk propagation runs only against rows whose `politician_id` actually changed (`IS DISTINCT FROM` guard) — keeps the work idempotent. **Schedule seed:** new `scanner_schedules` row `('QC date-windowed speaker resolver', 'resolve-qc-speakers-dated', '{}'::jsonb, '32 16 * * *', true)` slotted between the name-only resolver (`30 16`) and `fetch-qc-bill-introduced-dates` (`35 16`). Mirrored into `scripts/seed-daily-ingest-schedules.sql` for pgdata-rebuild reproducibility. **Outcomes:** QC attribution `79.1% → 82.6%` (+3.5pp, +10,917 speeches at confidence 0.85). Remaining `politician_id IS NULL` rows characterized: 27,737 by-design-NULL (group + staff), 26,868 surname-ambiguous (Tremblay / Couillard intersections etc.), 4 edge cases. The 26,868 ambiguous bucket needs riding-hint or first-initial disambiguation — separate deeper-cycle work mirroring how the BC pre-1990 ALL-CAPS resolver (commit `b0b2562`) extended its dated-resolver with three-tier `lastname+date / riding / first-initial` narrowing. `docs/research/quebec.md` status snapshot extended. No new code, no new migrations.

- **ON parser staff/group `speech_type` classification** (uncommitted; same cycle; spawned as the second parallel agent). Hybrid scope — ON parser at `services/scanner/src/legislative/on_hansard_parse.py` already detected the staff family (9 variants: Clerk / Clerk-at-the-Table / Deputy Clerk / Acting Clerk-at-the-Table / Clerk of the Committee / Acting Clerk of the Assembly / Acting Table Clerk / Sergeant-at-Arms / parens-name William Wong) but did NOT detect chorus shapes (`An hon. member` / `Some hon. members` / `Interjection` and case variants — speaker_role was NULL for all of them). **What's new:** `_ROLE_PATTERNS` extended at lines 197-266 with 8 new staff entries (placed BEFORE the existing generic `The Clerk` pattern so more-specific Clerk-at-the-Table family wins — order is load-bearing) + 4 new chorus entries; `_STAFF_ROLES` + `_GROUP_ROLES` frozensets added. `_build_speech` emission site at lines ~478-482 replaced hardcoded `speech_type="floor"` with the three-way conditional. **Outcomes:** 6,482 newly-tagged chorus rows reclassified empty-role-floor → `group` (Interjection 5,068 + An hon. member 1,154 + Some hon. members 260; the SQL set both speaker_role AND speech_type in one UPDATE pass per role); 3,033 already-tagged staff rows reclassified `floor → staff`; **total 9,515 rows** in one transaction. ON attribution % unchanged at 92.5%. ON Acting Speaker (16,275) / Deputy Speaker (13,285, has its own Pass-3 work) / rotating-chair family (~10K) explicitly out of scope. The parens-name variant `The Clerk-at-the-Table (Mr. Wai Lam (William) Wong)` (26 rows) handled by explicit `_STAFF_ROLES` membership rather than touching `_ROLE_WITH_PARENS_RE` (which is non-greedy on the first `(` so nested-parens names leave the role uncanonicalized) — the William Wong case is the only nested-parens form in the corpus. `docs/research/ontario.md` line 7 status snapshot bumped to 2026-05-21. **Surprise:** ON's `parse_attribution` has TWO paths through `_ROLE_PATTERNS` (role-with-parens branch at line 314 + bare-text fallback at line 394) — adding entries to the single role table covers both shapes simultaneously, no parallel `_GROUP_RAW_RE` analog needed.

### Cycle 2026-05-20

- **MB parliamentary-staff role-tagging + Chairperson defer-decision + TODO/timeline hygiene** (uncommitted; plan at `~/.claude/plans/what-should-we-work-cheerful-sun.md`). Close-out cycle on the MB chamber-parser empty-role bucket that TODO.md and this file both still listed at the stale "~46K rows" framing. Pre-work investigation surfaced two facts: (a) the parser-recovery pass the flag described as pending actually shipped 2026-05-14 via `mb_speaker_role_relink.py` (see cycle 2026-05-14 entry in TODO.md); (b) MB is currently at **89.4% attribution** (367,493/410,973) and the residual 43,480 unattributed rows are mostly explicitly-deferred-elsewhere — 29,320 `Honourable Members` chorus (by-design-NULL), 10,909 `The Chairperson` rotating (deferred), 2,109 parliamentary staff (this cycle's target), 625 metadata headings (correctly classified), 224 floor edge cases (long-tail). **What's new:** `services/scanner/src/legislative/mb_hansard_parse.py:263–278` consolidates the previous two narrow patterns (`^the\s+clerk\s*$` / `^the\s+sergeant[-\s]at[-\s]arms\s*$`) into 5 parens-aware patterns covering Clerk Assistant / Deputy Clerk / Clerk / Deputy Sergeant-at-Arms / Sergeant-at-Arms, each with `(?:the\s+|madam\s+|mr\s+)?(?:acting\s+)?` honorific prefixes and `(?:\s+\S.*)?` trailing-name tails. The trailing-name tail is what makes the patterns parens-aware indirectly: `_norm()` (line 119) replaces parens with whitespace via `re.sub(r"[^\w\s-]", " ", t)`, so `Madam Clerk (Patricia Chaychuk)` normalizes to `madam clerk patricia chaychuk` and the tail catches the name portion. Pattern order is significant — `clerk assistant` before `deputy clerk` before `clerk` so iteration in `_match_role()` picks the more-specific role first. **No new commands, no new schedules:** the existing daily `relink-mb-speaker-roles` cron at 17:40 UTC imports `_match_role` + `_clean_speaker` directly from the parser (`mb_speaker_role_relink.py:26`), so the back-population is automatic. Manual `--dry-run` then live run on 2026-05-20 confirmed clean lift. **Outcomes:** 2,109 staff rows role-tagged (`The Clerk: 1,586 / The Deputy Clerk: 335 / The Clerk Assistant: 93 / The Deputy Sergeant-at-Arms: 87 / The Sergeant-at-Arms: 8`); zero remaining NULL-role rows in `speech_type='staff'` MB bucket; MB attribution % unchanged (politician_id denominator unchanged by design — these are parliamentary staff, not MLAs, so they correctly keep `politician_id=NULL`); the "unattributed-and-untagged" honest-coverage row count dropped 2,958 → 849 (remaining 849 = 625 metadata headings + 224 floor edge cases, both intentionally out of scope). **Chairperson defer-decision captured in code:** comment block added above `ROLE_ONLY_PRESIDING_ROSTER["MB"]` in `services/scanner/src/legislative/presiding_officer_resolver.py:713` documenting a 2026-05-20 probe of MB Hansard text for chair-handover announcement language (`text ~* '(invite.*take the chair|elected.*chairperson|appointed.*chairperson)'` returned 3 of 11,170 rows; sample inspection confirmed the matches are content references, not announcement shapes — unlike the SK Deputy Speaker 2026-05-14 mining that found explicit "it is my duty to inform you that … has been elected" patterns). Same shape as deferred AB Acting Speaker / BC Chairman families; re-evaluate if an external roster source emerges (Manitoba Assembly Journals?). **Doc hygiene:** TODO.md line 75 cross-reference to the stale "~46K rows" gap struck and replaced with the cycle-2026-05-14 ship link; this file's Tier-2 attribution bullet (~line 38) corrected to reflect the actual residual. No migrations, no API contract changes, coverage stats refreshed with no jurisdiction-status flips.

### Cycle 2026-05-19

- **AB standing-committee transcripts — first provincial-committee pipeline** (commits `ebdba93` + `67c4d05`; plan at `~/.claude/plans/okay-what-do-we-validated-mitten.md`). Extends the federal-committee shape from cycle 2026-05-18 to the provincial layer. **What's new:** `services/scanner/src/legislative/ab_committees.py` (~370 lines + 240 follow-up for substitute-MLA detection) mirrors `ab_hansard.py`'s PDF-fetch + pdftotext + speaker-line parser, swapping in `assembly.ab.ca/.../transcripts-by-type?comm={ACRONYM}&legl={L}&session={S}` listings and `docs.assembly.ab.ca/LADDAR_files\docs\committees\{acronym}\...` PDF URLs. 11 standing committees hardcoded (HS/EF/CI/CEB/FC/LO/MS/PB/PE/PA/RS). `ab_hansard.py` got a small surgical change: `_pick_speech_type` is now URL-aware (routes `/docs/committees/` → 'committee'), and `_upsert_speech` accepts optional `committee_acronym` / `committee_name` kwargs that emit an `ab_committee` raw payload shape (vs. the existing `ab_hansard` shape). Click command `ingest-ab-committees` registered in both `jobs_catalog.py` + `admin.ts COMMAND_CATALOG` (CLAUDE.md two-place rule); daily 15:25 UTC schedule between AB Hansard (15:15) and the AB presiding resolver (15:30); seeded in `scripts/seed-daily-ingest-schedules.sql`. **Speaker resolution — two layers:** (a) committee-restricted lookup joins `politician_committees` on `committee_name + ended_at` gate, restricting the candidate pool to ~10-12 members vs the 87-MLA chamber — correctly rejects witnesses (AIMCo CIO "Mr. Lord" no longer over-matches to MLA Jonathan Lord). (b) per-meeting PDF cover-page roster parser (`parse_meeting_roster` + `_ROSTER_LINE_RE` + `_ROLE_RE`) picks up substitutes the membership table doesn't know about — handles compound surnames (`Calahoo Stonehouse`), lowercase-prefix surnames (`van Dijken`, `de Jonge`), honorific-in-first (`Hon. Ric`), middle initials with period (`Scott J.`, `Peggy K.`), Acting-Chair role variant, and substitute markers both after the paren (`(UC)*`) and after the comma (`(NDP),*Acting Chair`). FK-matched attendees merge into the existing per-meeting SpeakerLookup via `augment_lookup_with_attendees`, skipping already-present IDs. **Outcomes:** **9,988 speeches across 60 L31-S2 meetings** (5 active committees: HS/LO/PA/MS/RS — others didn't meet in current session); 3,344 resolved (33.5%); 78 net-new substitute MLAs picked up via roster augmentation (553/589 cover-page attendees FK-resolved = 94%). HS 2026-04-13 spot check: Cyr (13 turns) / Petrovic (4) / Schulz (1) now correctly attributed; Lord (10) / Thompson (11) stay NULL (witness control preserved). AB `committees_status: editorial 'live' → count-derived 'partial'` (back to 'live' after historical backfill crosses 5K). 10,432 chunks built + embedded same-day via the new chunker filter (see below); end-to-end semantic search returns 44 hits on `q=AIMCo+conflict+of+interest`.

- **`chunk-speeches --source-system` / `--speech-type` filters** (same commits as above). The chunker's `WHERE c.id IS NULL ORDER BY spoken_at DESC` global queue sits behind a 481K-row backlog of perma-skipped tiny turns; a freshly-ingested provincial slice can't reach the front for days. New optional kwargs on `chunk_pending()` AND-in to the SELECT via asyncpg `$N` binds; default behaviour unchanged for the daily cron. Operator-facing log line surfaces filter values when set. `embed-speech-chunks` unchanged — when the unembedded pool is empty, a plain run only processes the chunks the targeted chunk just added. Verified end-to-end: AB committee chunk-then-embed completed in ~3 min for 2,017 chunks (first cycle) / ~14 min for 8,415 chunks (full re-run after roster augmentation), GPU healthy throughout, 0 errors / 0 retries. Reusable primitive for the next provincial-committee slice (BC/ON/QC).

- **AB committees substitute-MLA detection** (commit `67c4d05`). Follow-up that the AB ingester's first cycle left unaddressed. Without it, the committee-restricted lookup correctly rejected witnesses but ALSO missed substitute MLAs — MLAs not on a committee's regular `politician_committees` roster who attend a specific meeting in someone's place. They're listed on the PDF cover page with one-or-more asterisks (e.g. `Cyr, Scott J., Bonnyville-Cold Lake-St. Paul (UC)*`). New `parse_meeting_roster` + `AttendeeRef` + `resolve_attendees_to_politicians` + `augment_lookup_with_attendees` in `ab_committees.py` close that gap. `is_restricted` gate ensures the augmentation only fires when the committee has membership rows (committees without it already fall through to chamber-wide lookup). New `IngestCommitteeStats` counters: `attendees_parsed` / `attendees_resolved` / `attendees_augmented`. **Outcomes (full corpus re-run):** 589 roster lines parsed across 60 meetings; 553 (94%) FK-resolved; 78 net-new substitutes augmented. HS 2026-04-13 attribution went from 35 → 53 resolved (+18 turns: Cyr 13 + Petrovic 4 + Schulz 1) with zero false-positive lift on witnesses.

- **BC Committee-of-the-Whole misclassification — resolved** (uncommitted; investigation prompted by *Now* item (d)). The BC chamber parser's `SECTION_TO_TYPE` map in `services/scanner/src/legislative/bc_hansard_parse.py:289` was routing three Committee-of-the-Whole-family section headings (`Committee of the Whole` / `Committee of the Whole House` / `Committee of Supply`) to `speech_type='committee'`, leaking **34,386 BC speeches across 235 chamber-Hansard documents (2008-2026)** into the standing-committee bucket. Confirmed via `raw->'bc_hansard'->>'section'` aggregation: all 34,386 rows belong to those three sections, none are real standing-committee transcripts. C-of-W is procedurally chamber business — same MLAs, same room, just resolved into committee to debate bill clauses — so the right speech_type is `floor`. **Three-part fix:** (a) parser `SECTION_TO_TYPE` remapped, with comment block explaining the rationale and pointing at the `raw.bc_hansard.section` preservation seam that makes the change fully reversible. (b) one-shot `UPDATE speeches SET speech_type='floor' WHERE province_territory='BC' AND speech_type='committee' AND raw->'bc_hansard'->>'section' IN (...)` — exactly 34,386 rows reclassified, 0 orphans. (c) `jurisdiction_sources.committees_status` for BC manually flipped from `live` (the false-positive editorial value) to `none` — BC has neither standing-committee transcripts nor a `politician_committees` membership roster, so `none` is the honest signal until either lands. `coverage_stats._committees_status(0)` returns `None` to preserve editorial values, so the auto-derivation can't downgrade on its own — the manual flip is the correct shape. **Side effects audited:** `speech_chunks` doesn't denormalize `speech_type` (search filters via EXISTS subquery against `speeches`), so no parallel chunk-table update needed; `/api/v1/committees/meetings` listing dropped from 324 → 89 meetings (BC 235 gone, leaving AB 60 + federal 29 — exactly the genuine standing-committee transcripts); the `committee-meetings.ts` comment block warning about BC contamination is now obsolete and replaced with a "resolved 2026-05-19" note. Closes the unfinished investigation that the API route already documented at design time but never executed.

### Cycle 2026-05-18

- **Federal committee transcripts — `speech_type='committee'` via openparliament fabric** (uncommitted; plan at `~/.claude/plans/hay-dude-i-know-bubbly-sun.md`). Phase 4 of `plans/semantic-layer.md` finally landing now that votes shipped 2026-04-30 cleared the gate. Tiny PR by code volume because the infrastructure was ~90% pre-built — the existing `federal_hansard.py:_pick_speech_type()` was already routing `document_url.startswith("/committees/")` to `speech_type='committee'`; chunker, embedder, API search routes, frontend `SPEECH_TYPE_VALUES`, and `SpeechFilters.tsx` were already speech_type-agnostic. **What's new:** `fetch_committee_meetings()` paginates openparliament's `/committees/meetings/?format=json&date__gte=…&date__lte=…` endpoint, filtering `has_evidence=true` (meetings without transcripts get re-visited on subsequent runs as evidence lands upstream) and skipping in-camera by default. `ingest_committees()` mirrors `ingest()` but swaps the discovery loop; everything else (session FK, slug→politician cache, `fetch_speeches_for_document`, `_upsert_speech`) is reused verbatim. New `ingest-federal-committees` Click command in `__main__.py` mirrors `ingest-federal-hansard` Click surface (parliament/session/since/since-days/until/limit-meetings/limit-speeches/include-in-camera), uses `clamp_since_with_days()` precedence + `federal_session_bounds()` 896k-mis-tagging guardrail. Whitelist sync: `jobs_catalog.py` + `admin.ts COMMAND_CATALOG` (CLAUDE.md two-place rule). Schedule seeded daily 11:20 UTC `{since_days: 14}` between federal-hansard (11:15) and federal-votes (11:30); mirrored into `scripts/seed-daily-ingest-schedules.sql`. `coverage_stats.py` extended with `_committees_status` derivation (5,000-row threshold for 'live', 1-4999 for 'partial', None signal preserves editorial — AB's roster-based 'live' stays untouched until/unless AB committee transcripts ship; YT 'blocked' preserved). **Outcomes:** P45-S1 first run **5,471 speeches across 30 meetings, 100% MP politician-FK** via openparliament_slug (witnesses correctly land as `politician_id=NULL` rows — same shape as unresolved floor speakers). 4,896 of 5,430 chunks embedded before GPU went unhealthy mid-run; remaining 534 will pick up next daily run. **Coverage flip: federal `committees_status: 'none' → 'live'`.** `docs/research/federal.md` Committee Activity section updated to reflect shipped status. **Out of scope (deferred follow-ups):** historical backfill (`--all-sessions` walker for P39-P44 — gated on GPU regression mitigation; sustained embed load over 30+ meeting backfill reliably re-triggers Xid 62 on `nvidia-open` 580.142, surfaced again during today's smoke test); provincial committee pipelines (research-handoff gated per province; AB at difficulty=2 is the natural next); witness schema (`speaker_type` column / `witnesses` table) — deferred until an analytics use case emerges; `bill_events.committee_name` ingestion from LEGISinfo XML (column has existed since migration 0009 with UNIQUE constraint participation; `federal_bill_events.py` parses milestones but skips committee referrals — natural pair with transcripts).

- **GPU regression re-surfaced mid-smoke-test** (operational, no commit; documented in handoff memory `project_embed_regression.md`). Exact same fault class as the 2026-04-15 module-update incident: **Xid 62** (`Internal micro-controller error`) on `pid=text-embeddings` at 11:08 UTC, followed by 8× **Xid 45** (`Preemptive cleanup`) on each of TEI's CUDA channels (0x0c–0x13), then **Xid 154** explicitly transitioning the GPU into `GPU Reset Required` state. Subsequent `GspRmAlloc` calls returned `NV_ERR_RESET_REQUIRED (0x00000062)` — the GPU itself refused to allocate new channels. Driver is **`NVIDIA UNIX Open Kernel Module 580.142`** — confirming we're still on the `nvidia-open` variant suspected as the root cause in the original memory; the planned mitigation (proprietary `nvidia-driver-580` swap) hasn't been done. Trigger this time: 30-meeting committee smoke test pushed 5,471 speeches → 5,430 chunks → ~170 sequential 32-chunk batches through TEI in a tight loop. Routine daily ingest with `since_days=14` doesn't hit it because the workload is smaller increments; sustained CUDA work is the trigger. **Resilience layer worked exactly as designed:** TEI's device-aware healthcheck flipped to unhealthy on CPU fallback, scanner-side preflight refused subsequent batches, the 5-consecutive-batch-failure abort guard fired cleanly; 534 pending chunks queued for next daily run. **Mitigation recommended:** `apt remove --purge nvidia-open* … && apt install nvidia-driver-580 && reboot` — pure package swap, no physical device work, ~15-minute change. Until then, manual sustained-load runs should chunk `--limit-speeches` into ≤ ~2000-speech batches with pauses. Now flagged in *Now* until the swap lands.

### Cycle 2026-05-16

- **Daily-ingest survey — forward-incremental harness + 3 phases of stabilization** (commit `54358ba`, plan at `.claude/plans/we-want-to-work-mutable-liskov.md`). Survey of CPD's daily-cron ingest surface across three phases. **Phase 1 — daily-job stabilization (5 fixes):** `jobs_catalog.py` consolidated 7 duplicate dict keys (`ingest-{quebec,manitoba}-mlas`, `ingest-{nb,nl,ns}-former-mlas`, `relink-mb-speaker-roles`, `resolve-role-only-presiding-officers`) — the silent dict-merge had been masking arg-spec mismatches causing never-succeeded weekly roster failures. `speech_chunker.py` bumped `sync_chunk_denorm` timeout 600s → 1800s after a Tier-2 attribution burst pushed the daily run past the cap (603s on 2026-05-15). `mb_votes.py` + `nl_votes.py` broadened SQL pre-filter to match the regex classifier's `(Motion|Amendment|Bill|Question)` shape instead of `motion` only — lifts MB 47 → 413 votes (+292 bill_links), NL 2 → 14 votes. **Phase 2 — forward-incremental pass (12 pipelines):** new `services/scanner/src/legislative/_forward.py` with two helpers — `resolve_since` (DB high-water minus 14d overlap) and `clamp_since_with_days` (only narrows when explicit `--since-days` is passed; preserves the load-bearing "full session" default added after the 896k mis-labelled federal speeches incident). `extract-federal-votes` skips per-vote ballot fetches for votes whose `occurred_at` is below the cutoff — verified 1565s → 35s (~45× speedup), 22 api_calls vs ~1500. `ingest-federal-hansard` + 10 provincial Hansard ingesters (`ingest-{bc,qc,ns,on,mb,ab,nb,nl,sk,nt}-hansard`) gained `--since-days` clamp narrowing the session-bounded default — verified federal-hansard 1097s → 11s (~100×), bc-hansard 319s → 51s (~6×). `ingest-federal-bills` skips per-bill detail fetches for bills introduced before the cutoff. Live `scanner_schedules` rows updated to pass `{"since_days": 14}`; seed script (`scripts/seed-daily-ingest-schedules.sql`) updated to match so a pgdata rebuild reproduces the same state. **Phase 3a — admin `COMMAND_CATALOG` sync (18 missing commands):** `services/api/src/routes/admin.ts` gained ON Hansard chain (4 cmds), SK chain (3 cmds), `ingest-federal-bills`, 3 resolvers (ab-speakers / bc-allcaps / inline-presiding-officers), 7 escribe entries. 110/110 keys now in lockstep with scanner `jobs_catalog.py`. **Phase 3b — SK votes pipeline (first per-MLA roll-call on a consensus-shape province):** new `services/scanner/src/legislative/sk_votes.py` — Journals PDF parser, 127 PDFs discovered from `/legislative-business/journals/`, structured `YEAS / POUR — N / NAYS / CONTRE — M` grids parsed via pdftotext reading-order, surnames FK-matched against `politicians.sk_assembly_slug` with `(Constituency)` parens disambiguation. New `extract-sk-votes` Click cmd + catalog + admin entry + daily 22:50 UTC schedule. Smoke test on 29L3S (2023 Journal): 11/11 divisions parsed correctly, 9 votes / 410 vote_positions inserted, 372/410 (90.7%) MLA resolution. **Phase 3c — NU Hansard pipeline + 5th & 6th Assembly rosters:** new `services/scanner/src/legislative/nu_hansard.py` — Drupal-9 PDF source at assembly.nu.ca/hansard, ~59 PDFs back to 2021-02-24, English-primary with `(interpretation)` markers for Inuktitut passages, token-based surname index + first-name disambiguator (`P.J.` ↔ `P. J.`) + Mr.-Speaker-comma false-positive filter. New `services/scanner/src/legislative/nu_former_mlas.py` — 47 historical MLAs (22 in 5th Assembly + 25 in 6th, with 11 overlap returners + 3 by-election replacements) sourced from Wikipedia's `5th_Nunavut_Legislature` + `6th_Nunavut_Legislature` articles; fuzzier name match tolerates multi-word Inuit-language surnames (`Pitsiulaaq Brewster ⊇ Brewster`) to avoid duplicating returners. Daily `ingest-nu-hansard` at 21:15 UTC with `since_days=14`. **Full backfill outcome:** 59 sittings ingested, **11,439 speeches across 2021-02-24 → 2024-05-31**. Resolution: 5th Assembly 95.8%, 6th Assembly 96.5% non-presiding. The 537 truly-unresolved are all Committee-of-the-Whole departmental witnesses (Seeley / Audla / Mansell / etc.) — not MLAs, correctly NULL. `docs/research/saskatchewan.md` + `docs/research/nunavut.md` updated as shipped. Estimated O(1,000–3,500) historical SK votes recoverable via `--all-journals` backfill (deferred).

- **In-flight scanner fixes — 5 operator-side maintenance changes** (commit `8ae9e24`). A batch of pending working-tree changes consolidated as part of the daily-ingest survey. **(a) `on_hansard.py`:** scoped the post-ingest `speech_chunks` UPDATE to the date floor of the just-processed sittings (`idx_chunks_filters` lookup) instead of unscoped seq-scanning ~5M ON chunk rows. The unscoped form had been exceeding asyncpg `statement_timeout` on every daily run since 2026-05-03; scoped form keeps the join under the index. **Fixes the long-running `ingest-on-hansard` timeout streak (14 consecutive daily failures 5/3–5/14, succeeding 5/14+).** **(b) `qc_bills.py`:** wraps RSS fetch in try/except with `verify=False` fallback + early-return on `httpx` errors. The assnat.qc.ca SyndicationRSS-210 endpoint now serves a FortiADC WAF challenge (`waf_deny_rule_id=1190000001`) with a missing intermediate cert ahead of it; CSV path (`ingest-qc-bills`) remains the bill-discovery source of truth. Daily `ingest-qc-bills-rss` now logs a warning and exits 0 instead of failing. **(c) `federal_hansard.py`:** closes P44-S1 at 2024-12-17 (dissolution) and adds **P45-S1 starting 2025-05-26** to `FEDERAL_SESSION_DATES`. **(d) `politicians_op_backfill.py`:** broadens the missing-slug collector to also source from `vote_positions.politician_name_raw` (federal-votes shape) alongside `speeches.raw->'op_speech'->>'politician_url'` (Hansard shape) — lets a newly-sitting MP land via division ballots before they appear in any speech. **(e) `opennorth.py`:** switches the `constituency_boundaries` upsert to write `boundaries_version + effective_from` per the migration 0021 temporal schema. Previous `ON CONFLICT (constituency_id)` raised `InvalidColumnReferenceError` because the table's unique key is the `(constituency_id, boundaries_version)` pair. `docs/research/yukon.md` + `prince-edward-island.md` appended with 2026-05-14 re-probe notes — YT yukonassembly.ca + yukon.ca both Cloudflare cf-mitigated on every path; PE bills page now returns 200 OK with a 1.4KB perfdrive challenge stub rather than real content. **Net:** no server-side path to YT/PE; browser automation or civic-transparency allowlist outreach is the only avenue. Both correctly classified to *Later*.

### Cycle 2026-05-14 (fourth arc — clearing the deferred follow-ups)

- **MB 1970-fallback-date bug + remaining edge-case attribution misses** (uncommitted). Closes the two follow-ups deferred from the third arc. Six slices in one file (`mb_hansard_parse.py`) + targeted SQL backfills. **1970-fallback root cause:** `extract_sitting_date()` didn't decode HTML entities before regex-matching, so `&nbsp;` between day-of-week and month in legacy MB sittings (`Tuesday,&nbsp;June 12, 2007`) blocked the header-date regex and triggered the `or date(1970, 1, 1)` fallback. One-line fix (added `_decode_entities` call in the tag-strip path) + relaxed `_HEADER_DATE_RE` to accept optional comma after month name (one 39_2 sitting had a typo `Thursday, May, 8, 2008`). Backfill: ad-hoc Python script using cached `raw_html` on each sitting's sequence=1 row, called the corrected `extract_sitting_date`, UPDATEd `spoken_at` preserving time-of-day. **1,803 rows fixed across 10 sittings (2007-04 → 2008-09, 38L-5S / 39L-1S / 39L-2S).** **Edge-case misses:** (a) **94 surname-only FK-miss rows** (Mrs. Driedger 68 / Mrs. Stefanson 18 / Mr. Robinson 4) — all resolved once dates were correct via `resolve-mb-speakers-dated`. (b) **61 of 111 paren-only constituency-fragment rows** (Radisson / Brandon West / Premier / Minister of X / etc.) resolved via Python backfill that used `raw->'mb_hansard'->>'section'` (the heading captured by the parser before the broken speaker line) as a surname/full-name hint, looked up MB politicians by last-name with date-windowed narrowing when ambiguous. Root cause is a multi-paragraph speaker-name bug where MB Hansard splits `MLA Jelynn Dela Cruz (Radisson):` across three paragraphs — parser fix deferred (regression risk); SQL backfill captures the high-confidence subset. (c) **25 `The Acting Chairperson (Mr. Reid)` rows** bound to Daryl Reid via one-shot UPDATE — 3 MB politicians have surname Reid but only Daryl was active in 2008-2010. (d) **8 soft-hyphen `An Hon­our­able Member` rows** retagged `speech_type='group'` + parser hardened: `_clean_speaker` now strips U+00AD soft hyphens and `_GROUP_RAW_RE` accepts optional period after `Hon`. **Outcomes:** **0 rows at spoken_at='1970-01-01'** (down from 1,803); 94 surname FK-miss rows attributed; 86 paren-only rows resolved (61 section-surname + 25 Mr. Reid); 8 group-tag rows reclassified; MB attribution 89.33% → 89.37%. **Out of scope:** 50 paren-only rows whose section heading is generic ("Introduction of Guests" / "Motion presented.") — fundamentally need a multi-paragraph speaker-name parser fix that's a deeper refactor.

### Cycle 2026-05-14 (third arc — operational hygiene)

- **AB/BC/MB Pass-3 daily-cron schedule rows seeded + MB heading-misclassification cleanup** (uncommitted; plan at `~/.claude/plans/what-needs-doing-in-cozy-neumann.md`). Two small operational fixes. **Slice A:** three new `scanner_schedules` rows for `resolve-role-only-presiding-officers` at BC `50 14`, AB `50 15`, MB `50 17` — closes the schedule gap that the SK rollout in the first arc surfaced; role-only Pass-3 resolution now runs daily for every province with a roster (AB/BC/MB/SK). **Slice B:** MB chamber parser was inserting committee-transcript section headings (`Meetings`, `Committee Membership`, `Matters under Consideration`, `Officials Speaking on Record at the …`, etc.) as `speech_type='floor'` rows because `_is_heading()` structural detection missed shapes with trailing colons. New `_HEADING_TEXT_RE` in `mb_hansard_parse.py` covering 20+ named-section-heading shapes; wired into the speaker-line detection so heading-shaped text now updates `current_section` instead of opening a turn. Regex unit-tested on 30 cases (20 headings + 10 real speakers); 0 false positives. One-shot SQL UPDATE applied the same regex to existing rows: **628 MB rows reclassified `speech_type='floor' → 'metadata'`**. The `speech_type` column is plain text (no enum), so `'metadata'` is schema-safe. **Out of scope:** ~157 misc real-speaker FK-miss rows (Mrs. Driedger / Mrs. Stefanson / Mr. Robinson / constituency-fragment shapes / The Acting Chairperson paren-name miss) — separate small follow-ups; MB 1970-01-01 fallback-date bug (~1,803 rows, moderate refactor, deferred per user guidance).

### Cycle 2026-05-14 (second arc)

- **MB chamber-parser empty-role bucket — honorific-prefix regex extension + relink + Deputy Speaker pre-43L roster expansion** (uncommitted; plan at `~/.claude/plans/what-needs-doing-in-cozy-neumann.md`). Closes the largest remaining non-deferred attribution bucket on the corpus. Three slices in three files. **Slice A** added four entries to `_ROLE_PATTERNS` in `services/scanner/src/legislative/mb_hansard_parse.py` catching `Mr./Mrs./Ms./Madam` honorific-prefixed variants of `Speaker`, `Deputy Speaker`, `Chairperson`, `Deputy Chairperson`. MB pre-43L Hansard uses these instead of `The`-prefixed forms; same semantic roles, routed to identical canonical strings. **Slice B** added `services/scanner/src/legislative/mb_speaker_role_relink.py` exporting `relink_mb_speaker_roles(db, *, limit, dry_run)` — imports `_match_role` + `_clean_speaker` from the parser so future regex additions auto-propagate without copy-paste maintenance. New `relink-mb-speaker-roles` Click command, whitelisted in both `jobs_catalog.py` + `admin.ts COMMAND_CATALOG`. Run reported `scanned=23,597 role_assigned=20,452` with `{The Deputy Speaker: 10,624, The Chairperson: 9,461, The Speaker: 278, The Deputy Chair: 89}` — within 1% of plan estimates. **Slice C** expanded `ROLE_ONLY_PRESIDING_ROSTER["MB"]["Deputy Speaker"]` from 1 entry (Tyler Blashko 43L only) to 6 entries spanning 37L–43L (Conrad Santos / Marilyn Brick / Thomas Nevakshonoff / Doyle Piwniuk / Andrew Micklefield / Tyler Blashko). Names + per-Legislature windows mined from Pass-1-attributed inline-paren rows already in the corpus — free in-corpus source of truth identical-in-spirit to the AB phase-1a Hansard-text-mining trick from cycle 2026-05-06. All 6 candidates pre-existed in `politicians`; no new stubs. **Outcomes:** MB overall attribution **86.67% → 89.33%** (+2.66pp, +10,985 speeches at confidence 0.85). Pass-3 attributed all 11,688 Deputy Speaker rows (5000 Micklefield + 2207 Piwniuk + 1326 Brick + 1076 Blashko + 752 Santos + 342 Nevakshonoff). 12/12 spot-checks correct, including one self-referential Micklefield turn confirming his Deputy Speaker tenure verbatim. ~10,971 Chairperson/Deputy Chair rows newly role-tagged but unattributed — sets up future rotating-role Pass-3 work cleanly; attribution itself deferred consistent with AB Acting Speaker / BC Chairman family. Schedule seeded: daily 17:40 UTC `MB speaker-role relink` in `scanner_schedules` between MB Hansard ingest and the MB presiding resolver chain. coverage_stats refreshed. **Out of scope:** 276 `The Speaker` + 12 `The Deputy Speaker` rows with `spoken_at=1970-01-01` corpus-fallback dates (data-quality issue with MB Hansard date extraction, separate work); Chairperson/Deputy Chair rotating-role attribution; 860 heading-misclassification rows; 187 misc edge cases.

### Cycle 2026-05-14 (first arc)

- **ON Speaker historical roster backfill (Tier-1 Pass 0 expansion) + SK Deputy Speaker (Tier-2 Pass 3 propagation)** (uncommitted; plan at `~/.claude/plans/what-needs-doing-in-cozy-neumann.md`). The first attribution-lift cycle since the new `Now`-is-empty horizon-pick moment. Two slices in the same file (`services/scanner/src/legislative/presiding_officer_resolver.py`), both roster-only data changes. **Slice A — ON Speaker historical (Pass 0):** extended `SPEAKER_ROSTER["ON"]` from a single 44L Skelly entry to 12 entries spanning 32L–44L (1981–present): Turner / Edighoffer / Warner / McLean / Stockwell / Carr / Curling / Brown / Peters / Levac / Arnott / Skelly. **ON `The Speaker` 46,415 unattributed → 0; ON overall 87.54% → 92.45%** (+4.91pp). All 11 historical Speakers already existed in `politicians` with clean first_name/last_name; no new stubs created. **Slice B — SK Deputy Speaker (Pass 3 propagation):** added SK to `ROLE_ONLY_PRESIDING_ROSTER` + `ROLE_ONLY_OFFICE_MAP["SK"]["deputy_speaker"]` (lowercase snake — the SK parser emits `deputy_speaker`). 4-entry roster covering 28L3S–30L mined directly from in-Hansard election announcements: Wilson 2020-12-01, Hargrave 2021-10-28, Bradshaw 2023-10-10, McLeod 2024-11-26. **SK `deputy_speaker` 2,174 unattributed → 0; SK overall 81.91% → 86.10%** (+4.19pp). **Drive-by fix:** `_find_politician_id` `LIMIT 1` lookup was nondeterministic — added `ORDER BY is_active DESC, created_at ASC` to both the exact-match and surname-fallback queries. SK has a known duplicate-politician problem (the same MLA appears with both `legassembly.sk.ca:30L-speaker-index:slug` and `opennorth:saskatchewan-legislature:slug` source_ids) — Blaine McLeod would otherwise have bound to the orphan stub. Defensive change benefits every roster, not just SK. **Whitelist sync:** `resolve-role-only-presiding-officers` added to both `services/scanner/src/jobs_catalog.py` and `services/api/src/routes/admin.ts COMMAND_CATALOG` per CLAUDE.md § Admin panel two-place rule — the command existed in `__main__.py` but neither catalog since the Pass-3 phase-1a ship in cycle 2026-05-06 (schedule gap was latent for AB/BC/MB too; only SK's daily 22:35 UTC schedule row was seeded this cycle since its primary Speaker resolver was the chain anchor that triggered the audit). **Net deliverable**: +48,589 speeches attributed (46,415 ON + 2,174 SK) at confidence 0.85–0.9; ~37K speech_chunks updated. `refresh-coverage-stats` re-run so `/coverage` reflects the lift. 12/12 spot-checks correct on each slice. **Out of scope (deferred):** ON `The Acting Speaker` (16,275 rotating, same family as deferred AB Acting Speaker — no in-corpus name signal); SK `chorus` (4,841 generic noise); AB/BC/MB daily-cron Pass-3 schedule rows (resolvers exist + rosters shipped 2026-05-06, but no `scanner_schedules` entries — flagged for backfill).

### Cycle 2026-05-13

- **Headless Claude Code scheduled task — daily socials enrichment** (uncommitted). New operational pattern: a `claude -p` invocation fired by OS cron (`7 9 * * *` daily, ~09:07 local), running against the operator's Claude Code subscription rather than per-token Anthropic API. Closes the operational gap exposed when Wikidata's SPARQL service went into a multi-hour 1-req/min outage (incident `wdqs 797a132`) and the existing Tier-1 deterministic enrichers couldn't pick up the 422 inactive-roster politicians whose handles are upstream-only on Wikidata. **File map**: `scripts/scheduled-tasks/socials-weekly-enrichment.md` (version-controlled self-contained prompt, ~250 lines) → `scripts/scheduled-tasks/run-socials-weekly.sh` (bash wrapper strips frontmatter via awk, sets cron-safe PATH, invokes `claude -p --model sonnet --permission-mode acceptEdits --add-dir /home/bunker-admin/sovpro`, logs to timestamped file, captures exit code via tmpfile-out-of-redirection-block pattern) → `scripts/scheduled-tasks/send-run-summary.py` (extracts everything after the agent's `socials enrichment complete` signal line, emails to `admin@thebunkerops.ca` via project Proton SMTP creds, silent skip when SMTP not configured) → `~/.claude/scheduled-tasks/socials-weekly-enrichment/SKILL.md` (Desktop-app convention file, latent on Linux but kept for portability) → `docs/runbooks/socials-agent-logs/<timestamp>.log` (per-run log, auto-pruned >84 days) → `docs/runbooks/socials-agent-<YYYY>-W<week>.md` (per-run markdown summary written by the agent itself). **What each run does**: ranks top-25 politicians by `has_active_term × recent_speech_count × inverse(confident_platforms)`, web-searches each with a 3-call-per-politician budget (75 total cap), evidence-weights handles using a 0.3 / 0.5 / 0.7 / 0.9 rubric, inserts via `INSERT INTO public.politician_socials ... source='claude-code-agent'`, calls `verify-socials --limit 200`, writes runbook. **Audit primitive**: `source='claude-code-agent'` on every insert lets the operator SQL-revert any run with `DELETE FROM public.politician_socials WHERE source='claude-code-agent' AND created_at > now() - interval '24 hours'`; `evidence_url` column on each row carries the corroborating source page for per-row audit. **First test run** (2026-05-13): 25 politicians processed, 20 handles inserted across 12 politicians (mostly confidence 0.9, one 0.7), 19 live / 0 dead / 1 transient (Kiel Giddens Facebook blocked by FB login wall — correctly NOT marked dead), 39 of 75 search budget used, ~11 min runtime. Agent surfaced non-obvious findings (David Williams's Twitter is `@Shuswapland` — his riding, not his name; Richard Bragdon's existing Facebook row was the CPC umbrella page, not personal). CLAUDE.md gained a new "Headless Claude Code scheduled tasks" section documenting the pattern for adding more. `docs/operations.md` gains a "Daily socials enrichment" subsection with operator runbook (manual run, watch live, see what landed, pause, revert, re-send email). Email summary working: first test run emailed via Proton SMTP with subject `[CPD socials enrichment] ✓ run 2026-05-14T034758Z (exit=0)`. **What this unlocks**: subscription-quota-billed agent enrichment as an operational primitive — different billing model from `agent-missing-socials` (API-billed), same kind of work. Future headless tasks (websites enrichment? bills cross-ref? runbook-driven audits?) follow the same wrapper + email-helper + audit-tag pattern.

- **Wikidata SPARQL retry/backoff with Retry-After honoring** (uncommitted, `services/scanner/src/socials_enrichment.py`). Shipped during the WDQS outage debugging that motivated the daily-cron task above. New `_wikidata_sparql_with_retry()` helper detects 429 responses, parses `Retry-After` (seconds or HTTP-date form), waits the requested duration (capped at `ENRICH_WIKIDATA_RETRY_CAP_S=600` to bound operator wait time), retries up to `ENRICH_WIKIDATA_MAX_ATTEMPTS=5` times. Network-layer failures and 5xx get the floor wait (`ENRICH_WIKIDATA_RETRY_FLOOR_S=65` — 5s margin over WDQS's documented 1-req/min rate-limit-during-outage). Env-tunable so the operator can adjust without redeploying when WDQS rules change. The shipped retry survived its real-world stress test today (5 attempts × 600s cap = 40 min, all 429'd by Wikidata's 1000s Retry-After requests — outage still active; will self-heal on next run when WDQS recovers).

### Cycle 2026-05-12

- **Apify monitoring v3 — sponsorship discoverability + linkable attribution** (uncommitted). Same-day continuation of the v2 public-on-profile flip, driven by the explicit goal of *"get users to start building out dataset"* — acquisition mechanics rather than education. Three pieces. **Schema (migration 0053)**: `private.saved_searches` gains `scrape_attribute_url TEXT` (no CHECK; API-zod validates `https://...` and rejects malformed values with 400). Backfill `UPDATE`s the operator anchor + Pascal Paradis subscriptions to `https://canadianpoliticaldata.org` so the 41 already-attributed posts become clickable links immediately on deploy. **API**: `services/api/src/routes/socials.ts` LATERAL projects one more column (`ss.scrape_attribute_url AS funded_by_url`) alongside `funded_by`. The two-column LATERAL is still the only cross-schema bleed point in the public response. `services/api/src/routes/me.ts`: new `attributionUrlSchema` zod refinement, INSERT/UPDATE/SELECT extended to handle the column, empty-string normalizes to NULL at the handler boundary. **Frontend**: `PoliticianPostsTab.tsx` renders attribution handles as `<a href={url} rel="nofollow noopener external" target="_blank">@handle</a>` when URL present, plain text otherwise. New CTA banner above the post list — *"These posts come from paid social-media monitoring. [Set up your own →]"* (signed-in → `#monitor` hash) or *"[Sign in to fund your own →]"* (anon → `/login?from=...#posts`). Strengthened empty-state CTA with the same anon/signed-in branching. `MonitorPoliticianButton.tsx` watches `location.hash === '#monitor'` and auto-opens the config panel (then strips the hash via `history.replaceState`) — lets the Posts-tab CTAs trigger the Monitor flow without inter-component plumbing. New URL input in the attribution opt-in block (visible only when the attribution checkbox is on); thread-through to the saved-search POST/PATCH payload. **Forward-looking**: `services/scanner/src/scrape_worker.py one_shot_scrape()` now looks up the operator anchor (`[operator] One-shot attribution anchor`) by `(user_id, name)` and passes its id as `saved_search_id`, so future admin one-shots auto-attribute via the public LATERAL without manual SQL backfill. Falls back to NULL when the anchor isn't present (graceful for fresh deploys). **Docs**: new `mkdocs/docs/about/monitoring.md` explainer page covering audience / scope / pricing / attribution UX, sitting between Pricing and Privacy in the about-nav. Cross-links from `disclaimer.md` / `takedown.md` / `dsar.md` Related-policies blocks. CLAUDE.md skill updated with the v3 LATERAL projection + the URL validation + the hash-trigger + the auto-link policy. **End-to-end verified**: anonymous visit to Pascal Paradis #posts shows "Funded by [The Bunker Operations](https://canadianpoliticaldata.org)" as a clickable link with the right rel attrs; signed-in click on Posts-tab CTA opens the Monitor panel via hash; mkdocs `--strict` build clean (modulo pre-existing draft-post warnings); frontend + API TypeScript exit 0.

- **Apify monitoring v2 — public-on-profile flip + attribution opt-in** (uncommitted). Same-day promotion from the v1 subscriber-only stance once the governance docs landed (DSAR / takedown / disclaimer at `mkdocs/docs/about/*` — see entry below). Three changes. **API**: `services/api/src/routes/socials.ts GET /politicians/:id/posts` dropped the `EXISTS (SELECT 1 FROM private.scrape_jobs sj WHERE sj.user_id = $auth)` subscriber gate. It is now an anonymous-visible route — no `optionalUser` middleware, no session required. Attribution is exposed via a tight `LEFT JOIN LATERAL` against `private.scrape_jobs → private.saved_searches` that projects ONLY `ss.scrape_attribute_handle AS funded_by` (the opt-in handle from the most-recently-finished capturing scrape with a non-null handle); everything else stays inside the private schema. The LATERAL is now the sole cross-schema bleed point in the v2 query — adding more fields would leak `private.*` into the public response and break the schema-boundary invariant in CLAUDE.md § Privacy boundary. **Frontend**: new `services/frontend/src/components/PoliticianPostsTab.tsx` rendered as a *Recent posts* tab on `PoliticianDetail.tsx` between *Socials* and *Offices*. Anonymous-visible (no auth gate, no signed-in-only branch). Each row shows platform pill + date + text + engagement + source link + attribution line ("Funded by @handle" when opted in, "Scraped via paid monitoring" otherwise). Empty-state copy links to `/about/disclaimer/` + `/about/takedown/`. **Attribution opt-in**: `MonitorPoliticianButton.tsx` config panel gained a dashed-bordered section below the cost preview with a "Show me as the funder of these scrapes on <politician>'s public profile" checkbox + handle text input (max 100 chars). Defaults off / NULL; normalized to `null` server-side when the checkbox is off OR the input is empty. Stored in `private.saved_searches.scrape_attribute_handle` via the already-extended POST/PATCH endpoints. **End-to-end browser-verified**: anonymous visit to Pascal Paradis `#posts` shows all 10 Twitter posts publicly with anonymous attribution; SQL-set `scrape_attribute_handle='@civicwatchca'` flipped the attribution line to "Funded by @civicwatchca" on the live page. **What this unlocks**: the data is now actually a public-record surface, not a private subscriber utility — journalists, researchers, and the general public can land on a politician's profile and see what they've been posting publicly, regardless of whether they've paid for monitoring themselves. The paying users are the funders, not the gatekeepers. Phase 2 (TikTok / Threads / FB-pages actors) and the original public-dataset-dump inclusion of `social_posts` are still on the table.

- **Apify-backed politician monitoring — paid user feature, Phase 1 end-to-end** (uncommitted; plan at `~/.claude/plans/okay-lets-do-purring-hearth.md`). Pivots the original admin-internal Apify proposal into a user-facing paid surface that overlays the existing `MonitorPoliticianButton` (saved_searches row). Three migrations: `0050_scrape_ledger_kinds.sql` extends `private.credit_ledger.kind` with `scrape_hold` / `scrape_commit` / `scrape_refund` via the 0034 drop-and-readd pattern; `0051_scrape_monitoring.sql` adds six `scrape_*` columns to `saved_searches`, creates `private.scrape_jobs` (UUID PK, FK to users/saved_searches/politicians, `scrape_kind` + `trigger_source` discriminators, `estimated_credits` snapshot + `hold_ledger_id` back-ref, `cost_usd_apify` for reconciliation, stale-claim sweep on `claimed_at`), and `public.social_posts` (UNIQUE on `(platform, post_id)` for dedup, JSONB engagement, intentionally NOT cross-schema FK'd to `scrape_jobs.id` so the private/public boundary holds); `0052_scrape_profile_cache.sql` adds `lifetime_post_count` / `posting_velocity_per_week` / `last_post_at` / `profile_metadata` JSONB / `last_profile_check_at` to `politician_socials` for the pre-flight cost calculator. **Three job kinds share one worker** (`scrape_kind` ∈ `monitoring|preflight|archive`): monitoring is recurring cadence-driven (weekly/monthly/quarterly), preflight is a cheap profile probe that caches lifetime post count, archive is a one-shot volume-priced deep history pull. **Billing fabric** mirrors the report-job hold/commit/release exactly: `holdScrapeCredits` / `commitScrapeHold` / `releaseScrapeHold` in `services/api/src/lib/credits.ts` + Python mirror in new `services/scanner/src/credits_py.py` (`hold_scrape_credits` / `commit_scrape_hold` / `release_scrape_hold` / `try_hold_scrape_credits` / `get_balance`). Each function narrows on `kind='scrape_hold'` in its WHERE clause — the SQL is the type system, so the report-flow commit can't accidentally finalize a scrape-hold and vice versa. **Pricing constants** at `services/api/src/lib/scrape-pricing.ts` (Twitter monitoring = 5 cr / Instagram = 8 / Bluesky = Mastodon = 1; preflight = 1 cr on Apify platforms / free on Bluesky+Mastodon; archive curves are tiered against lifetime_post_count with platform-specific floors). Mirror constants in `services/scanner/src/scrape_worker.py` — two-place sync that must agree byte-for-byte or estimates and debits drift. **Worker** at `services/scanner/src/scrape_worker.py` is the new `sw-scrape-worker` compose service: two-stage 60s loop. `dispatch_due_subscriptions` finds `saved_searches WHERE scrape_cadence != 'none' AND scrape_paused_reason IS NULL AND scrape_next_run_at <= now()`, enqueues one job per (sub, platform), places hold; insufficient balance → pause sub with `scrape_paused_reason='out_of_credits'` + advance `scrape_next_run_at` so dispatcher doesn't re-spin every tick. `run_queued_jobs` drains with stale-claim sweep; on success upserts posts to `social_posts` + commits hold, on failure releases hold (full refund, all-or-nothing per report-worker precedent). **Four platforms live**: Twitter via Apify `apidojo/tweet-scraper` (timeline) + `apidojo/twitter-user-scraper` (profile); Instagram via `apify/instagram-scraper` (posts) + `apify/instagram-profile-scraper` (profile); Bluesky via direct AT Protocol AppView (`public.api.bsky.app/xrpc/app.bsky.feed.getAuthorFeed` + `.actor.getProfile`, free, no auth); Mastodon via per-instance `api/v1/accounts/lookup` + `/statuses` (free, HTML-stripped). **`SCRAPE_DAILY_USD_CAP=$5`** is a platform-level circuit breaker — separate from per-user credit budgets — that summing `cost_usd_apify` across the UTC day. Some Apify actors (notably the Twitter timeline one) report `usageTotalUsd=0` synchronously and settle billing async; `estimate_apify_cost_floor()` returns `max(reported, locally_estimated)` with per-platform floors (Twitter $0.02/run = 50-tweet minimum × $0.40/1k; Instagram `result_count × $0.0015`) so the cap can actually fire. **API surface** at `services/api/src/routes/me.ts`: extended POST/PATCH `/me/saved-searches` to accept `scrape_platforms[]` / `scrape_cadence` / `scrape_attribute_handle` and compute `scrape_next_run_at` server-side; new `GET /me/scrape-cost-estimate` returns monitoring + preflight + archive cost breakdown from cached politician_socials; new `GET /me/scrape-jobs` paginated history; new `POST /me/scrape-jobs` enqueues one-shot preflight/archive (returns 202 + job_id, frontend polls `/me/scrape-jobs/:id`); new `GET /me/scrape-jobs/:id` for that polling. **Visibility gate** for `social_posts` is in SQL via `EXISTS (SELECT 1 FROM private.scrape_jobs sj WHERE sj.user_id = $auth AND sj.status='succeeded')` on the new `GET /api/v1/socials/politicians/:id/posts` route — anonymous → empty list, subscriber → see posts. v2 (public-on-profile) is gated on the Always-on governance-docs item (DSAR / takedown / disclaimer) in `mkdocs/docs/`. **Frontend**: `MonitorPoliticianButton.tsx` rewritten with collapsible config panel — email alert cadence (existing free) + platform checkboxes + scrape cadence radio + live cost preview fetched from `/me/scrape-cost-estimate` + "Archive full history" button + "Run profile preview" button (each polls `/me/scrape-jobs/:id` every 2s via `useScrapeJobPoll`, capped at 60 tries). New `AccountMonitoringPage.tsx` at `/account/monitoring` with active-subscriptions list (cadence / last_run / next_run / paused_reason banner + "Stop monitoring" action) and recent-jobs table with status pills. New `cpd-monitor-panel__*` + `cpd-monitoring__*` CSS classes in `services/frontend/src/styles/user-auth.css` (~270 lines). **Click subcommands** at `services/scanner/src/__main__.py`: `run-scrape-worker` (daemon), `dispatch-scrapes` (one tick), `run-scrape-jobs --limit N` (drain queue), `scrape-politician --politician-id --platform --user-id --kind` (operator one-shot). Whitelisted in **both** `services/scanner/src/jobs_catalog.py` and `services/api/src/routes/admin.ts COMMAND_CATALOG` per CLAUDE.md § Admin panel two-place sync rule. **End-to-end smoke runs verified** against real politicians: Pascal Paradis (Twitter — 10 tweets, 7,892 lifetime cached), Alex Boulerice (Bluesky — 20 skeets, 506 lifetime cached, $0 cost), John Becker (Mastodon — 1 toot, $0 cost), Mark Carney (Instagram — 10 posts, 1,012 lifetime cached, $0.0138 real Apify cost), Anita Anand (preview button validated the preflight flow). Subscriber-only gate validated: signed-in admin sees posts, anonymous (credentials:omit) sees empty list. Refund-on-failure validated: a deliberate bad-input Twitter run flipped its 5-credit hold to `state='refunded'` and balance restored. Idempotency partial unique index on `(kind, reference_id)` caught a duplicate hold attempt. CLAUDE.md § "Scrape monitoring" + `.claude/skills/scrape-monitoring/SKILL.md` document the full subsystem with file map + gotchas. **Phase 2** (TikTok + Threads + FB-pages) deferred; **v2 public-on-profile** gated on the Always-on governance-docs item. LinkedIn + FB-posts permanently deferred (ToS / cookies, never until counsel sign-off).

- **Public developer API — phase 1e: bulk-export endpoints with `read:bulk` scope (lean MVP)** (commits `a551390` + `aca99dc` + `<phase-1e-iii>`). Three sub-phases. **Phase 1e-i (commit `a551390`):** new `services/api/src/middleware/api-scope-gate.ts` with `requireScope('read:bulk')` factory + `ALLOWED_SCOPES` allowlist. 403 with `code='insufficient_scope'` + `required_scope` + `current_scopes` + actionable message. Distinct axis from `requireTier`: tiers are billing levels (free / dev / pro), scopes are capability flags (`read:public` / `read:bulk`). A free-tier key CAN have `read:bulk` (cheap-but-allowed bulk download); a pro-tier key WITHOUT `read:bulk` can hammer search but can't download dumps. `services/api/src/routes/keys.ts` POST now accepts `scopes: string[]` in body validated against `ALLOWED_SCOPES`; defaults to `['read:public']` if omitted; `read:public` always implicitly merged in. `services/frontend/src/pages/ApiKeysPage.tsx` create form gains a Scopes fieldset with the `read:bulk` opt-in checkbox. **Phase 1e-ii (commit `aca99dc`):** new `services/api/src/routes/public/exports.ts` with two endpoints. `GET /exports/dumps` lists current full-dataset dump artifacts (filename, size_bytes, modified_at, kind in `pgcustom|sha256|manifest`); `GET /exports/dumps/:filename` streams the named file via `createReadStream` (no full-buffer load — multi-GB pgcustom files don't blow container memory) with `Content-Disposition: attachment` + `Content-Length`. Both `requireApiKey + requireScope('read:bulk')`. **SECURITY:** strict filename regex (`^cpd-public-[A-Za-z0-9_-]+\.(pgcustom|sha256|manifest\.tsv)$`) enforced via zod param schema AND re-validated inside the handler — defence in depth so a downstream refactor that swaps the schema can't accidentally open path traversal. Resolved-path guard checks the final path still lives under `config.publicDumpsDir`. `docker-compose.yml` mounts the same `/media/.../public-dumps` directory nginx serves from `/datasets/`, exposed at `/srv/datasets` inside the api container. New `PUBLIC_DUMPS_DIR` env var. ENOENT on the directory → 200 with empty list (more useful than 500 if dump cron hasn't run yet on a fresh deploy); ENOENT on a specific file → 404 with actionable message. Smoke-tested end-to-end against the live mount (2 dump sets present from May 4 + May 10): anonymous→401 (caught by `requireApiKey` before `requireScope`); free key without `read:bulk`→403 with `code='insufficient_scope', required_scope='read:bulk', current_scopes=['read:public']`; key WITH `read:bulk`→200 lists 6 files newest-first, 200 streams a 997-byte manifest with `Content-Type: text/tab-separated-values` + `Content-Disposition: attachment`. **Path traversal rejected on all four probes** (../etc/passwd, URL-encoded variant, embedded ../ in valid prefix, random unrelated filename). **Phase 1e-iii (docs sync):** new mkdocs page `developers/bulk-export.md` with curl + pg_restore guide explaining the `read:bulk` scope + same-files-as-/datasets/ relationship; updated `developers/index.md` "What's in v1.0" with the Bulk export section; updated `developers/authentication.md` with new "Scopes" section explaining the orthogonal tier vs scope axes; updated `developers/errors.md` 403 section into two flavours (`insufficient_tier` from phase 1d + `insufficient_scope` from 1e). OpenAPI now lists **11 endpoints across 5 tags** (Bulk export added). **Phase 1f deferred:** per-jurisdiction-month Parquet slicer (originally part of the v1.1 plan-doc spec) — needs a scanner snapshot generator pipeline; one cycle when prioritized. The lean-MVP value-add over `/datasets/` is auth-gated metered access via the same surface developers already use; per-jurisdiction-month slicing is the larger researcher-facing differentiator.

- **Public developer API — phase 1d: paid-tier search + free-tier search auxiliaries** (commits `f9c2a9d` + `4c365c3` + `0aaf858`). Six new endpoints under `/api/public/v1/search/*`. **Phase 1d-i (foundation):** new `services/api/src/lib/tei-semaphore.ts` wraps `p-limit` with depth-based admission control — refuses with `PublicSearchOverloadedError` when `active + pending > maxConcurrent + maxQueue` (default 2 + 6 = 8 slots). New `services/api/src/middleware/api-tier-gate.ts` exports `requireTier('dev'|'pro')` factory; 403 with `code='insufficient_tier'` + `required_tier` + `current_tier` + actionable message pointing at `/account/billing`. Anonymous → 403 with `current_tier='anonymous'`. Two new env vars (`PUBLIC_TEI_MAX_CONCURRENT`, `PUBLIC_TEI_MAX_QUEUE`). **Phase 1d-ii (commit `4c365c3`):** new `services/api/src/routes/public/search.ts` with six routes. **Pro-tier (TEI-dependent, 10000/hr):** `/search/speeches`, `/search/speeches/count`, `/search/facets` — `requireApiKey` + `requireTier('pro')` preHandlers + entire request wrapped in `withPublicTeiSlot` for the embed step. **Free-tier auxiliaries (60/hr authed, 30/hr anon):** `/search/sessions`, `/search/chunks/:id`, `/search/meta` — no TEI involvement, anonymous-friendly. **Implementation pattern: `app.inject` proxy** to the corresponding internal `/api/v1/search/*` route. Fastify's in-process request pipeline runs the internal handler without going over the network; the public surface stays a thin tier-gate + semaphore wrapper while route logic stays single-source-of-truth in `services/api/src/routes/search.ts`. Avoided ~500 lines of route-logic duplication. Smoke-tested end-to-end: anon→401 (caught by `requireApiKey` before `requireTier`); free-key→403 with `code='insufficient_tier', current_tier='free', required_tier='pro'`; pro-key→200 with 2 items + `mode='semantic'` + `X-RateLimit-Limit: 10000`; facets pro-key→200 with `analyzed_count=50, by_party[6]`. **Semaphore overflow validated:** 12 concurrent requests → exactly 8 OK + 4 503 with `Retry-After: 5` (precisely the slot budget). OpenAPI now lists **9 endpoints across 4 tags** (Coverage, Politicians, Search free, Search paid). **Phase 1d-iii (docs sync):** mkdocs `developers/index.md` updated with the nine-endpoint catalog grouped by tier; `developers/rate-limiting.md` gains "TEI semaphore on paid search" section explaining the 8-slot budget + 503 contract; `developers/errors.md` adds 403 (insufficient_tier) and clarifies the two 503 flavours (`search_overloaded` for semaphore overflow vs `embedding_service_unavailable` for TEI down); `developers/authentication.md` adds "Pro-tier-only endpoints" subsection. **The public dev-API workstream now spans phases 1a (free infra + 3 read endpoints) + 1b (Stripe subscriptions test mode) + 1c (OpenAPI + Swagger UI + mkdocs site) + 1d (paid + free search), all shipped this cycle.** Phase 1e candidate: `read:bulk` scope + Parquet/CSV per-jurisdiction-month export endpoints (was originally slated as v1.1 in the plan doc).

- **Public developer API — phase 1c: OpenAPI + Swagger UI + `/developers` mkdocs section** (commits `83316ac` + `cad22d4` + `d650bd5`). Closes the v1.0 surface — three endpoints, paid tiers, full developer documentation. Three sub-phases. **Phase 1c-i (commit `83316ac`):** added `@fastify/swagger` + `@fastify/swagger-ui` + `fastify-type-provider-zod` to `services/api/package.json`. Type provider scoped to `publicV1Routes` via Fastify encapsulation — internal `/api/v1/*` routes keep their existing manual `safeParse` pattern, no global validator/serializer change. Swagger UI mounts at `/api/public/v1/docs/`, OpenAPI 3.0.3 JSON at `/api/public/v1/docs/json` (and `/yaml`). Metadata: title "Canadian Political Data — Public API", version 1.0.0 matching the `/api/v1/search` v1.0 freeze, contact admin@thebunkerops.ca, production server URL, bearerAuth security scheme with `bearerFormat = "cpd_<env>_<random>_<checksum>"`. Three endpoints tagged Coverage (2) + Politicians (1). OPTIONS preflight catch-all hidden via `{ schema: { hide: true } }` so it doesn't pollute the spec as `/{*}`. **Response schemas deliberately omitted from route declarations** — pg returns `Date` objects for timestamptz columns + JSONB columns surface as already-parsed objects, so a strict response zod would either need `z.union([z.string(), z.date()])` on every timestamp field (high-maintenance) or `.transform()` (changes wire format). Response shapes documented in the schema's `description` string instead — Swagger UI renders the prose, mkdocs at /developers carries curl + jq examples. **Phase 1c-ii (mkdocs section):** new `mkdocs/docs/developers/` directory with four pages. **index.md** — pitch + quickstart with curl/python/js snippets + tier table + link to Swagger UI + what's in v1.0 + stability promise. **authentication.md** — token format diagram, sending bearer header in three languages, creating/rotating/revoking keys, plan-tier auto-promote interaction, security notes. **rate-limiting.md** — per-tier limit table, X-RateLimit-* response headers, 429 with Retry-After, retry strategy, caching guidance per endpoint, upgrade path. **errors.md** — full 400/401/404/429/503/500 catalog with response shapes + the three possible 401 messages + a robust error-handling pattern in python. nav entry "Developer API" added to `mkdocs/mkdocs.yml` between "Reports and credits" and "Contributors". Build verified: zero warnings on the new pages (4 strict-mode warnings on the unrelated seven-ways-to-analyze.md blog draft are pre-existing). **Phase 1c-iii (frontend nav + sync):** "Developers" link added to `services/frontend/src/components/Layout.tsx` next to the Blog link, opens `docs.canadianpoliticaldata.org/developers/` in a new tab. **Public dev-API workstream is now fully shipped end-to-end through phase 1c.** Phase 1d-and-beyond candidates: `/api/public/v1/search/*` paid-tier endpoints with TEI semaphore (depends on Phase 1b live mode), `read:bulk` scope + Parquet/CSV export endpoints (was deferred to v1.1 in the plan doc).

- **Public developer API — phase 1b: Stripe subscriptions for dev/pro tiers, test mode only** (commits `b3f95b9` + `55ffb65` + `c5c3a49`). Wires the path to flip the previously-unreachable dev (1000/hr) and pro (10000/hr) tier limits that already existed in the rate-limit middleware from phase 1a. Three sub-phases. **Phase 1b-i (foundation, commit `b3f95b9`):** migration `0049_subscriptions.sql` adds seven subscription columns to `private.users` (`current_plan` enum, `plan_status`, `stripe_subscription_id`, `plan_renews_at`, `plan_canceled_at`, `cancel_at_period_end`, `plan_updated_at`) plus `private.subscription_events` audit table with UNIQUE on `stripe_event_id` (downstream idempotency layer mirroring the credit-pack `unique partial index on (kind, reference_id)` pattern). Stripe lib gains six new helpers: `priceIdToPlan` reverse lookup, `planPriceId` forward lookup, `createSubscriptionCheckoutSession` (mode='subscription' reusing the exact Tax opts block from credit packs — recurring tax applies on renewals automatically), `createPortalSession` for Stripe Customer Portal redirects, `getSubscription` retrieve wrapper, `setSubscriptionCancelAtPeriodEnd` toggle. Two new env vars: `STRIPE_PRICE_ID_PLAN_DEV` / `_PRO`. **Phase 1b-ii (self-service + webhook + frontend):** new `/api/v1/me/subscriptions` route plugin with five endpoints (GET state, POST checkout/cancel/reactivate/portal). All require `requireUser` + CSRF; checkout 400s if user already has active subscription (use Customer Portal for plan changes). Webhook handler extended with three new event types: `customer.subscription.created` / `.updated` / `.deleted` → new `handleSubscriptionEvent` resolves user via `stripe_customer_id`, computes plan from price id, maps Stripe status to four-state enum, runs entire sync (subscription_events insert + `private.users` update + `api_keys` auto-promote/demote) inside ONE transaction so partial state is impossible. **Auto-promote model**: on subscribe (or plan upgrade), every non-revoked `api_key` for the user gets `tier=$plan`. On subscription.deleted, every non-revoked key gets `tier='free'`. `cancel_at_period_end` is NOT a demotion trigger (user keeps tier until period actually ends); `past_due` is also not a demotion trigger (Stripe handles dunning). This keeps `api_keys.tier` as the source of truth for the rate-limit middleware — no per-request join to `users.current_plan`. **Stripe SDK 22.0.2 quirk caught**: `subscription.current_period_end` moved off `Subscription` onto each `SubscriptionItem` in newer API versions; read from `sub.items.data[0].current_period_end`. Frontend `BillingPage.tsx` at `/account/billing` with status chip (Free / Active / Canceling on YYYY-MM-DD / Payment past due), plan picker (only for free users), Cancel/Reactivate/Manage Subscription buttons. AccountPage tile next to API Keys. End-to-end smoke-test (synthetic webhook simulation): subscribe sim flips user 'free'→'dev', auto-promotes the test API key 'free'→'dev', `/api/public/v1/coverage` call returns `X-RateLimit-Limit: 1000`. Cancel sim restores free + tier='free'. **Phase 1b-iii (operator walkthrough + docs sync):** new "Subscriptions activation walkthrough" in `docs/operations.md` mirroring the credit-pack walkthrough's three-phase shape (test mode + Tax + live mode); Stripe Tax for subscriptions reuses the credit-pack activation (no new dashboard work). `docs/api.md` § Public API now lists dev / pro tier limits + the subscribe → auto-promote model. Phase 1c (OpenAPI + `/developers` mkdocs) deferred. **Live-mode Stripe deploy** is a separate operator-led ship using the new walkthrough — same pattern as the 2026-05-05 credit-pack live-flip.

- **Public developer API — phase 1a debut: free-tier infra + 3 read endpoints** (commits `35b3f31` + `dc01191` + `057946f`). Promoted from *Later* to a *Now*-shaped ship in the same cycle as yesterday's v1.0 freeze that unblocked it. Three sub-phases by dependency. **Phase 1a-i (foundation, commit `35b3f31`):** migration `0048_developer_api_keys.sql` adds three tables in `private` (`api_keys` / `api_key_events` / `api_usage_daily`) per CLAUDE.md convention #8 (user-scoped credentials never bleed across the public/private boundary). Token lib `services/api/src/lib/api-key-token.ts` mints `cpd_<env>_<22-char-base62>_<6-char-checksum>` — `<env>` is `live` or `test` from `NODE_ENV` so test/live tokens can't accidentally cross-environments (Stripe-shaped lesson from 2026-05-05). HMAC-SHA256 hashing at rest with new `API_KEY_PEPPER` env var. Auth middleware `services/api/src/middleware/api-key-auth.ts` parallel of user-auth (`requireApiKey` / `optionalApiKey`); 503 if pepper unset, 401 on missing/invalid/expired/revoked/wrong-env. Throttled fire-and-forget `last_used_at` writes (LRU-bounded, ≤1/min/key). **Phase 1a-ii (self-service):** `services/api/src/routes/keys.ts` exposes `GET /me/api-keys`, `POST /me/api-keys` (full token returned ONCE), `POST /me/api-keys/:id/rotate` (new key with `rotated_from_id`; old key gets `grace_until = NOW() + 24h`), `DELETE /me/api-keys/:id` (soft-delete via `revoked_at`); audit rows on every mutation; ownership-scoped 404-not-403 cross-user discipline. Frontend `services/frontend/src/pages/ApiKeysPage.tsx` with inline create form + one-shot full-token warning banner + per-row Rotate/Revoke buttons + status chips (active / rotating / expired / revoked). New `/account/api-keys` route + AccountPage tile. **Phase 1a-iii (public surface):** `services/api/src/middleware/api-rate-limit.ts` per-tier resolver (free=60/hr per key, anon=30/hr per IP; dev/pro reserved); `onExceeded` writes `rate_limited` audit rows. `services/api/src/routes/public/index.ts` plugin with `optionalApiKey` at `onRequest` (so `req.apiKey` is populated before `@fastify/rate-limit`'s `max` evaluates — preHandler-stage hook would be too late) + permissive CORS via `onSend` hook (can't re-register `@fastify/cors`). Three endpoints: `/coverage`, `/jurisdiction-sources`, `/politicians/:id` with inline SQL mirroring the internal handlers. Smoke-tested: anon shows `X-RateLimit-Limit: 30`, authed shows `60`; hammering 65× authed produced 60 OK + 5× 429 + 5 audit rows. **Phase 1b deferred** (Stripe subscriptions for dev/pro — touches live mode); **Phase 1c deferred** (OpenAPI + Swagger UI + `/developers` mkdocs). Plan + design history: `/home/bunker-admin/.claude/plans/yo-so-what-are-wild-cosmos.md`.

- **`/api/v1/search` v1.0 contract freeze** (commits `93bcf85` + `aec15f2`). Closes the long-running `Now` item that was at "remaining is the public contract freeze" since `1df626f` shipped the perf+UX lift on 2026-05-03. Pure documentation work — server behaviour was correct and frozen-shaped already; the gaps were in `docs/api.md`. Two-phase ship. **Phase 1 (audit + fill, commit `93bcf85`):** verified the wire format with `curl`, fixed the one factual bug (`docs/api.md:71` claimed response field was `totalCapped` camelCase; actual is `capped` snake_case), documented the missing knobs (`politician_active` enum, `anchor_chunk_id` UUID with mutually-exclusive-with-`q` semantics, `include_count` literal-aware boolean, `source_system` denorm, `chunk_ids` field on `/facets`, `?limit` query param on `/facets` clamped [10, 500]), added the four endpoints that were live but undocumented (`/speeches/count` with the 10,000 HNSW-LIMIT capping behaviour explained, `/chunks/:id` with full chunk shape + politician-or-null), polished the existing `/sessions` + `/meta` + `/politician-quotes` entries. **Phase 2 (v1.0 declarations):** added a version banner blockquote at the top of `docs/api.md`; added a "Stability & Versioning" section enumerating backward-compatible vs breaking changes (new optional fields/params/endpoints OK; field removal or default-altering output changes require v2; 6-month deprecation notice with a `Sunset` HTTP header on the deprecated endpoints); added an "Error responses" section enumerating 400 / 404 / 429 / 503 with the `EmbeddingServiceUnavailableError → 503 {code:"embedding_service_unavailable"}` mapping documented (`services/api/src/index.ts` setErrorHandler + `services/api/src/routes/search.ts encodeQuery()`). Schema source of truth is the route file; docs are the bug if they drift. **Unblocks** the deferred-Later **Public developer API** workstream (`docs/plans/public-developer-api.md`) — that surface derives `/api/public/v1/*` from this contract, and trying to plan a tiered + Stripe-gated public surface against an unfrozen internal contract was the logjam. Auth tier annotations (free/dev/pro) are deliberately out of scope for v1.0 since they belong to that future workstream. **Now horizon empty for the first time on record.**

- **Roster hygiene fix — close stale `politician_terms` + SK retirement detection** (commits `0114575` + `965fd3c`). Closes the Always-on TODO item that's been load-bearing as the documented pre-req for the new websites agent's schedule. Three slices in two commits. **Slice 1 (data correction):** one-shot SQL closed 1,196 `politician_terms.ended_at=NULL` rows on already-inactive provincial politicians (4 AB + 232 MB + 960 QC) — `UPDATE politician_terms SET ended_at = COALESCE(started_at, NOW())` filtered on `is_active=false`. The drift was latent: `is_active` was already correct (matched chamber sizes) and no production view consumes the open-term count, but any future `pt.ended_at IS NULL` "sitting now" query would have over-counted (QC reported 1083, MB 287). **Slice 2 (MB + QC scheduling):** added `ingest-manitoba-mlas` and `ingest-quebec-mnas` to `services/scanner/src/jobs_catalog.py` + `services/api/src/routes/admin.ts COMMAND_CATALOG` (two-place whitelist sync per CLAUDE.md § Admin panel); seeded weekly `scanner_schedules` rows at Sun 03:30 / 03:45 UTC. Both wrappers in `opennorth.py` already called `_ingest_set()` → `detect_retirements()` on full fetches (default `--limit=200` covers QC's 124 + MB's 56 with margin) — they just never ran. **Slice 3 (SK new code):** SK was the only production-breaking case (159 active politicians vs 61-seat chamber, polluting `v_websites_missing` + `v_socials_missing` + the would-be agent input queue). SK politicians are keyed on `sk_assembly_slug` with `source_id=legassembly.sk.ca:N-speaker-index:slug` — the canonical `compare_politicians.detect_retirements()` matches on opennorth-shaped `source_id` prefixes only, so it can't see SK rows. Couldn't fix by scheduling the opennorth-side `ingest-saskatchewan-mlas` either: opennorth keys on its own `source_id`, would create dup politician rows, and orphan all existing speech attribution (set via `sk_assembly_slug`). New `_detect_sk_retirements()` helper in `services/scanner/src/legislative/sk_mlas.py` mirrors the canonical helper's body but identifies candidates by `(level='provincial', province_territory='SK', is_active=true)`. Three-step retirement: `politician_changes` audit row + close open `politician_terms` + flip `is_active=false`. **Critical safety gate:** `SK_RETIREMENT_ROSTER_FLOOR=55` — SK's Hansard speaker index only lists members who have spoken, so a sparse fetch or sitting-but-silent MLA early in a parliament could leave the parsed roster under-populated; below the floor the retirement pass is skipped entirely with a warning + `stats.failures` entry. Pass only fires when `SK_CURRENT_PARLIAMENT=30` is in the parliaments list (so `--parliaments 28,29` historical-roster runs don't trigger it). **Outcomes:** SK active 159 → 61 (exact 30L chamber match); 98 retirement audit rows landed; spot-checked 5 (Wyant/Wilson/Vermette/Stewart/Steele) all real former SK MLAs not in 30L. SK schedule seeded weekly Sun 04:00 UTC. The earlier websites-agent TODO entry's stated "fix Roster hygiene first" pre-req is now satisfied; only budget remains as the gate to enable that schedule.

- **Websites-provenance + Tier-3 Sonnet discovery agent + `/admin/websites` review queue** (commits `10c9f75` + `930f5aa`). Mirrors the `politician_socials` provenance shape (migration `0026`) onto the `websites` table so agent-discovered politician sites become auditable and routable through a low-confidence review queue. **Migration 0047** adds six columns to `websites` (`source` / `confidence` / `evidence_url` / `flagged_low_confidence` / `discovered_at` / `last_verified_at`), a partial index on flagged rows, and a `v_websites_missing` coverage view (politicians with no active website row AND no denormalised `personal_url` / `official_url`). **`websites_agent.py`** is the Sonnet 4.6 + `web_search_20250305` agent paralleling `socials_agent.py` — hard cap 3 searches per politician, dual confidence thresholds (≥0.85 auto-promote / 0.60–0.85 flag for review / <0.60 reject), batch of 10 politicians with `max_batches=20` default. **`websites.py`** is the canonicalize+upsert helper (lowercases hostname, strips `www.` prefix, drops tracking params, rejects non-http(s) schemes). **`agent-missing-websites`** Click command registered in `__main__.py`, whitelisted in both `jobs_catalog.py` and `admin.ts COMMAND_CATALOG` (two-place whitelist per CLAUDE.md § Admin panel). **`/admin/websites`** review UI mirrors `/admin/socials` byte-for-byte — same `useAdminFetch` + `adminFetch` pattern, same stats card + paginated flagged-row table + per-row approve/reject buttons; field swaps are `platform→label`, `handle→hostname`. Four new backend routes in `admin.ts` paralleling the socials trio (`GET /websites/coverage`, `GET /websites/flagged`, `POST /websites/:id/approve`, `POST /websites/:id/reject`); `AdminWebsitesReview.tsx` page + `main.tsx` route + `AdminLayout.tsx` NavLink. **State on ship:** 354 agent-discovered rows already existed in the table from prior on-demand runs (236 `agent_sonnet` + 118 `agent_session`); 144 of those are `flagged_low_confidence=true` and sitting in the new review queue ready for an operator pass. **Agent not enabled — no `scanner_schedules` entry seeded.** Per-run cost is ~$0.35–$0.50 on Anthropic (~2K politicians × 3 searches × Sonnet pricing). To enable a weekly cadence, paste one INSERT into psql (see [`docs/operations.md`](./operations.md) § Agent-driven enrichment). Pre-req: fix Roster hygiene first or ~25–30% of QC + MB + SK budget will chase ex-politicians. **Drive-by fix:** `socials_agent.py` search budget tightened 3 → 2 per (politician, platform) with explicit `max_uses` on the `web_search_20250305` tool — keeps the existing socials agent from spiralling on the same constraint as the new websites agent.

### Cycle 2026-05-06

- **Tier-2 Pass 4 — cross-jurisdictional named-speaker resolver + MB ingester fix** (`59f0c7a` + pending). Targeting the largest remaining attribution gap on the corpus: chamber-parser misses on regular MLA speeches with named labels (`Mrs. Driedger`, `Mr. McFadyen`, `Hon. Erin Selby (Minister of Health)`) where `speaker_role` is NULL/empty and the chamber parser couldn't FK-resolve. Cross-jurisdictional volume: MB 44K, ON 15K, NB 9.5K, AB 4.4K candidates — total ~73K rows. Two-phase ship. **Phase B** (`59f0c7a`): fixed `mb_former_mlas` to handle 4 banner format variants on gov.mb.ca (Mc/Mac-prefixed surnames, no-comma `SELBY Erin` shape, split-strong `McFADYEN, / Hugh` shape, `<p>`-only `McGIFFORD, Diane` shape) plus a pre-existing precedence bug where ministerial-portfolio spans masqueraded as MLA tenure spans on the living page. MB politicians 798 → 880 (+82). **Phase C** (pending): new `named_speaker_resolver.py` + `resolve-named-speakers` Click command. Three confidence buckets (0.85 full-name, 0.80 surname-single, 0.75 date-window-narrowed). Pre-filters vocatives, parliamentary staff, generic placeholders. Cross-jurisdictional regex handles AB-style `Mr. R. Miller` initial-first, `Mr. Schow moved` trailing-verb, `Member Brar` honorific variant. **Outcomes (cross-jurisdictional total ~30K speeches resolved):** **MB 81.1% → 86.2%** (+21K, includes a re-run after the bug fix); **AB 90.5% → 91.1%** (+2.6K); **ON 86.6% → 87.5%** (+4.6K — small relative because ON was already saturated); **NB 61.6% → 62.2%** (+1.3K, limited by surname ambiguity — NB `politician_terms` are sparse on date windows so the disambiguation step couldn't fire); **BC barely moved** (BC's 28K NULL rows are pre-1990 ALL-CAPS forms handled by `bc_allcaps_resolver`, not Pass-4 territory). 16/16 MB + 12/12 AB + 16 ON/NB random spot-checks correct against Wikipedia/historical record. **Bonus fix shipped same cycle:** the `mb_former_mlas` event regexes were missing two filler patterns: "Not a candidate **in** g.e. <date>" (Oscar Bjornson's 1966 → NULL term-closer) and "Elected **by-election** <date>" (Hugh McFadyen's 2005 first-term opener). Both added to `_EVENT_END_RE` / `_EVENT_START_RE`. The term upsert was changed from SKIP-on-conflict to UPDATE-`ended_at`-when-differs so the parser fix propagates into existing data on re-run. Closed Oscar Bjornson's stale open-ended term and restored McFadyen's missing 2005-2007 first term. NB Pass-4 lift is bounded by NB politician_terms sparseness — a future NB roster backfill (similar to AB/MB former-MLA work) would unlock more hits. **NB roster backfill shipped same cycle** (`nb_former_mlas.py` + `ingest-nb-former-mlas`): 85-MLA hand-curated roster covering 58L-60L (2014-2024) sourced from per-Legislature Wikipedia articles (legnb.ca's former-members directory 404s; legacy gnb.ca/legis/bios endpoints 500). Per-Legislature term windows with mid-Leg overrides for known resignations / by-elections / deaths. +55 new NB politicians, +153 term rows. **NB Pass 4 re-run: 62.2% → 94.2%** (+7,400 speeches, +32pp — biggest single-province lift of the session). Spot-check 12/12 correct (Higgs/Flemming/Shephard/Anderson-Mason/Legacy/Holder/LeBlanc/Johnson). NB now at #3 across all 10 jurisdictions, behind only NT (97.1%) and BC (95.1%).

- **Tier-2 Pass 3 phase 1d — MB Deputy Speaker (43L).** Single-entry slice closing MB's role-only `The Deputy Speaker` bucket. Tyler Blashko (Lagimodière, NDP), Deputy Speaker from 2023-11-21 (43rd Legislature opening, same day as Tom Lindsey's Speaker acclamation) → present. New `ROLE_ONLY_PRESIDING_ROSTER["MB"]` plus `ROLE_ONLY_OFFICE_MAP["MB"]["The Deputy Speaker"] = "Deputy Speaker"`. **Why so small:** the MB chamber parser only emits the role-only `The Deputy Speaker` shape for the current 43rd Legislature. Pre-43L Deputy Speaker turns came with inline names like `Madam Deputy Speaker (Marilyn Brick)` which Pass 1's `inline_presiding_resolver` already attributes — that's where MB's pre-existing 91.5% coverage came from. Confirmed Blashko by querying Pass-1 attributed rows: all 23 attributed Deputy Speaker turns map to him. **Outcomes:** MB `The Deputy Speaker` 0% → **100%** (1,027 speeches at confidence 0.85). Spot-checked 6 random rows; all 6 attribute to Blashko within his 2023-11-21 → present window. Out of scope: MB `The Chairperson` rotating-role bucket (~1,262 rows, same shape as deferred AB/BC rotating buckets); MB Tier-1 chamber-parser empty-role gap (~46K rows where the parser left `speaker_role` NULL on regular MLA speeches — separate work, not in Tier-2 Pass 3's domain).

- **Tier-2 Pass 3 phase 1c — BC Deputy Speaker.** Adds BC to the role-only resolver infrastructure shipped in phase 1a/1b. 4-entry `ROLE_ONLY_PRESIDING_ROSTER["BC"]["Deputy Speaker"]` covering 39L→43L: Linda Reid (39L, 2009-08-25 → 2013-06-26, became Speaker for 40L) / Raj Chouhan (41L, 2017-06-22 → 2020-12-07, became Speaker for 42L) / Spencer Chandra Herbert (42L, 2020-12-07 → 2024-10-19) / Mable Elmore (43L, 2025-03-01 → present). BC's chamber parser tags these as bare `Deputy Speaker` (no "The" prefix), distinguished from AB in `ROLE_ONLY_OFFICE_MAP["BC"]` — the same office key joins both provinces' rosters but the role-text mapping diverges. **Roster sourcing differed from AB**: BC's `politicians.extras` doesn't carry presiding-officer history (BC LIMS GraphQL ingester populates lims_member_id and term info but not Deputy Speaker tenure), so the data came from per-Parliament Wikipedia pages for 41L+42L+43L plus targeted searches confirming Reid 2009 appointment and Elmore March 2025 appointment. **Outcomes:** BC `Deputy Speaker` 0% → 83.5% (+4,267 speeches resolved at confidence 0.85). 842 unattributed remain across three documented gaps where the Deputy Speaker holder couldn't be confirmed from public sources: 38L (2008 + first half of 2009, ~376 rows), 40L (2013-2017, ~408 rows — Linda Reid was 40L Speaker, Raj Chouhan was 40L Assistant Deputy Speaker, but the actual Deputy Speaker for that BC Liberal majority parliament wasn't named in the search results), and Feb 2025 (~58 rows between 42L Spencer Chandra Herbert end and Mable Elmore March 2025 appointment). Spot-checked 12 random rows across 39L/41L/42L/43L windows; all 12 attribute to historically-correct holder. **Out of scope:** BC `Chairman` / `The Chair` rotating-role buckets (~24K rows total) — same shape as the deferred AB Acting Speaker work; would need external rotation source.

- **Tier-2 Pass 3 phase 1b — AB pre-2005 Deputy Speaker + Deputy Chair of Committees full chain.** Closes the two AB single-person date-determined role-only buckets to 100%. Two roster extensions to `ROLE_ONLY_PRESIDING_ROSTER["AB"]`: pre-Marz Deputy Speaker filled with Donald A. Tannas (1993-08-30 → 2005-03-01, covers 23L+24L+25L); Deputy Chair of Committees added as a new office with 10 entries spanning 1997-04-14 → present (Gordon 24L → Shariff 25L+26L → Mitzel + Zwozdesky 27L mid-Leg cabinet swap → Jablonski 28L → Feehan + Sweet 29L mid-Leg cabinet promotion → Milliken + Reid 30L mid-Leg cabinet promotion → van Dijken 31L). `ROLE_ONLY_OFFICE_MAP["AB"]` extended with `"The Deputy Chair" → "Deputy Chair of Committees"`. **Roster source insight:** `politicians.extras.ab_member_info.offices` (the assembly.ab.ca structured member records ingested by `enrich-ab-mlas`) already had complete tenure data for both offices — found by exploring extras rather than scraping Wikipedia or further mining Hansard. Already-ingested data, just needed querying. **Outcomes:** AB `The Deputy Speaker` 85.4% → **100%** (+2,828 newly resolved on top of phase 1a); AB `The Deputy Chair` 0% → **100%** (+11,631); 16/16 random spot-checks correct across 24L–31L. Two short Deputy-Chair vacancy gaps acknowledged (Feehan→Sweet 2016-02-08 to 2016-03-09 ~1 month, Milliken→Reid 2022-06-27 to 2022-11-30 ~5 months — office vacant during cabinet promotion of incumbent). First-name fallback in `_find_politician_id` absorbed `Don Tannas → Donald A. Tannas`, `Richard Marz → Richard A. Marz`, `Judith Gordon → Judith D. Gordon`, `Leonard Mitzel → Leonard W. Mitzel` automatically. **Total AB attribution 87.2% → 90.5%** (cumulative across phase 1a + phase 1b: +30,994 speeches resolved at confidence 0.85). Phase 2 (Acting Speaker / Chair, ~30K rotating-role rows) **deferred** with documented rationale — AB Hansard genuinely lacks chair-handover narratives; the only `took the chair` mentions are speakers describing the chair, not announcements of who's holding it. Build cost of external rotation scraping is high relative to the marginal ranking lift on these mostly-procedural rows.

- **Tier-2 Pass 3 phase 1a — role-only presiding resolver (AB Deputy Speaker scaffolding).** First slice of the largest remaining attribution gap (the TODO budgeted ~60K AB role-only rows alone). Pass 3 covers role-only labels where the chamber parser left `politician_id=NULL` because the speaker line carries no name — only the role title. AB's Hansard format does this for `The Deputy Speaker`, `The Deputy Chair`, `The Acting Speaker`, and `The Chair` rows. Pass 3 phase 1a tackles the **single-person date-determined** subset only (Deputy Speaker, ~19K rows); rotating roles are deferred. Implementation: new `ROLE_ONLY_PRESIDING_ROSTER` (dict of province → office → list[SpeakerTerm]) parallel to `SPEAKER_ROSTER` and `DEPUTY_PRESIDING_ROSTER`, plus `ROLE_ONLY_OFFICE_MAP` (province → speaker_role → office) so the resolver can join `speeches.speaker_role` to `politician_terms.office` for each per-province configuration. Helpers `ensure_role_only_presiding_politicians` / `ensure_role_only_presiding_terms` mirror the existing Speaker shape, and `resolve_role_only_presiding` iterates over the role map for a given province, joining each role's speeches against the right office's date-windowed terms. New Click command `resolve-role-only-presiding-officers` with `--province` scope; seed is idempotent at startup. Confidence 0.85 (single-person + perfect date match, but the chamber parser left this NULL for a reason — slightly below Tier-1 Speaker's 0.9). **Roster mining:** AB's Hansard *itself* records every Deputy Speaker election verbatim (`<Member> is hereby declared Deputy Speaker and Chair of Committees for the Legislative Assembly of Alberta for the Nth Legislature.`) — a single regex query on `speeches.text` turned out to be a higher-confidence source than any web scrape would have been, with exact election dates straight from the official record. AB roster: 5 entries spanning 2005-03-01 → present (Marz / Cao / Rogers / Jabbour / Pitt across 26L–31L). The existing `_find_politician_id` first-name fallback absorbed `Richard Marz` → DB's `Richard A. Marz` automatically. **Outcomes:** AB `The Deputy Speaker` attribution **0% → 85.4%** (+16,535 of 19,263 candidates resolved); 2,828 `no_term_match` are exactly the pre-2005 (24L+25L) acknowledged gap that would need an external roster. Total AB attribution now 87.2%. Spot-checked 12 random rows across all five Deputy Speaker windows; all 12 attribute to the historically-correct Deputy Speaker for the date. **Out of scope this round:** AB Deputy Chair (mid-Legislature replacements complicate the roster shape — a 2011-11-21 mid-26L re-election in the corpus shows multiple candidates and would need careful text-mining); rotating Acting Speaker / Chair (~30K AB rows, need same-document name propagation or external rotation source); pre-2005 Deputy Speaker (~2,800 rows); BC / ON / MB role-only propagation.

- **Tier-2 Pass 2 — date-windowed disambiguation (QC vertical slice).** Closes the 67% ceiling Pass 1 hit on QC. Layered a fourth narrowing step onto `inline_presiding_resolver.py`: when the existing surname → first-initial → exact-first-name chain still leaves >1 candidate, intersect with the set of politicians who held a Speaker / Deputy-Speaker term on the speech date. Implementation reuses every load-bearing pattern from the Tier-1 Speaker work — `SpeakerTerm` dataclass, `_find_politician_id` (with surname-only fallback for first-name drift), `_insert_minimal_politician` for stub-seeding historical Vice-Présidents not in the current MNA roster, DELETE-by-source-tag idempotency. New `DEPUTY_PRESIDING_ROSTER` + `DEPUTY_SOURCE_TAG = "deputy_presiding_seed"` parallel structure (rather than nesting `SPEAKER_ROSTER` and rewriting downstream callers). Per-province presiding-terms cache built lazily inside the resolver to avoid re-querying for every speech. Pass-2 hits bucketed separately and bulk-UPDATEd at confidence 0.80 (vs Pass-1 0.85) via a `GREATEST(confidence, $3)` floor that protects existing higher-confidence values. **QC roster:** 22 entries spanning 37L (2003-06-04, Charest I — Sirros / Leblanc / Gendron, with Cusano replacing Sirros mid-leg 2004-10-19) → 43L (2022-11-29, Legault II — Picard / Soucy / Benjamin / Lévesque). Roster checkpoint loop earned its keep: initial draft excluded Sylvain Lévesque from 43L based on a Wikipedia inconsistency (first fetch listed both him and Soucy as 2e VP, second fetch listed only Soucy); resolver-side miss-bucket diagnosis showed 3,087 unattributed `Le Vice-Président (M. Lévesque)` rows clustered 2023-2024, news-search confirmed Lévesque resigned 2024-11-06 after an ethics-commissioner finding and Sylvie D'Amours was elected to the seat 2024-11-07 (Le Devoir, Radio-Canada). One-day Wikipedia table artifacts (Picard 3e VP 2009-01-13, Ouimet 1er VP 2018-11-27 — both look like sworn-in-then-immediately-swapped) excluded after the user's "use your judgement" call. **Outcomes:** QC parens-attributed surface **67.4% → 96.7%** (+29.3 pp); 21,223 speeches newly attributed at confidence 0.80; total QC corpus attribution 78.0% → 79.0%. Acceptance bar was ≥90% — cleared by 6.7 pp. Spot-checked 12 random Pass-2 hits across 41L/42L/43L date ranges; all 12 match the historically-correct Vice-Président. No regression on Pass-1 hits across ON/MB/QC. Drive-by fix to a latent crash where the bare-name miss path referenced `m.group(...)` after `m` was None. AB/BC/ON/MB/SK/NT propagation deferred to follow-up cycles, one province at a time per user pref. **Plan + design history:** `/home/bunker-admin/.claude/plans/checking-out-the-todo-md-wise-robin.md`.

### Cycle 2026-05-05

- **SK Hansard + bills + presiding-resolver — full coverage column shipped in one cycle.** From `none` to `hansard=live, bills=partial` across 50,948 speeches (360 sittings spanning 29L1S → 30L2S) and 367 bills (8 sessions, 28L3S → 30L2S). Six commits land sequentially: MLA roster from speaker index (`7245dda`, migration `0044`, 61 30L MLAs with synthesised slugs since SK exposes no stable per-MLA ID); HTML pipeline with archive-paginated discovery (`e0a11fe`); daily schedule + 29L Mr./Ms./Mrs./Miss honorific fix (`60de19a`); presiding-speaker resolver with SPEAKER_ROSTER for 23L–30L (`c2820b3`, parser refactored to distinguish role='speaker' / 'deputy_speaker' / 'chair' / 'deputy_chair'; 29L4S 0% → 73.8% attribution); Hansard PDF parser for the 29L1S–29L3S all-PDF era (`2a8c99a`, +26,487 speeches; `hansard_status` flipped `partial` → `live`); bills ingester from `progress-of-bills.pdf` (`e6dcae1`, single-PDF tabular parser with per-page header detection because column widths drift across pages of the same PDF, `--selftest`/`--dry-run`/`--all-sessions` flags). Six commits in roughly that order; the multi-step PDF discoveries and sponsor FK iterations are documented in commit bodies.
- **Tier-2 attribution — Pass 1: inline-name presiding-officer resolver + BC pre-1990 ALL-CAPS resolver.** Two cross-jurisdictional quality-lift commits at the end of 2026-05-05 cycle. `bb41d88` adds `inline_presiding_resolver` for parens-bearing labels (ON +862, MB +1,961, QC +16 — QC capped at 67% by surname-only ambiguity, deferred to Pass 2). `b0b2562` adds `bc_allcaps_resolver` for `MR. G.S. WALLACE (Oak Bay)` / `HON. D. BARRETT (Premier)` shapes the existing `resolve-bc-speakers-dated` mishandled (extracted constituency-in-parens as surname); three-tier disambiguation (lastname+date / riding / first-initial), +11,017 BC speeches, **BC overall 92.5% → 94.4%**.
- **Provincial votes follow-ups** (commits `4c26b1e`, `e2ffa9d`). Votes-extractor v2 closes the AB/NS Speaker-truncated-question-call gaps and reshapes NB around heading-style detection (`Recorded Vote—…Defeated`); NB jumps 0 → 125 votes. Daily-ingest schedule entries seeded for all 8 provinces (BC/AB/QC/MB/NB/NL/NS/ON); total 10 votes-extraction schedules now live (Federal + NT + 8 new).
- **Stripe live-mode deploy + premium-reports analysis-jobs substrate** (evening of 2026-05-05, no commit yet — all migration + worker + frontend; six tasks shipped as one arc). **Stripe:** swapped `sk_test_…` → `sk_live_…` and three live `price_…` IDs (the `prod_…` vs `price_…` UI prominence trips first-timers — Stripe surfaces the product ID more visibly than the price ID); registered the webhook endpoint fresh in Dashboard live mode (separate list from test mode); corrected an initial `.ca` vs `.org` URL mismatch (live deploy is on `.org`, `.ca` doesn't currently resolve); cleared a stale test-mode `cus_…` off `users.stripe_customer_id` for both seeded users (Stripe customer namespace is per-mode and the live API rejects test-mode customer IDs). End-to-end validated with a real $5 charge → webhook → ledger row. Stripe Tax left **not activated**. **Analysis-jobs substrate:** Migration 0045 generalises `private.report_jobs` with a `kind` text column (CHECK enumerates `full_report | search_synthesis | stance_map | topic_pulse | narrative_timeline | voting_audit | compare_politicians`), `inputs` JSONB, `politician_id`/`query` relaxed to nullable, default `kind = 'full_report'` for back-compat. API extended via zod discriminated union; worker `KIND_HANDLERS` dispatcher with shared `run_map_reduce_pipeline` skeleton; `handle_full_report` lifted unchanged; new chunk-driven kinds share `_handle_chunk_driven_kind`. Concurrency 2 → 4 to keep K=500 under ~3min. Aggregations (party / top speakers / year span / language) computed server-side from chunks and passed to the reduce prompt as JSON; reduce prompts updated to emit a `<table class="report-stats">` at the top of the HTML; bleach allowlist extended for table tags. Frontend: generic `AnalysisButton` + `AnalysisConfirmModal` + `useAnalysisSubmit` substrate; `AIFullReportButton` etc. became thin shims. New `<SearchSetAnalysisRow>` on `/search` renders Synthesize + Stance map CTAs on both Analysis tab (default variant) and Timeline view (compact variant). User-controllable "Analyse top N" picker (preset 25/50/100/200/500 + "Other…" with slider+number-input dialog showing live cost preview, persisted to localStorage). `/facets` extended to return `chunk_ids` of its top-N CTE plus accept `?limit=` (clamped [10, 500]); `useSpeechFacets` hoisted to the page so the dashboard tiles + CTA share one fetch (`SearchDashboard` accepts an optional `facets` prop to dedupe). Kind chips on `/me/reports` list; kind-aware fallback titles in viewer when politician is null. Validated K=500: 217s wall time, 169K input + 39K output tokens, ~$1.10 model cost vs 55-credit charge. **Plan + design history:** `/home/bunker-admin/.claude/plans/slick-help-me-now-merry-stonebraker.md`.
- **Politician-socials + website hygiene catchup.** End-of-day operational cycle (no commit; all DB-side). All 6,114 social rows re-verified after 8-day drift (4,957 live / 9 dead / 1,148 stuck-unknown on FB+LinkedIn anti-bot residual; 2 fresh live→dead flips). All 2,353 tracked websites re-scanned after a 21-day drift; sovereignty distribution refreshed — tier 1 85 → 90, CA-hosted 1,481 → 1,502, US-hosted 512 → 496 since the 2026-04-14 baseline. `enrich-legislatures` + `enrich-municipal` + wikidata retry confirmed per-jurisdiction website discovery is at its structural ceiling — **0 new personal sites discovered across all 12 provinces/territories**; the 452 wikidata upserts were mostly metadata-refreshes on already-known handles, not new pols. Manual research filled 5 high-confidence socials (federal MP Jasraj Hallan FB/IG/LinkedIn, Calgary councillor Kim Tyers FB, Surrey BC councillor Harry Bains Twitter); territorial gap confirmed as **real-world absence** (NU/YT MLAs largely don't run public socials; the 17 NU `<riding>mla.ca` domains turned out to be GoDaddy-style parked pages redirecting to `/lander`). **Cleanup batch shipped:** 22 NU parked-domain `personal_url`s nulled out + 25 corresponding websites deactivated; defeated-Nov-2025 QC councillors Jean Fontaine and Marianne Lambert marked inactive; Bertrand Collins's personal site bertrandcollins.ca recorded. **Two new weekly `scanner_schedules` rows** — `verify-socials` Sun 05:00 UTC + `scan` Sun 06:00 UTC — close the manual-cadence drift that motivated the catchup. **One bug surfaced as a TODO:** QC and MB enrichers don't close `politician_terms.ended_at` for politicians dropped from upstream rosters (QC inflates to 1,082 sitting MNAs vs the chamber's 125; MB to 256 vs 57); SK `politicians.is_active` similarly stuck on former MLAs. Tracked under TODO.md "Always-on" as *Roster hygiene*.

### Cycle 2026-05-04

- **Federal `bill_events` from LEGISinfo XML + MB/NS `introduced_date` SQL backfill** (2026-05-04). Closes the federal stage-timeline gap that `services/scanner/src/legislative/federal_bills.py` self-documented at line 18-22 as "openparliament doesn't expose them — separate follow-up." New `federal_bill_events.py` ingester walks parl.ca/LegisInfo XML; the list-level XML carries inline `Passed{Chamber}{Stage}DateTime` fields per bill, so one HTTP GET per session unlocks ~7 milestone timestamps without per-bill detail fetches. **9,970 federal events** landed across 15 sessions on first backfill (P37-S1 → P44-S1); 5,471/5,542 bills matched (98.7% via `bills.raw->>'legisinfo_id'` exact join); spot-checked C-11/C-21/C-27 against LEGISinfo web view. Bundled with a **pure-SQL `relink-bill-introduced-dates` command** that backfills `bills.introduced_date` from any first_reading event — closes MB (0 → 81) and NS (0 → 2,114) immediately, and is generic across jurisdictions for future use. Daily 11:45 UTC federal events slot + 07:55 UTC cross-jurisdictional intro-date relink slot.
- **NL + AB historical `bill_events` parser fixes** (2026-05-04). Two single-line bugs uncovered while inspecting coverage gaps. NL `_parse_date` learned the older `"Mar. 5/2012"` shape (period-abbreviated month, slash separator) — bills with first-reading events 198 → 573, total NL events 914 → 2,737. AB `_AM_PM_RE` learned to strip the `eve` evening-sitting suffix — total AB events 4,973 → 6,599 (+33%). The remaining AB gap (10K bills with no events at all) is upstream Assembly Dashboard sparseness, not parser-side.
- **ON + QC `introduced_date` completion** (2026-05-04). **ON**: single 11-line `if/elif/else` branch in `on_bills.py:extract_status_events` to parse pre-P42 sessions' 4-column `/status` table in addition to the 5-column P42+ layout. Re-parsed cached HTML — no upstream calls. ON `bills.introduced_date` 786 → **3,414 (100%)**; total ON `bill_events` 4,755 → **17,820** (+13,065 historical events as a secondary win). **QC**: new `fetch-qc-bill-introduced-dates` Click command + sibling function in `qc_bills.py` extracts the `Sitting held on <date>` token from each detail page's `<h3>Introduction</h3>` block, writes `bill_events` first-reading rows, then the existing `derive_qc_introduced_dates` SQL pass rolls them up onto `bills.introduced_date`. Daily 16:35 UTC schedule slot. QC partial this cycle: **173 → 368 (30.9%)** — assnat.qc.ca rate-limits aggressive backfill (silent-timeout slow-loris kicked in after ~190 fetches); the remaining ~824 will catch up over a few daily runs since the SELECT filters on `introduced_date IS NULL` and each run only touches the still-undated tail.

For context on what just landed, so this doc reads against a known baseline.

### Cycle 2026-05-01 → 2026-05-03

- **Search perf lift + speech-detail rebuild + graph-view exploration** (2026-05-03, bulk commit `1df626f`). Three workstreams that landed together: (a) **API perf** — pg pool 10 → 30 (25/25 burst test, was 13/25), query-embedding LRU (60s/500), result-cache LRU in `useSpeechSearch`, `include_count` split off the hot path, `source_system` denorm; (b) **Search exploration UI** — `anchor_chunk_id` parameter on `/search/speeches` re-centres the ranking on a specific chunk, surfaced in the UI by the new `AnchorChunkBanner` and `SearchMapView` (radial graph of top-K results, click a satellite to make it the new anchor); (c) **Speech-detail rebuild** — new `SpeechDetailPage` with `ExchangeSpeechRow` (back-and-forth turn rendering), `RelatedSpeechesPanel` (list + graph modes), `QuoteShareMenu`, `MapleLeafLoader`, plus helper libs `speechHelpers` / `textHighlight` / `videoEmbedUrl`. Edge: nginx switched to variable-based `proxy_pass` with Docker embedded DNS resolver (kills the ~30s 502 window on `docker compose up -d --build api`); `/docs/` root-anchored in `.gitignore` as the durable fix for the runbook accidental-add issue. Runbook trail: [`runbooks/handoff-2026-04-27-semantic-explore-graph-redesign.md`](./runbooks/handoff-2026-04-27-semantic-explore-graph-redesign.md), [`runbooks/handoff-2026-05-02-search-efficiency-and-laptop-capacity.md`](./runbooks/handoff-2026-05-02-search-efficiency-and-laptop-capacity.md).
- **Provincial historical bills — workstream complete.** `--all-sessions` walker pattern propagated to ON (2026-05-01: 111 → 3,412 across P36-P44), then MB + QC (2026-05-02: 81 → 1,971 across 31 MB sessions; 497 → 1,192 across 8 QC sessions via new `discover_qc_bills_html` against assnat.qc.ca historical session pages). All three provinces flipped `bills_status='live'`. QC joins federal/AB/BC/NS/ON with all three legislative columns at live. Commits `8d39fb7` (ON), `11be12d` (MB+QC).
- **Federal vote→bill linkage 10.2% → 54.7%** (2026-05-01) via new `relink-federal-votes` Click subcommand — pure-SQL UPDATE pass against `votes.raw->'openparliament_vote'->>'bill_url'`. 1,993 newly linked, 0 unmatched. Avoided re-fetching 1.45M ballots. Commit `3a90893`.
- **ON / QC bills `introduced_date`** (2026-05-01) — ON parser-side patch derives from /status sub-page first_reading events, hits 100% on P42-P44 (786/786). QC partial via RSS-window roll-up. Older parliaments need separate work — documented gap. Commit `3a90893`.
- **`bills_status` auto-derivation** (2026-04-30) — `coverage_stats.py` now flips all three legislative status columns (hansard / votes / bills) from row counts. 500-row threshold for 'live'. Five overstated jurisdictions honestly downgraded; federal first to flip all-three-live, NS from stale 'partial' to 'live'. Commit `bdc791f`.
- **Federal historical bills backfill** (2026-04-30) — `ingest-federal-bills --all-sessions` walks every federal session in `legislative_sessions`. 412 → **5,542** bills across P37-S1 → P44-S1 (openparliament.ca coverage floor at 2001). Sponsor FK rate 86.5%. Same commit as auto-derive (`bdc791f`).

### Cycle 2026-04-26 → 2026-04-30

- **Provincial votes extractors — 8 jurisdictions live** — one self-contained module per province (`bc_votes.py`, `ab_votes.py`, `qc_votes.py`, `on_votes.py`, `mb_votes.py`, `ns_votes.py`, `nl_votes.py`, `nb_votes.py`); **7,303 provincial votes added** (QC dominant at 2,961 with 1,840 numerical divisions). Schema validated against every `vote_type` value (division/consensus/acclamation). Runbook: [`runbooks/handoff-2026-04-30-provincial-votes.md`](./runbooks/handoff-2026-04-30-provincial-votes.md) (2026-04-30).
- **Federal votes extractor live + full historical backfill complete** — openparliament.ca structured-JSON pipeline; **4,481 divisions / 1.45M vote_positions / 18.5 years (2006-05 → 2024-12) / 99.98% politician-FK** via `openparliament_slug` exact match (the cleanest votes pipeline in the project). Daily 11:30 UTC schedule slot. Runbook: [`runbooks/handoff-2026-04-30-federal-votes.md`](./runbooks/handoff-2026-04-30-federal-votes.md) (2026-04-30).
- **Migration 0018 votes applied + NT votes extractor live** — closes the project's longest-parked migration. NT consensus-government data validates the schema without revisions; 31 consensus votes across 2013-2026 NT Hansard corpus, all `vote_type='consensus'` with NULL ayes/nays (NT doesn't publish per-member positions). Daily 21:50 UTC NT votes-extraction schedule slot. Runbook: [`runbooks/handoff-2026-04-30-votes-layer.md`](./runbooks/handoff-2026-04-30-votes-layer.md) (2026-04-30).
- **NT Hansard pipeline live** — Drupal HTML scrape of ntlegislativeassembly.ca with per-turn `<a href="/meet-members/mla/{slug}">` direct slug-FK speaker attribution (the cleanest pattern of any sub-national pipeline so far). 19 current MLAs slug-stamped + 117 former MLAs inserted; 13th-20th Assembly Speaker roster seeded into `presiding_officer_resolver`. Migration 0041 added `nt_mla_slug` to politicians (CLAUDE.md convention #1). Daily 21:30 UTC schedule slot. Runbook: [`runbooks/handoff-2026-04-29-nt-hansard.md`](./runbooks/handoff-2026-04-29-nt-hansard.md) (2026-04-29).
- **BC pre-P35 historical roster + dated resolver + extended Speaker roster** — Wikipedia per-parliament wikitable parser ingested 160 pre-1992 MLAs across P29-P34 + 359 per-parliament term rows; new `resolve-bc-speakers-dated` CTE with inline surname extraction; `SPEAKER_ROSTER["BC"]` extended back from P38 to P29 (+13 historical Speakers). BC corpus attribution lift: 67.6% → 91.9% (+136K speeches, +20K Speaker-tagged, +186K chunks). Runbook: [`runbooks/handoff-2026-04-29-bc-pre-p35-roster.md`](./runbooks/handoff-2026-04-29-bc-pre-p35-roster.md) (2026-04-29).
- **TEI + scanner-side embed resilience layer** — device-aware TEI healthcheck (single-token /embed, fails on CPU fallback), restart-on-failure cap, preflight inference-latency check, per-batch exponential backoff, abort-on-5-consecutive-failures guard. Closes the GPU-regression silent-CPU-fallback gap. (2026-04-28)
- **`chunk-and-embed-speeches` daily schedule** — single combined Click command + `scanner_schedules` row at 08:00 UTC (02:00 Mountain), atomic chunk → embed ordering in one process. (2026-04-29)
- **BC + QC historical-roster backfills** — QC alphabet-walk of assnat.qc.ca (2,090 career spans extracted; migration 0038 promoted `qc_assnat_id` to UNIQUE partial after merging an Open North Éric/Eric Girard duplicate); BC `enrich-bc-member-parliaments` (LIMS GraphQL `allMemberParliaments`, 750 edges, BC terms 103 → 853). QC P39-P42 +12-33% on resolution. (2026-04-27)
- **BC pre-P38 Hansard parser** — era-branching `bc_hansard_parse.py` extension for legacy ALL-CAPS (P29-P34) + mixed-case (P36-P37) markup. +378K BC speeches; corpus ~198K → ~577K. (2026-04-27)
- **ON historical MPP roster + pre-2007 Hansard parser** — propagated date-windowed-resolver pattern to ON. (2026-04-26)

### Cycle 2026-04-23 → 2026-04-26

- **Phase 1b — premium reports** — `report_jobs` table (migration 0035), `reports-worker` compose service, OpenRouter map-reduce, sanitised-HTML viewer at `/reports/<id>`, refund flow (released-hold vs compensating admin-credit), admin triage queue. First credit-spending feature.
- **Phase 1c #1 — public-share + citation** — `is_public` flag (migration 0036), `/public/reports/<id>` route, citation block in viewer footer (2026-04-24).
- **ON Hansard pipeline** — name-based resolution + parens-name extraction; six ON commands packed into the 18:00 UTC daily-ingest slot (2026-04-24).
- **`/api/v1/search` filter expansion** — `min_similarity`, `parliament_number + session_number`, and `speech_type` filters on `baseFilterSchema`; new `/search/sessions` endpoint backing the cascading parliament/session dropdown; advanced-filters disclosure on `/search` (2026-04-26).
- **Operational hygiene** — `OPENROUTER_MODEL` → `OPENROUTER_CONTRADICTIONS_MODEL` rename with legacy fallback + boot-time deprecation warning; `scripts/backup-database.sh` hardened (`.env`-sourced knobs, file-pinned `DB_PASSWORD`, zstd default 19 → 3); BetaBadge in the site header; `docs/api.md` Search section finally written (2026-04-26).
- **Stripe Tax wiring** — `STRIPE_TAX_ENABLED` env flag, `automatic_tax` + address collection + `tax_id_collection` on the Checkout Session when on, `tax_enabled` field on `/me/credits/packs`, frontend disclosure on `/account/credits`, plan-doc + operations.md activation checklist. Default off — operator dashboard activation (Stripe Tax + Canadian registration + per-product tax codes) is the remaining deploy step (2026-04-26).

